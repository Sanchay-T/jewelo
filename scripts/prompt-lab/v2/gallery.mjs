import {readFileSync,writeFileSync,existsSync,mkdirSync} from 'node:fs';
import {resolve,relative} from 'node:path';
import {OUT,read} from './prepare.mjs';
import {auditVerdict,budgetStatus} from './ledger.mjs';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const state=read(resolve(OUT,'ledger.json')),campaign=read(resolve(OUT,'campaign.json')),cases=read(resolve(OUT,'cases.json'));
const nameOrder=['ليان','نور','إيمان','Ava','Lily','Christopher'];
const ordered=[...cases].sort((a,b)=>nameOrder.indexOf(read(resolve(OUT,a.promptFile)).config.name)-nameOrder.indexOf(read(resolve(OUT,b.promptFile)).config.name)||a.method.localeCompare(b.method)||a.repeat-b.repeat);
const cards=ordered.map(c=>{
 const compiled=read(resolve(OUT,c.promptFile)),e=state.entries.find(e=>e.caseId===c.id),verdict=e?auditVerdict(e,e.audit,campaign.rubricHash):'not_submitted';
 const image=e?.output?`<a href="${esc(e.output)}"><img loading="lazy" src="${esc(e.output)}" alt="${esc(c.blindId+' '+compiled.config.name)}"></a>`:`<div class="placeholder">${esc(e?.status??'Not submitted')}</div>`;
 return `<article id="${esc(c.blindId)}" data-script="${esc(compiled.config.script)}" data-name="${esc(compiled.config.name)}" data-verdict="${esc(verdict)}"><h2>${esc(c.blindId)} · <bdi>${esc(compiled.config.name)}</bdi> <span class="verdict ${verdict}">${esc(verdict)}</span></h2><div class="reveal"><p>${esc(c.method)} · repetition ${c.repeat} · ${esc(e?.taskId??'No task')}</p><details open><summary>Exact submitted prompt</summary><pre>${esc(compiled.prompt)}</pre></details><p>References: ${compiled.referenceDescriptors.map(r=>`<a href="${esc(relative(OUT,resolve(OUT,'../../',r.path)))}">${esc(r.id)}</a>`).join(' · ')||'None'}</p></div>${image}<div class="details">${e?.output?['tl','tr','bl','br'].map(q=>`<a href="crops/${c.blindId}-${q}.png">${q.toUpperCase()} detail</a>`).join(' · '):''}</div><p>${esc(e?.audit?.notes??'Awaiting review.')}</p><div class="reveal"><p>Style: ${esc(e?.audit?.styleScore??'—')}/5 · appeal: ${esc(e?.audit?.appealScore??'—')}/5</p><small>Prompt ${esc(compiled.promptHash)}<br>Output ${esc(e?.outputHash??'—')}</small></div></article>`;
}).join('\n');
const budget=budgetStatus(state);
writeFileSync(resolve(OUT,'review.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Arabic name pendants · v2 comparison</title><style>
*{box-sizing:border-box}body{margin:0;background:#f4f1ea;color:#20231f;font:16px/1.5 system-ui}header{padding:32px 4vw;background:#172821;color:#f8f5ea}h1{font-size:32px;margin:0 0 10px}header p{max-width:1000px}nav{position:sticky;top:0;padding:12px 4vw;background:#e1dfd5;z-index:1;display:flex;gap:12px;flex-wrap:wrap}button,select{font:inherit;padding:8px}main{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:24px;padding:28px 4vw}article{background:white;border:1px solid #d3d6cf;border-radius:12px;overflow:hidden;padding:20px}article h2{font-size:20px;margin:0 0 8px}img{display:block;width:100%;height:auto;background:#eee}pre{font:12px/1.5 ui-monospace,monospace;white-space:pre-wrap;max-height:340px;overflow:auto;background:#f4f4ef;padding:12px}a{color:#245e47}small{overflow-wrap:anywhere;color:#56615b}.verdict{font-size:12px;border-radius:10px;background:#eee;padding:4px 8px}.pass{background:#dcefd7}.reject{background:#f6d7d3}.needs_review{background:#fff0bd}.placeholder{height:280px;background:#eee;display:grid;place-items:center}.details{margin-top:12px}.blind .reveal{display:none}[hidden]{display:none!important}@media(max-width:850px){main{grid-template-columns:1fr}}
</style><header><h1>Arabic name pendants · results first</h1><p><strong>Arabic: 1 / 24 passed · 19 rejected · 4 uncertain.</strong> English comparison: 16 / 24 passed. Arabic remains unsolved; one passing image does not establish a reliable prompt.</p><p><strong><bdi>ليان</bdi>:</strong> 0 / 8 passed · <strong><bdi>نور</bdi>:</strong> 1 / 8 passed · <strong><bdi>إيمان</bdi>:</strong> 0 / 8 passed. <a style="color:white" href="#V2-43">See the passing Noor image, V2-43</a>.</p><p>Original name-specific designs, four reference methods and two repetitions. Prompts appear above each image; detail links preserve the original output. Failures and uncertainties remain visible.</p><p>${state.entries.length}/48 screening submissions · ${state.entries.filter(e=>e.status==='succeeded').length} outputs saved · observed debit ${budget.reconciledSpend} credits · ${budget.reservedCredits} credits reserved/unreconciled. Maximum60 new calls /2460 credits, with12 continuations conditional on the screening gate.</p><p><strong>${esc(state.selection?.winner?'Selected method: '+state.selection.winner:state.selection?'No method met the advancement gate.':'Screening in progress; no method selected.')}</strong> This is research, not a99% reliability or production qualification.</p><a style="color:#fff" href="report.md">Report</a> · <a style="color:#fff" href="ledger.json">Ledger</a> · <a style="color:#fff" href="rubric.json">Frozen review rubric</a></header><nav><button id="blind">Hide methods and prompts for review</button><label>Show <select id="script"><option value="Arabic" selected>Arabic — 24 images</option><option value="English">English — 24 images</option><option value="">All 48 images</option></select></label><select id="name"><option value="">All names</option>${[...new Set(cases.map(c=>read(resolve(OUT,c.promptFile)).config.name))].map(n=>`<option>${esc(n)}</option>`).join('')}</select><select id="verdict"><option value="">All outcomes</option>${['pass','reject','needs_review','pending','not_submitted'].map(v=>`<option>${v}</option>`).join('')}</select></nav><p id="visible-count" style="padding:0 4vw" aria-live="polite"></p><main>${cards}</main><script>const b=document.querySelector('#blind');function toggle(){document.body.classList.toggle('blind');b.textContent=document.body.classList.contains('blind')?'Reveal methods and prompts':'Hide methods and prompts for review'}b.onclick=toggle;if(new URLSearchParams(location.search).has('blind'))toggle();function filter(){const script=document.querySelector('#script').value,name=document.querySelector('#name').value,verdict=document.querySelector('#verdict').value;let count=0;document.querySelectorAll('article').forEach(a=>{a.hidden=!!((script&&a.dataset.script!==script)||(name&&a.dataset.name!==name)||(verdict&&a.dataset.verdict!==verdict));if(!a.hidden)count++});document.querySelector('#visible-count').textContent='Showing '+count+' '+(script||'Arabic and English')+' images, grouped by name, method and repetition.'}document.querySelector('#script').onchange=()=>{document.querySelector('#name').value='';filter()};document.querySelector('#name').onchange=()=>{const name=document.querySelector('#name').value;if(name)document.querySelector('#script').value=/[\u0600-\u06ff]/.test(name)?'Arabic':'English';filter()};document.querySelector('#verdict').onchange=filter;document.querySelector('a[href="#V2-43"]').onclick=()=>{document.querySelector('#script').value='Arabic';document.querySelector('#name').value='نور';document.querySelector('#verdict').value='';filter()};filter();</script></html>`);
console.log(JSON.stringify({gallery:resolve(OUT,'review.html'),submissions:state.entries.length}));
