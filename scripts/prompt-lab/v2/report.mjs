import {readFileSync,writeFileSync} from 'node:fs';
import {resolve} from 'node:path';
import {OUT,ROOT,read} from './prepare.mjs';
import {hash} from './compiler.mjs';
import {METHODS,HARD_GATES,auditVerdict,budgetStatus} from './ledger.mjs';
const state=read(resolve(OUT,'ledger.json')),campaign=read(resolve(OUT,'campaign.json')),cases=read(resolve(OUT,'cases.json'));
const entries=state.entries,counts=Object.fromEntries(['pass','reject','needs_review','pending'].map(v=>[v,entries.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)===v).length]));
const methods=METHODS.map(method=>{
 const group=entries.filter(e=>e.method===method),pass=group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='pass').length;
 return {method,submitted:group.length,pass,reject:group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='reject').length,uncertain:group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='needs_review').length,eligible:state.selection?.methods.find(m=>m.method===method)?.eligible??null};
});
const nameResults=[...new Set(entries.map(e=>e.name))].map(name=>{const group=entries.filter(e=>e.name===name);return {name,script:/[\p{Script=Arabic}]/u.test(name)?'Arabic':'English',submitted:group.length,pass:group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='pass').length,reject:group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='reject').length,uncertain:group.filter(e=>auditVerdict(e,e.audit,campaign.rubricHash)==='needs_review').length};});
const gateCounts=Object.fromEntries(HARD_GATES.map(g=>[g,{reject:entries.filter(e=>e.audit?.gates[g]==='reject').length,uncertain:entries.filter(e=>e.audit?.gates[g]==='needs_review').length}]));
const budget=budgetStatus(state),complete=entries.length===48&&entries.every(e=>['succeeded','failed'].includes(e.status))&&!!state.selection;
const result={status:complete?'screening_complete':'screening_in_progress',qualified:false,productionApproved:false,counts,methods,nameResults,gateCounts,
 submissions:entries.length,originalOutputs:entries.filter(e=>e.status==='succeeded').length,providerFailures:entries.filter(e=>e.status==='failed').length,
 spending:budget,initialBalance:campaign.initialBalance,lastCheckpointBalance:state.account.credits,
 selection:state.selection,continuityPerformed:entries.some(e=>e.stage==='continuity'),
 scope:'Sixnames, Classical/Classic, white gold, stone-free, Cable32 nominal, single name, Studio.48case diagnostic.',
 review:'Blinded assistant visual review of originals and detail crops; no paid visual judge. Arabic marks explicitly read back. No claim of a professional Arabic calligrapher or jewelry engineering certification.'};
writeFileSync(resolve(OUT,'report.json'),JSON.stringify(result,null,2)+'\n');
const nameTable=nameResults.map(n=>`| ${n.name} | ${n.script} | ${n.submitted} | ${n.pass} | ${n.reject} | ${n.uncertain} |`).join('\n');
const methodTable=methods.map(m=>`| ${m.method} | ${m.submitted} | ${m.pass} | ${m.reject} | ${m.uncertain} | ${m.eligible===null?'Pending':m.eligible?'Yes':'No'} |`).join('\n');
writeFileSync(resolve(OUT,'report.md'),`# Creative name pendant comparison — v2\n\n${complete?'Screening complete.':'Screening in progress.'} **This candidate is not qualified or approved for production.**\n\n${entries.length}/48 first attempts submitted; ${result.originalOutputs} original outputs saved. Reviewed outcomes: ${counts.pass} pass, ${counts.reject} reject, ${counts.needs_review} uncertain/unreviewed. Provider terminal failures: ${result.providerFailures}. No retries replace first attempts.\n\n| Method | Submitted | Pass | Reject | Uncertain/unreviewed | Eligible |\n|---|---:|---:|---:|---:|---|\n${methodTable}\n\n| Name | Script | Submitted | Pass | Reject | Uncertain/unreviewed |\n|---|---|---:|---:|---:|---:|\n${nameTable}\n\n${state.selection?.winner?'Selected method: '+state.selection.winner+'. Conditional continuity remains to be reported.':state.selection?'No method met the fixed12/12 hard-pass and every-style-score≥3 gate. The conditional12-generation continuity stage is not opened.':'Selection waits for all48 outputs and reviews plus the final balance checkpoint.'}\n\nObserved reconciled workspace debit: ${budget.reconciledSpend} credits. Outstanding conservative reservations: ${budget.reservedCredits}. Initial balance: ${campaign.initialBalance}; last authenticated checkpoint: ${state.account.credits}. Preparation debit was0. Each submission has its reported balance saved; account deltas are observations, not a per-task invoice. Authorization remains60 NEW submissions and2460 existing credits, max2active.\n\n## What this tests\n\nA new pendant is invented from a customer name, with reusable lettering/hardware/spelling references assigned narrow roles. No finished customer-specific pendant silhouette is supplied. Six fixed names, two repetitions and four nested reference methods share the same construction and photographic instructions. Cases are randomized and frozen before submission. Exact prompts are above images in [the gallery](review.html). [Blind originals](blind/manifest.json), [the rubric](rubric.json), [ledger](ledger.json) and [case manifest](cases.json) retain the evidence.\n\nThe first design must preserve name spelling, dots/hamzas, supported components and plausible hardware. A beautiful material rendering cannot override those checks. Every required connection must be visible. Uncertainty fails advancement. Reviews are blinded assistant visual assessments, not a professional calligraphy or manufacturing certification.\n\n## Provider readback limitation\n\nThe exact tool-submitted prompts are saved. Runway tool echoes collapse whitespace; long submission echoes stop at3500characters and completion echoes at60. Their visible text matches normalized prefixes. This verifies displayed readback only, not the full downstream provider payload; it is not proof that model input was truncated. The full3-reference arm exceeds this echo limit. Resolve adapter-level payload visibility before any production qualification, and do not attribute image failures to echo abbreviation. See [readback evidence](provider-prompt-verification.json).\n\n## Scope and limits\n\nOnly Classical, Classic lettering, White gold, No stones, Cable, nominal32, single names and Studio are tested. Origami ribbon, Framed minimal and Diamond rails remain draft recipes; other form values, two-name layouts, gemstones,22nominal, engraving and actual millimeter dimensions remain outside this screen. The four later view bundles are conditional diagnostics, not a99% qualification. No production API, interface or validation behavior was changed.\n\nThe v1 reconstruction experiment and its243 hashed files remain separate evidence. See [the v2 specification](../../docs/rnd/PROMPT-SYSTEM-V2.md) and [research revisit](../../docs/rnd/creative-name-research/REVISIT.md).\n`);
console.log(JSON.stringify({status:result.status,counts,spending:budget}));
