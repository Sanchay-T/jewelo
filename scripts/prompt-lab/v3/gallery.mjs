import {readFileSync,writeFileSync} from 'node:fs';
import {resolve,dirname} from 'node:path';
import {fileURLToPath} from 'node:url';
const root=resolve(dirname(fileURLToPath(import.meta.url)),'../../..'),out=resolve(root,'reviews/2026-09-06-arabic-v3-inputs');
const read=f=>JSON.parse(readFileSync(resolve(out,f)));
const index=read('candidate-index.json'),manifest=read('reference-manifest.json'),diagnosis=read('diagnosis.json');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cards=['إيمان','ليان','نور'].map(name=>{
 const row=index.requests.find(r=>r.name===name&&r.method==='name_only'),p=read(row.path),r=p.referenceDescriptors[0];
 return `<section class="name" data-name="${esc(name)}"><h2><bdi>${esc(name)}</bdi> — exact requirements, creative design</h2><div class="facts"><div><strong>Required marks</strong><ul>${p.identity.marks.map(m=>`<li>${m.count} ${esc(m.kind)}${m.count>1?'s':''} ${esc(m.position)} <bdi>${esc(m.ownerCharacter)}</bdi>, owned by letter ${Number(m.ownerLetterId.split('-')[1])+1}.</li>`).join('')}</ul></div><div><strong>Natural letter groups</strong><p>${p.identity.groups.map(g=>'<bdi>'+esc(g.text)+'</bdi>').join(' / ')}</p><p>Bridge these groups with metal. Preserve the linguistic breaks and the mark positions.</p></div></div><details open><summary>Exact candidate prompt — name reference only</summary><pre>${esc(p.prompt)}</pre></details><div class="comparison"><div><h3>Reference 1: this customer's name</h3><img src="${esc(r.path)}" alt="Naturally shaped ${esc(name)} spelling reference"><p>Code renders this from the pinned font. The name is authoritative; its exact silhouette is not the finished pendant design.</p></div><div class="freedom"><h3>The model still designs the pendant</h3><p>It chooses proportions, curves, flourishes, visual balance and support routing within the fixed spelling and connection rules.</p><p><strong>Local candidate only.</strong> No new jewelry photograph has been generated from this input yet.</p><p><a href="${esc(row.path)}">Complete request + identity graph</a> · <a href="${esc(row.promptPath)}">Exact prompt text</a></p></div></div><details><summary>Optional second reference: construction relationships</summary><p>Same identity, with one unnamed support/attachment example. This is a separate input hypothesis.</p><img class="construction" src="references/construction.png" alt="Generic mark support and eyelet connector chain reference"><a href="${esc(index.requests.find(r=>r.name===name&&r.method==='name_and_construction').promptPath)}">Exact prompt for the two-reference candidate</a></details></section>`;
}).join('');
writeFileSync(resolve(out,'input-review.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Arabic pipeline changes · local v3 inputs</title><style>*{box-sizing:border-box}body{font:16px/1.55 system-ui;margin:0;background:#f6f2e9;color:#1d2c24}header{background:#1d3026;color:#fff;padding:28px 5vw}h1{font-size:30px;margin:0 0 12px}header p{max-width:950px}.state{color:#f5d292;font-weight:650}nav{position:sticky;top:0;background:#e8e3d7;display:flex;gap:12px;align-items:center;padding:12px 5vw;z-index:1}button{font:inherit;border:1px solid #8b968e;background:#fff;padding:7px 23px;border-radius:6px;cursor:pointer}button[aria-pressed=true]{background:#254b36;color:white}main{max-width:1300px;margin:auto;padding:20px 5vw}.facts,.comparison{display:grid;grid-template-columns:1fr 1fr;gap:24px}.facts>div,.freedom{padding:16px;background:#e8eee6;border-radius:8px}section{background:white;padding:24px;border:1px solid #d6d9d1;border-radius:12px}h2{margin-top:0}h3{font-size:18px}img{width:100%;height:auto;border:1px solid #ddd}pre{white-space:pre-wrap;font:13px/1.55 ui-monospace,monospace;background:#f3f5f1;padding:16px;max-height:245px;overflow:auto}details{margin:20px 0}summary{font-weight:650;cursor:pointer}a{color:#235c41}.construction{display:block;max-width:450px;margin:12px 0}.flow{padding:14px 0;font-weight:600}footer{padding:20px 5vw;font-size:14px}bdi{font-size:1.25em}[hidden]{display:none!important}@media(max-width:760px){.facts,.comparison{grid-template-columns:1fr}section{padding:16px}}</style><header><h1>What changes for Arabic</h1><p class="state">Implemented and locally tested inputs · 0 new model calls · improvement in generated images is not yet tested</p><p>The previous images exposed two separate problems: preserving the Arabic name and making all its metal parts connect. Code now describes both before the final prompt and references reach Runway.</p><div class="flow">Customer name → exact marks + natural joins → name reference → complete prompt + checked image bindings → Runway</div></header><nav><span>Inspect a name:</span>${['إيمان','ليان','نور'].map((n,i)=>`<button aria-pressed="${i===0}" data-show="${esc(n)}"><bdi>${esc(n)}</bdi></button>`).join('')}</nav><main>${cards}</main><footer>Previous Arabic results: identity ${diagnosis.arabic.gates.identity.pass}/24 passed; construction ${diagnosis.arabic.gates.construction.pass}/24; all checks ${diagnosis.arabic.pass}/24. These categories overlap.<br><a href="README.md">Diagnosis, changes and test boundary</a> · <a href="diagnosis.json">Original evidence aggregation</a> · <a href="../2026-09-06-creative-name-v2/review.html">Unchanged previous image results</a></footer><script>function select(name){document.querySelectorAll('.name').forEach(s=>s.hidden=s.dataset.name!==name);document.querySelectorAll('button[data-show]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.show===name)))}document.querySelectorAll('button[data-show]').forEach(b=>b.onclick=()=>select(b.dataset.show));select('إيمان');</script></html>`);
console.log(JSON.stringify({gallery:resolve(out,'input-review.html')}));
