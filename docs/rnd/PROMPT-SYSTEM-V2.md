# Creative custom-name pendant experiment — v2

Status: 48-image screening completed; no method qualified. Arabic: 1/24 passed, 19 rejected, 4 uncertain. English: 16/24 passed. Total: 17 passed, 24 rejected, 7 uncertain. Actual debit: 960 credits; no continuation images generated. This R&D-only addendum supersedes the earlier reconstruction experiment for the new creative-name question. Production interfaces, adapters, identity rendering and verification are unchanged.

The original experiment remains frozen in `reviews/2026-09-06-prompt-system/`. Its 14/16 assembly-reference result measured reconstruction of one existing Muhammad body, not creation of unseen names. Preserve that evidence and its source specification without rewriting its history.

## What changes

One frozen customer configuration fills a deterministic template. The model creates a new composition in the selected style, preserving exact name spelling and linguistic structure. Discreet metal bridges may support separated components without a full backing plate or surrounding frame. References teach lettering traits, attachment relationships and optionally spelling; they do not prescribe a finished pendant silhouette.

After an original Studio design passes review, that exact original output can become the design anchor for On skin, Close-up and Dark views. Each continuation uses the same original anchor independently, with its configuration and output hash bound into lineage. This tests whether a creative design can subsequently remain consistent; it does not establish manufacturing feasibility.

## Bounded execution

User authorized up to60 NEW Runway MCP submissions and2460 existing credits. This explicitly replaces the previous self-imposed cumulative50 stop; it does not establish a provider quota reset. No purchases, direct API calls, built-in ImageGen substitution, automatic retries, or paid judge.

Stage1: six names (`ليان`, `نور`, `إيمان`, `Ava`, `Lily`, `Christopher`) × four methods × two repetitions =48 Studio outputs. Fixed Classical, Classic lettering, White gold, no stones, Cable, nominal32, one name and no engraving. Methods: text; lettering reference; lettering plus hardware; lettering plus hardware plus spelling aid. Freeze and randomly interleave all cases before spending. All4 construction recipes exist; only Classical is in this paid screen.

Stage2 is conditional: a method must pass all12 hard checks and have style fit≥3/5 on every output. Select by median style fit, ties fewer references. Then use repetition1 of `ليان`, `إيمان`, `Lily`, `Christopher` as the four fixed anchors, each in3 additional views (maximum12 calls). If no method qualifies, stop at48 and retain the failures.

Reserve41 credits per call, allow at most2 active, reconcile balance every20 and at stage end. Stop on ambiguous submissions, unexplained excess debit, actual provider limits or authorization ceilings. The reservation is a conservative allowance, not a verified unit price.

## Reference kit

HarfBuzz renders naturally shaped text from pinned Noto Naskh Arabic and Playfair Display font bytes. No tracking modification, glyph fusion, dilation or relocation of Arabic marks. The hardware reference is a deterministic depth-rendered example of one integral eyelet, a separate closed connector and initial alternating links. Role descriptions and visual preflight are hash-bound before submission.

Arabic lettering specimens: مريم / فاطمة / جلال / أمينة. English specimens: Amelia / George / Sophia / James. Spelling aids contain each customer name alone and do not dictate pendant contours. These are reference examples for the experiment, not proof of a universal approved calligraphy family.

## Review and practical limits

Blind comparisons judge exact identity, supported components, plausible attachments, selections, inspectable photography, unrequested content and reference leakage. Style fit and appeal remain separate. Uncertainty fails advancement; inferred hidden connections cannot pass. Arabic output review must explicitly read back dots and hamzas, including إ versus ا. Any unresolved script judgment stays uncertain.

All results and costs, including failures, go into `reviews/2026-09-06-creative-name-v2/`. Its HTML gallery puts exact prompts above images when method details are revealed, and provides a blind view for comparison. Later continuation success would be a four-design diagnostic only. No99% reliability, dimension accuracy, production readiness, or broader form coverage is claimed.

Research rationale: `creative-name-research/REVISIT.md`, `OPENAI-NOTEBOOK-DIGEST.md` and its full cell-indexed extraction. The user-supplied notebook was read, not executed. The earlier `GPT-IMAGE-2-RESEARCH.md` is preserved unchanged.
