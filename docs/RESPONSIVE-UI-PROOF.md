# Responsive CALEUMS local delivery

> Current implementation: original v1 photographic reference set restored at user request. The fixed renderer below was rejected for visual quality and is no longer used by the customer preview. Earlier sections are historical evidence, not current acceptance.
Goal: `docs/goals/responsive-customer-experience.md`. Branch: `codex/responsive-customer-experience`. Local entry: http://localhost:3001/en/design/new (Arabic: /ar/design/new). Landing stays outside scope.

Implemented: fresh Design → Review, grouped and conditional controls, 22/32 mm, chain and gemstones, optional engraving/requests; immutable local preview revisions, partial mock failure and independent retry, old-preview labeling, confirmation-gated configuration snapshots, bag edit/quantity/removal/multiple items/reload; native modal inspection with zoom and keyboard wrapping; RTL and reduced motion; mobile accordions and fixed action with input-focus/keyboard clearance.

Six site assets were generated with the built-in imagegen tool at the user's explicit request: Asma Studio, On skin, Close-up, Dark, Arabic أسماء, and Asma–Fatima heart. Versioned originals and complete prompts/lineage: `apps/web/public/atelier/v1/manifest.json`. Cost was not reported by the built-in tool. These are fictional AI sample concepts; spelling and geometry have not received manufacturing approval. Images deliberately do not claim to match arbitrary customer input.

## Verification

- `pnpm --filter @jewelo/web typecheck`: pass.
- `pnpm --filter @jewelo/web lint`: pass.
- `pnpm --filter @jewelo/web exec vitest run src/features/atelier/model.test.ts`: 5 passed.
- Full `pnpm --filter @jewelo/web test`: 47 passed, 3 pre-existing failures in untouched `supabase-jewelo-client.test.ts` (document absent in Node environment), `ui-presentation.test.ts` (stale supported-style expectation), and `backend/shopify-adapter.test.ts` (error text expectation). No checks removed or weakened.
- Customer browser command: `pnpm --filter @jewelo/web exec playwright test --config playwright.atelier.config.ts`. Covers widths 320/390/768/1024/1440, 320×568 initial mobile, 320×420 input clearance, 390×500 resizing, touch target width and height, RTL, reduced motion, validation, conditional fields, spelling, stale changes during retry, per-image network failure, generation interruption/reload, quantity and multiple items, edit cancellation/update, corrupted storage, denied storage, dialog keyboard wrapping and focus restoration. Network assertion prohibits external and /api requests during the full customer flow.
- Native physical iOS/Android virtual keyboards were not available. Input focus and short-viewport clearance were exercised in Chromium; real-device keyboard validation remains a follow-up before launch.

## Review and fixes

Required plan reviewer approved the isolated local scope with immutability/recovery conditions. Fresh adversarial/UX/security source review found narrow touch targets, missing focus after bag-to-design transitions, and desktop toggles that could not collapse. All fixed and rechecked by reviewer. Short-viewport test additionally found a partly obscured textarea and a 320 px pointer-up timing issue when the bottom bar reappeared. Focus centering and delayed restoration of the bar fixed these; tests retain the original assertions.

Graph evidence: jewelo-main last known generation 2026-08-26T19:51:02Z; relevant files not tracked. Graph index_status/check_index_coverage subsequently returned Transport closed. Used exact source fallback for scoped new files; no exhaustive graph claims.

## Boundaries and operation

No deployment, merge, Shopify order/payment, backend mutation, authentication changes or application provider generation. Existing operator/backend and unrelated work preserved. No secret values read, copied or introduced into client components. Local state uses only the distinct `caleums.atelier.v1` key; name data remains on this device. Prototype mock runs retain up to eight recent revisions, and bag snapshots preserve their own run.

The long-running old Turbopack dev process became unresponsive; restarted only the identified Jewelo process on port 3001 with `--webpack`. Port 3000 belongs to another project and was untouched. Current run command: `PROVIDER_MODE=mock NODE_OPTIONS=--require=/tmp/jewelo-local-env-normalize.cjs pnpm --filter @jewelo/web dev --webpack --port 3001`. The existing temporary preload normalizes empty optional browser-observability environment values; no environment file was changed.

Next work requires user direction after visual feedback. Real generation, approved construction support, manufacturing verification, pricing and Shopify checkout remain outside this delivery.

Final browser result: **20 passed (1.4m)**. Browser tab was exercised through Design → Preview → Review → Bag and left on Design for feedback. A locally saved Asma sample remains in that Browser bag from this verification. Screenshots: `docs/proof/responsive-atelier/design-desktop.png`, `design-mobile.png`, `review-mobile.png`, `rtl-mobile.png`.

## Customer feedback revision — 6 September 2026

User superseded the earlier visible manufacturing gates and preview-history messaging. Removed those warnings and old-preview labels, enabled construction choices, removed separate centimetre chain-length controls, and retained 22/32 mm Size plus chain style. Internal snapshot/signature checks still protect bag consistency. Failure simulation is now available only in development with `?preview-test=1`.

Fixed sticky positioning by replacing the customer page's ancestor horizontal overflow with `clip`; adjusted tablet photo height to keep the preview, camera controls and summary together. Studio / On skin / Close-up / Dark now use large selectable photo cards. One resolver keeps Studio's English, Arabic and two-name samples consistent across hero, thumbnails, image-failure readiness and saved bag entries.

Typecheck, lint and six focused model tests pass. Browser checks now include sticky bounds and prominent camera targets at every width. Fresh reviewer identified readiness/bag source mismatches; both corrected and covered by a model regression test.

The existing six samples remain the current asset library. The full choice-matched catalogue is not implemented: construction, lettering, metals, gemstones, sizes and chains do not yet have matching photographs. A scope question for the next asset stage is pending (individual-option samples first versus the complete combination catalogue). No new images were generated in this revision. Earlier proof screenshots document the prior UI, not this revision.

Revision final browser result: **25 passed (1.4m)** at 320, 390, 768, 1024 and 1440 px. In-app Browser visually checked and left beside Style & Arrangement with sticky preview and all four photo cards visible. No deployment or merge.

## Native catalogue and preview dock — 6 September 2026

This revision supersedes the six-image catalogue status and pending scope question above. The user explicitly requested the complete categorical catalogue. It remains unfinished: 131,328 selectable configurations × four camera views require 525,312 unique keys. There are currently 70 exact photo keys and 525,242 missing keys. No compatibility exclusions were invented to reduce that count.

Generated 69 new native imagegen assets under `apps/web/public/atelier/v2`; visually accepted 65 and rejected four (one indistinct curb chain and three Arabic spellings with extra marks). Corrected replacements are included. All originals are retained; manifest records prompts, parent references, review outcomes and cost unavailable from the tool. Existing five usable v1 photographs bring the customer catalogue to 70. Visual review covers visible spelling, option distinction and framing, not calibrated physical dimensions or identical geometry across camera views.

Implemented canonical photo selection, immediate selection-focused examples, persisted sample focus and image identity in bag snapshots. The photo description distinguishes its depicted configuration from the customer's selected configuration whenever an exact image is missing. Added a searchable, filterable gallery with zoom at `/atelier/v2/index.html`. Studio, On skin, Close-up and Dark now have a larger cream-backed section, photo cards, strong selected states, accessible statuses, and a separate selection summary. The desktop preview remains sticky; short desktop windows use normal page scrolling to keep every control reachable.

Final static checks: typecheck and lint pass. Focused tests: 10 pass, including all 525,312 resolver keys and successful-photo bag selection. Enumeration verifies mapping and coverage, not visual correctness of missing photographs. Coverage and next missing batch are reproducible via `ATELIER_CATALOGUE_REPORT=1 pnpm --filter @jewelo/web exec vitest run src/features/atelier/catalogue.test.ts`; reports are in `docs/proof/responsive-atelier/catalogue-v2`.

Responsive browser suite: 33 passed, six intentional cross-width skips; the additional asset sweep initially hit a dynamic-import test-runner issue. Static imports fixed that issue and its focused rerun passed. Main interactions passed at 320, 390, 768, 1024 and 1440 pixels, including short viewports. Fresh reviewer confirmed fixes for failed-active-photo bag snapshots, accessible view statuses, and reload/bag-edit sample focus. Earlier unrelated full-suite failures remain recorded above. In-app Browser main preview, gallery filters and zoom were exercised. No deployment, merge, checkout, backend/provider requests from the app, or environment-file changes.

The complete generated permutation catalogue is still outstanding. Current deliverables are a working local UI, 65 newly accepted studies, deterministic catalogue infrastructure and exhaustive logical coverage reporting—not a completed 525,312-image library.

Final expanded catalogue browser sweep: **70/70 photos rendered successfully**, exact key/id assertions and no overflow/page errors; one Playwright sweep passed in 1.1 minutes. Preview-dock evidence saved at `docs/proof/responsive-atelier/catalogue-v2/preview-dock.png`.

## Coherent photo carousel — 6 September 2026

Replaced the static Explore heading with an active-angle editorial navigator, previous/next, Play/Pause, timed progress, image transitions and touch swipe. Rotation advances every five seconds after image load. It suspends for hover, focus, text editing, hidden pages, pending previews and open dialogs; manual camera selection pauses until explicit Play. Reduced motion starts paused. Explicit keyboard Play overrides the current interaction pause until the next focus/pointer entry.

Fixed cross-angle sample drift: a Studio anchor chooses one catalogue configuration, and only accepted camera images with the same canonical option key can be displayed. Missing angles are disabled instead of showing another pendant. Successful bag images use that same family. This does not create missing configuration assets or verify identical physical geometry across generated photos.

Verification: 11 focused unit tests passed, including all 131,328 configuration families and the existing 525,312 resolver keys. Initial responsive run: 44 passed and six intentional skips; five journey tests still expected a now-unavailable Dark angle. Updated the helper to await the available Studio angle, retaining independent failure/retry tests. Final affected-test run: 20 passed across 320/390/768/1024/1440, covering full bag/reload journey, autoplay/manual pause, keyboard Play, focus/hover/reduced motion, image failure, and editing from Dark into a Studio-only saved piece. The earlier run also passed all 70 asset renders and every-option click/family checks at all widths. Plan and fresh UX/adversarial review completed; fixed review findings for retained invalid angle, keyboard Play and failed-image accessible descriptions. No deployment, merge or backend changes. Complete photographic permutation coverage is still outstanding.

Final carousel typecheck and lint pass after reviewer fixes. Desktop/mobile preview screenshots inspected: `docs/proof/responsive-atelier/catalogue-v2/carousel-1440.png` and `carousel-390.png`. In-app Browser updated without overwriting the user's existing draft. Current Arabic Kufi example has only Studio available; full-view samples rotate automatically. No new generated photographs in this carousel revision.

## Restored preview, cumulative selections and jewelry review — 6 September 2026

The user rejected the enlarged-card and horizontal-rail experiments. Restored the earlier four-card camera carousel and separate selected-specification summary. The main image retains its existing dominant size and sticky desktop placement. The rejected `large-cards-*` / `view-rail-*` screenshots are historical experiments, not the delivered layout.

Three parallel reviewers were explicitly requested: selection continuity, shopping state, and visible jewelry integrity. Graph transport remained unavailable; reviews used bounded exact source reads. Fixed last-click filtering so the same complete specification resolves identically regardless of selection order. Exact catalogue matches win; missing combinations use deterministic priorities that retain the principal identity/construction before finish details. Clicking an already-selected option is now a no-op, retaining the active camera and confirmation. An old failed preview run no longer masks photos for a changed draft. Cancel bag edit restores the previous draft/run/stage/focus, including after reload; removing the actively edited item clears its return snapshot. The final shopping recheck identified that removal edge, now corrected and browser-covered.

Generated and independently reviewed twelve v3 photographs: Origami ribbon in white gold, then diamond accents, then ruby accents; Studio, On skin, Close-up and Dark for each. The tested cumulative route retains Origami and white gold as accents are added/replaced; removing stones returns the white-gold Origami family. This is a concrete added sequence, not full permutation completion.

Dedicated visual reviewer inspected 94 unique images: all 70 original customer photographs, twelve cumulative v3 additions, and twelve regenerated v2 files. Four definite disconnected Arabic letter-group defects were found in Kufi, Minimal, Signature and Thuluth Studio originals. Regenerated them with visible gold supports preserving the glyphs; independent recheck accepted all four. Initial floating-i-dot concerns in heart/interlocked designs were withdrawn after enlarged crops revealed existing fine necks. Eight regenerated views for those families were accepted as connection-visibility refinements, not confirmed broken-piece repairs. Per-file evidence: `docs/proof/responsive-atelier/catalogue-v2/image-review.md`.

Client catalogue now references all twelve approved replacement filenames. Replaced originals remain immutable in v2 with replacement lineage and review status. The v3 manifest records reference ancestry, prompts, review and unreported generation cost. The gallery at `/atelier/v2/index.html` now includes both v2 and v3 accepted studies (77); five existing v1 photos bring the app catalogue to 82.

Checks: typecheck and lint pass. Twelve focused model/catalogue tests pass, including 525,312 resolver cases and 131,328 coherent-family cases. The exhaustive family audit exceeded its default five-second test budget under concurrent load; its explicit budget is now thirty seconds without changing assertions. Responsive suite initially returned 60 passed, six intentional skips and four timeouts in the 24-option/reload/camera sweep. Those sweeps needed more than the default thirty seconds; all five widths pass with a ninety-second test budget and unchanged assertions. Full journey, cumulative selection, re-click, undo/reload, failed-run recovery, RTL, reduced motion, keyboard, sticky and short viewport checks passed at 320/390/768/1024/1440. Existing unrelated full-repository failures remain recorded above.

Browser gallery refreshed and repaired Kufi opened at full size. The user's active design draft was preserved. Native imagegen only; no application provider, payment, checkout, deployment or merge actions. Full photographic coverage remains incomplete: 82 exact image keys, 525,230 missing. Enumeration tests establish consistent mapping, not visual correctness of ungenerated combinations.

Final post-integration verification: all 82 catalogue images rendered with exact id/configuration assertions and no page errors/overflow; the asset sweep passed. Five keyboard/bag-edit/removal cases passed across all widths (six tests passed, four intentional duplicate-sweep skips). Screenshots inspected and retained as `catalogue-v2/reviewed-origami-white-ruby-preview.png`, `reviewed-arabic-kufi-preview.png`, and `reviewed-heart-preview.png`.

## Fixed example renderer — 6 September 2026 (current)

Implemented locally on `codex/responsive-customer-experience`. Customer choices now modify one deterministic Three.js assembly; the live designer no longer chooses a nearest catalogue photo. Studio, On skin, Close-up and Dark capture that same assembled example. Camera selection survives specification edits. The fixed example names remain Asma/Fatima or أسماء/فاطمة; customer-entered spelling, engraving and requests remain saved configuration text.

Added 24 shaped, filled SVG name outlines across two scripts and six lettering mappings, with reproducible build script, pinned source-font checksums and OFL licenses. Added six native imagegen background candidates (two each Studio/skin/Dark), selected three jewelry-free plates; Close-up shares the Studio setting. Manifests retain lineage, hashes and unavailable-cost notes. Three.js and its types are pinned; renderer loads lazily and reuses identical geometry for review/retry.

All four constructions, five two-name layouts, three metals, nested stone coverage, gemstone materials, 22/32 mm and four chain geometries feed the assembly. Lab and natural diamond intentionally share appearance. A fixed camera scale per view preserves visible size differences. Rendering includes rear component bridges, stone seats within filled outlines and connected chains. The On-skin camera is illustrative rather than a body-fit measurement.

Review captures now use real local rendering instead of the previous timed mock completion. Per-view capture failures preserve successful siblings, one retry settles the failed view, and superseded asynchronous work cannot publish an old selection. Loss of WebGL context has an explicit retry path. Bag pieces save immutable image Blobs in IndexedDB and only descriptors in localStorage; old bag catalogue images remain supported. Storage failure is explicitly session-only, and a missing saved image never resolves to an unrelated new photograph.

Independent visual review found and resolved reversed front-face winding, Arabic stacked/interlocked text collisions, detached-looking upper chain links, excessive full-width support rails, and short-desktop cropping. Tall assemblies use contain fitting within unchanged main panel dimensions; normal horizontal examples retain their prominence. The original four camera cards and sticky preview remain. Bounded visual recheck: 16 representative designs and four short-desktop cases passed; evidence and scope in `docs/proof/responsive-atelier/fixed-renderer/visual-review.md`.

Current responsive acceptance: 18 executed browser tests passed across the full run and two focused regressions. The full run exercised Design → Review → Bag at 320/390/768/1024/1440; selected option state and actual rendered-byte changes; size changes; immutable bag images, reload and editing; successful siblings during Dark failure and single retry; rapid changes; keyboard/RTL/reduced motion; and actual WebGL context-loss recovery. Four redundant context-loss width cases were skipped because that injection ran once at 1440. Additional focused passes prove customer-name/already-selected-option pixel invariance and full tall Arabic pieces at 1265×712 without reducing the panel. The headless test browser required explicit software ANGLE flags after its initial WebGL context failed to initialize; the corrected environment runs the actual renderer, with no mocked canvas.

Current browser command: `pnpm --filter @jewelo/web exec playwright test -c playwright.renderer.config.ts`. The earlier `atelier.spec.ts` is retained as historical catalogue/mock-contract evidence; its former photo-ID/timer expectations are superseded and are not claimed passing for the new renderer. Current unit suite: 21 tests passed, including enumeration of all 131,328 effective configuration keys, geometry-placement rules, snapshot immutability and model compatibility. Typecheck and lint passed. Existing unrelated full-web test failures are recorded in the earlier proof section and were not changed.

No deployment, merge, authentication, backend/operator mutation, Shopify/payment integration or application provider requests. Background generation occurred only during authoring through native imagegen. This delivery provides deterministic fixed-example visualization; it does not claim all 525,312 possible configuration/camera images have been individually visually reviewed.

Final rendering proof: all 288 base assemblies captured in all four views (1,152 PNGs), zero runtime/capture errors. Independent review of the four contact sheets inspected 192 representative frames spanning all script/lettering/construction categories, with no further visible breakage. Renderer lifecycle proof passed mesh reuse, latest edit wins, stale-build rejection, PNG capture and pending-build disposal (`renderer-sweep/lifecycle.json`).

The initial 300-case material sweep passed state/count assertions, but root and independent image review rejected its sparse Full pavé appearance. That evidence remains in `material-sweep-initial-sparse`. Replaced triangle-incenter selection with dense deterministic contour packing, preserving hole/edge clearance and nested tiers. Stone/bezel meshes are instanced; fixed-outline placements are cached. Updated ruby/emerald materials to retain their color instead of washing out. Both high-resolution pilots passed independent coverage, attachment, spelling and color inspection (`stone-density-pilot/visual-review.md`).

Final corrected material sweep: 306/306 PNG captures, zero runtime/capture failures. All 48 identity groups pass nonzero stones, strictly increasing nested coverage and six distinct coverage/color images; Full pavé has 28–149 seats depending on stroke area. All 12 metal/chain combinations produce distinct images. The six-gem check confirms identical Lab/Natural diamond appearance and five distinct appearances overall. Detailed evidence: `material-sweep/summary.json`, `report.json`, `stone-checks.json` and `gem-checks.json`. These are actual renderer outputs, distinct from the exhaustive canonical-key test.

After the final density/cache changes, typecheck, lint and all 21 focused unit tests passed. Re-ran the complete 390px cumulative-selection → review → immutable bag/reload/edit journey: passed in 36.9 seconds. Existing 18 distinct responsive acceptance cases remain the broader flow evidence. Legacy bag migration uses its original example source when no catalogue ID exists; missing archive IDs show an unavailable state instead of an unrelated replacement photo. Six generated backdrop candidates and all selected-file checksums verified. The working designer is open in the Codex in-app Browser at localhost:3001/en/design/new; local only, ready for user feedback.

Final six-gem strip review strengthened Pink sapphire so it stays visibly pink beside diamonds at card size. Only the affected six-gem comparison was recaptured; final report rows 300–305, hashes and gem checks were updated. The other 300 material cases are unchanged. Final strip: `docs/proof/responsive-atelier/material-sweep/gem-options.png`.


## Original photo restoration — 6 September 2026

User rejected the procedural jewelry and requested the old photos, without generating replacements. Customer preview now uses the immutable original Studio, On skin, light Close-up and Dark Asma files. The same four-photo set stays together; selected specifications remain separately recorded and photos explicitly carry no exact-configuration claim. Renderer is no longer invoked by the customer preview. Sticky preview dimensions and view cards retained. Existing saved bags are not rewritten; new snapshots store photographic blobs with photographic-v1 lineage.

Validation: typecheck and lint passed. Five focused Playwright cases passed at 320/390/768/1024/1440, covering all four exact original asset URLs, successful decoding, review transition, draft reload and no horizontal overflow or API/geometry requests. All four desktop screenshots visually inspected; close-up is light, On skin shows the worn pendant. In-app Browser shows restored images with the user's existing draft and bag. Prior renderer permutation tests are superseded, not evidence for exact photographic option matching. No new assets generated, deployment or merge.
