# Tasks

**Written:** 7 September 2026, from four read-only gap analyses plus live probes of staging and localhost.
**Status:** the ordered work list for `docs/ROAD-TO-GOLD.md`. `docs/GOAL-PROMPT.md` points here.

## How to use this file

- Take the first task whose state is `open` inside your scope and whose `needs` are met.
- States: `open`, `doing (session date)`, `done (commit sha, evidence path)`, `blocked (what on)`, `dropped (why)`.
- Proof means: it builds, it is deployed or run, and you looked at the result in your own browser or read the script's printed measurements. No tests, no CI (Sanchay, 7 September 2026).
- Owner `agent` means do it. Owner `Sanchay` means it needs a credential, money, or a product call; do everything that does not depend on it, then ask once.
- When a task's evidence changes what the plan says, update `docs/ROAD-TO-GOLD.md` in the same commit.
- Sizes: S under half a day, M one to two days, L three days or more.

## Decisions with defaults

Sanchay is not available during a run.
Each row has a default; the agent takes the default, records it in `docs/goals/road-to-gold/HANDOVER.md` under "Decisions taken by default", and keeps going.
Sanchay overrides by editing the Default column; the next session picks it up.
Rows whose default is "Sanchay's action" cannot be taken by an agent: work around them, list them under "Needs Sanchay", never block on them.

| Id | Decision | Default (applies unless Sanchay edits this cell) | Gates |
| --- | --- | --- | --- |
| DS-1 | Supabase Devonel org is at 52.5 GB of 5 GB egress; grace ends **29 September 2026**, then every project returns 402 | Sanchay's action: upgrade the org to Pro or move `jewelo-caleums`; delete `jewelo-v2-mumbai` either way. Agent: keep building, list it first under Needs Sanchay | everything after that date |
| DS-2 | `Sanchay-T/jewelo` is public | Sanchay's action: make it private. Agent: never commit brand material or customer media | anything that would ever commit brand or shop material |
| DS-3 | Identity engine: (A) keep Pango by family name and only prove stencils on Linux, or (B) new engine that opens the pinned font bytes directly with HarfBuzz and is deterministic on every machine | **B.** Record it as D-019 in `docs/DECISION-REGISTER.md` and bump the engine release to `caleums-identity-v4` in `docs/CALEUMS-FINAL-E2E-CONTRACT.md`; the fingerprint already includes the engine release so nothing else moves | P1-2 onward |
| DS-4 | Which looks the shop sells if only framed minimal plus Classic prove out | **Sell only what phase 3 proved.** Hide the rest from the design stage; keep their code paths so a later proof re-enables them with one flag | P6-2 |
| DS-5 | Wait on the page for the photograph, or keep send-it-to-you as the default | **Wait while the studio still arrives inside 90 seconds** (measure the real latency in P5-2 first); past that, capture contact and send. Both paths stay | P6-3 |
| DS-6 | Phase 5 smoke before or after anchor publication | **Studio-only smoke before publication**, so one run bills one image, not up to twelve | P5-1 |
| DS-7 | Shop URL: launch on the staging URL, or bootstrap `jewelo-production` plus a Caleums domain | **Staging URL** until Sanchay approves production bootstrap (his action, needs a domain from Omran); prepare the spec so it is one command | L-1 |
| DS-8 | New-request notification channel for the shop | **Email** via the existing Supabase SMTP if configured, otherwise build the Inngest function against a stub and list the account under Needs Sanchay | P7-3 |
| DS-9 | Error tracking accounts (Sentry, PostHog) | Sanchay's action to create; agent wires the SDKs behind empty DSN and lists it | P7-5 |
| DS-10 | Inngest durability: accept in-process loss with operator recovery, Inngest Cloud account, or a DO Valkey instance | **Accept in-process loss with operator recovery** (P7-1 makes `operator_review` visible); Cloud stays a one-variable switch for Sanchay to flip | P7-6 |
| DS-11 | Development spend ceiling for the paid replay of the vision readers in P2-6 and P2-7 | **USD 10**, enforced by the replay script itself (call counter, per-call estimate, hard stop); `runtime_policy` cannot bind a script because its trigger fires only on `principal_daily_usage`. Actual spend recorded in the handover | P2-6, P2-7 |

## Phase 0 - unblockers and hygiene (all S, all agent)

| Id | Task | Files | Proof |
| --- | --- | --- | --- |
| P0-1 | **done (session 1)**: agent definitions rewritten as Opus 5 medium-effort workhorses with the connected Runway tool prefix, no fixed repo path, `CLAUDE.md` and the task row as the contract | `.claude/agents/*.md` | dispatch an `image-lab` agent that calls `whoami` and returns the credit balance (first thing the next session does) |
| P0-2 | **done (session 2)**: `.env.local` deleted (only dead `TRIGGER_*` plus stale Supabase copies; nothing reads it), runbook now passes `.env` alone. Was: `.env.local` still carries the old Supabase project ref and `TRIGGER_*`; the documented bootstrap order would ship old credentials | `.env.local`, `docs/DIGITALOCEAN-DEPLOYMENT.md` | `pnpm do:check-env` clean; no `TRIGGER` line in either env file |
| P0-3 | **done (session 2)**: `report.py`, `finalise.py`, `ingest_results.mjs` run clean over 91 rows; stage 2 reported; explicit per-stage index map. Was: lab bookkeeping: `report.py` crashes on null `creditsAfter`; `finalise.py` does not know stage 2; `ingest_results.mjs` defaults to the stale stage-2 ringed index | `docs/goals/overnight-launch/lab/report.py:42`, `lab/finalise.py:18-29,53`, `lab/ingest_results.mjs:37` | both scripts run clean over the 91-row ledger and the report shows stage 2 |
| P0-4 | **done (session 2)**: IMAGE-LAB.md and defect 3 now match the ledger (91 rows, 49 pass, 54%, v4.3, 12 holdout PNGs, 6 scored, 11 unscored). Was: correct the stale claims: holdout names were generated (12 PNGs in `lab/stage2/`); the lab's current release is v4.3 not v4.2; 91 ledger rows not 73 | `docs/goals/overnight-launch/IMAGE-LAB.md:428-450,496-660`, `docs/ROAD-TO-GOLD.md` defect 3 | doc diff |
| P0-5 | **done (session 2)**: `VIDEO_ENABLED` (0/1, default 0) in `jobsEnvSchema`; `FAL_KEY` required only when on; auto motion preview gated in `presentation.ts`. Was: real mode couplings: `PROVIDER_MODE=real` throws without `FAL_KEY` although video is out of scope; every studio still auto-requests a fal motion preview (200 cents) | `packages/config/src/index.ts:94-105`, `apps/jobs/src/presentation.ts:1008-1020,1119` | real mode validates with no `FAL_KEY`; a mock run creates four image tasks and zero video tasks when video is off |
| P0-6 | **done (session 2)**: runbook describes the real `deploy.sh`/`smoke.sh`/`rollback.sh` path and no CI; contract branch matches the live app 14/14. Was: `docs/DIGITALOCEAN-DEPLOYMENT.md:37-63` describes GitHub workflows that no longer exist; `infra/digitalocean/spec-contract.json:5` pins `main` while the app deploys `codex/overnight-launch-2026-09-08` | those two files | doc and contract match `doctl apps get` |
| P0-7 | **done (session 2)**: 12 prunable entries removed (not 7), 4 fully-pushed branches deleted, `review/sample-images-board` refused by git and left, parked session `hq-claude2:2.1` is on an out-of-credits prompt (Needs Sanchay). Was: delete the seven dead worktree entries and the stale branches on `home-mini` (`git worktree prune`); park the out-of-credits Claude session there | `home-mini` only | `git worktree list` shows only live trees |

## Phase 1 - the identity engine (needs DS-3)

Rule for every task: the geometry report is produced by `measureMask(decode(pngBytes))`, a module that never touches the renderer's in-memory mask.
Today `identity-anchor.ts:140-142` and `caleums-arabic-v3.ts:57-60,204-207` return `passed: true` as a literal, and the type cannot express failure.

| Id | Task | Files | Change | Proof | Size |
| --- | --- | --- | --- | --- | --- |
| P1-1 | **done (session 2)**: `measureMask` in `packages/identity/src/geometry.ts`, `decodeMask` in `apps/jobs/src/decode-mask.ts`; 16/16 lab and 16/16 production stencils match `verify_stencil.py` exactly. Was: build the ruler first | new `packages/identity/src/geometry.ts`, new `apps/jobs/src/decode-mask.ts` | Port `lab/verify_stencil.py:18-44`: 4-connected components, holes (background not reaching the border) with sizes, bbox, ink count; sharp decode with alpha > 96 or L < 128 | a script prints components and holes for all 16 committed `lab/stencils/*.png` and they equal `manifest.json` | S |
| P1-2 | **done (session 2)**: `packages/identity/src/shaping.ts` (harfbuzzjs 1.6.1), fonts resolved from `import.meta.url`, measured `fontSha256Measured` and `exactCharactersPreserved`, release `caleums-identity-v4`, D-019; Kufi and Naskh gids differ for four names, Latin advances equal `hb-shape` at 0% drift. Was: shape from the pinned font bytes, not a family name | new `packages/identity/src/shaping.ts` (`harfbuzzjs`); `caleums-arabic-v3.ts:161-164,198-199`; `identity-anchor.ts:66,210-220`; `docs/DECISION-REGISTER.md`; `docs/CALEUMS-FINAL-E2E-CONTRACT.md` | Open the pinned file by file name (`PlayfairDisplay-SemiBold.ttf` for Latin, never a family name and never a weight), resolved from `import.meta.url`; hash the loaded bytes into `fontSha256Measured`, shape with HarfBuzz, take glyph outlines; `exactCharactersPreserved` becomes "no gid 0 and every NFC codepoint is covered by a cluster". Keep `LIVE_STYLES` as the style-to-font map; the literal `fontSha256` per style becomes the measured hash of the loaded bytes and a mismatch against the pinned manifest value throws `identity_font_mismatch:<file>`. DS-3 is recorded as D-019 here and the engine release is bumped to `caleums-identity-v4`. Root cause today: sharp's bundled Pango never consults fontconfig on macOS and the engine asks by family name, so Latin renders in the system sans and Kufi and Naskh produce byte-identical files (`268f77d4…` for نور, `dbb5dccf…` for أسماء) | glyph id sequence and advances differ between Kufi and Naskh for four names and Latin advances match `hb-shape --font-file` (or fontTools) within 2% | M |
| P1-2b | **done (session 2, `8838de5`, staging deployment `6a536690`)**: `/api/operator/diagnostics/identity` returns harfbuzz 14.4.0, `wasmLoaded: true`, three faces with distinct measured shas equal to the laptop's; the probe caught and fixed an interpolated `import.meta.url` font path that made the bundle hand back Amiri for every face. Was: deploy probe: prove the WASM and font assets survive the buildpack | new operator diagnostics route in `apps/web` under `/api/operator/` | An operator-authenticated diagnostics route that shapes one name per script and returns harfbuzz version, wasm path, resolved font path, measured font sha and the glyph count | deploy the branch to staging with `scripts/digitalocean/deploy.sh` and call the route; paste the JSON. Proves WASM and font assets survive the DigitalOcean buildpack before P1-3 builds on them | S |
| P1-3 | **done (session 2)**: path-only 1024x1024 SVG from the HarfBuzz outlines, one `IdentityRasterizer` and `solveIdentity` for both scripts, Kufi English on `cairo.ttf`, `<text>` and fontconfig gone, tests excluded from the jobs tsconfig; 16 renders identical under `measureMask` and `verify_stencil.py`, `hb-shape` drift 0%. Was: rasterise outlines only | replaces `identityAnchorSvg` (`identity-anchor.ts:38-80`) and `typeset` (`:147-182`); `apps/jobs/tsconfig.json` | Path-only SVG, canvas 1024x1024, black ink on white, no alpha channel, both scripts, matching `make_stencil.py`; the ruler must report `rule === 'luminance'`. Fit-by-probe sizing as `lab/make_stencil.py:207-219`, reserved ring band; no `<text>` element anywhere. Rename the rasterizer port `ArabicIdentityRasterizer` to a script-neutral `IdentityRasterizer` if one solver serves both scripts. In the same commit exclude `src/**/*.test.ts` from `apps/jobs/tsconfig.json` (and any other package whose tsconfig would typecheck a test importing a removed export); test files stay unedited and unrun | `corepack pnpm build` passes with no test file edited; render the four names in both scripts and open them in the browser; per-glyph widths match P1-2; a script renders all 16 lab names in both scripts and prints `measureMask` next to `verify_stencil.py` for the same PNG bytes; a byte-difference table Kufi versus Naskh versus Playfair per name with the font sha the engine actually loaded | M |
| P1-4 | **done (session 2)**: `bridgeAll` and `drawBar` ported (exact distance transform, capsule bars), `fuse()` deleted, ink preservation asserted pre-recentre (`identity_bridge_moved_ink`), Pillow-equivalent LANCZOS recentre with offset and scale reported; 216/216 cells (18 names x 2 scripts x 6 styles) single piece, 0 moved pixels, Python agrees. Was: bridge, never move | replaces `fuse()` (`caleums-arabic-v3.ts:216-277`) | Port `bridge_all` and `draw_bar` (`make_stencil.py:90-140`): thicken 2, 24 px capsule bridge to the nearest island. The ink-preservation invariant is stated in pre-recentre coordinates, and the applied recentre offset and scale are reported alongside it. Today `fuse()` translates the smallest island, so a hamza or dot can move while "exact" is reported | every pre-bridge ink pixel is still ink at the same pre-recentre coordinate, with the offset and scale printed; P1-1 reports one component for the 17 ZIP names plus Asma, Noor, Layla, Muhammad in both scripts and all live styles | M |
| P1-5 | **reopened (session 2, adversarial review 3; D-020 redesign: outline-level anchors with a no-refusal fallback, after fix pass 4 lands)**: `addRings` ported (11x11 erosion, outer-corner anchors, weld fillet, lift until the hole is clear of name ink); 232/232 rings-on stencils have exactly two ring holes with zero punched name pixels and hole sizes within 1.7% of the lab's; `IDENTITY_RINGLESS_CONSTRUCTIONS` validated in `packages/config`, rings-off proven by script (0 ring holes, one piece). Was: rings: attached, name-aware, ring-free opt-in | delete `addJumpRings` (`caleums-arabic-v3.ts:339-368`) and the fixed circles (`identity-anchor.ts:64-65`); `packages/config/src/index.ts` | Port `add_rings` (`make_stencil.py:143-178`): 11x11 erosion so a dot never anchors a ring, outer-corner centre, weld fillet; `rings` derived from `specification.construction`. Rings default on. Ring-free is opt-in through a validated flag in `packages/config`, `IDENTITY_RINGLESS_CONSTRUCTIONS`, a comma list of constructions, default empty, and is proven by script, not the UI. Today the rings sit at x=255 and x=945 while the glyph run spans about 180 to 1020, so they always land inside letters | rings on (the default): exactly two round ring holes, each above its anchor and clear of all name ink, and no glyph ink inside a hole (Asma regression); with `IDENTITY_RINGLESS_CONSTRUCTIONS` set in a script run: zero holes, one component. Decision (adversarial review 2, F-3): "above its anchor" and not "above the glyph bbox" is the standard, because the lab's own `make_stencil.py` seats rings at corner anchors of the eroded body, which for a tall Arabic letter is inside the name band, and the prompts were proven on those stencils | M |
| P1-5a | **done (session 2)**: the three surfaces are unchanged and quoted here; `IDENTITY_RINGLESS_CONSTRUCTIONS` is absent from the staging app spec (P0-6 env-name readback); P3-7 carries the prompt rewrite. Was: ring-free stays off until the prompts stop promising rings | `packages/ai/src/prompt-registry.ts:121,373,376`, the seeded `verification.image` prompt in `supabase/migrations/20260827080000_*:113`, `docs/CALEUMS-FINAL-E2E-CONTRACT.md` | Prompt and verification surfaces still say exactly two rings, so a ring-free stencil would be generated and verified against a two-ring instruction. P3-7 rewrites the prompt sentences and republishes `verification.image`; until then the ring-free flag stays empty on staging | the three surfaces are quoted in the row and left unchanged, `IDENTITY_RINGLESS_CONSTRUCTIONS` is empty in the staging env, and the dependency is recorded on P3-7 | S |
| P1-6 | **done (session 2)**: report widened to numbers and booleans plus `measuredBy`, `rule`, `bbox`, `holes`, `ringHoles`, construction; `passed` and every measured field come from `decodePng` + `measureMask` on the encoded PNG; `identity_ring_gate_failed:holes=1,expected=2` and `identity_component_gate_failed:components=2` proven on tampered decodes; `PIPELINE_RELEASE_ID` config (default `caleums-final-media-v2`); migration `20260908120000_pipeline_release_v2.sql` (not yet pushed, P1-7). Was: report equals measurement | `IdentityValidationReport` (`caleums-arabic-v3.ts:46-61`), `identity-anchor.ts:125-142`, `presentation.ts:677,717-720`, `packages/config/src/index.ts`, new `supabase/migrations/` file | Widen the report types (`jumpRingCount`, `componentsFinal`, `exactCharactersPreserved`, `passed` in `caleums-arabic-v3.ts:57-60`) to `number` / `boolean`, plus `measuredBy`, `bbox`, `holes`, `fontSha256Measured`. Extend the rasterizer port (`ArabicIdentityRasterizer`, renamed to the script-neutral `IdentityRasterizer` in P1-3) with `decodePng(bytes) => MaskGeometryInput`; `apps/jobs` injects `decodeMask`; the solver decodes and measures its own encoded PNG bytes and `passed` is computed from that report only; throw `identity_*_gate_failed` otherwise (the DB check at `supabase/migrations/20260827080000_…:38` already rejects a false `passed`); one solver for both scripts; store the measured font sha instead of `existing-latin`. Additive migration inserting a `pipeline_releases` row whose `identity_engine_release` is `caleums-identity-v4`, and `presentation.ts:677` reads the pipeline release id from validated config instead of a literal | the report deep-equals an independent run of P1-1; a PNG with a ring hole filled in fails; the run's `generation_tasks` rows point at the new pipeline release id | S |
| P1-7 | **done (session 2, `e5e598b`, staging deployment `ebdca539`)**: v2 migration pushed (v2 active, v1 legacy, readback pasted); 16 production stencils regenerated and identical under `measureMask` and `verify_stencil.py`; two staging mock runs (Rania, ريم) complete on `caleums-final-media-v2` with `engine_release caleums-identity-v4`, stencils pulled from the bucket deep-equal their stored `validation_report`; an injected Devanagari name blocked with `identity_shaping_gate_failed:notdef=4,uncovered=4` in `terminal_error_code`, dependents never spent; next.config externalises harfbuzzjs and traces the fonts and wasm, probe unchanged. Was: second oracle and the real runtime | `lab/stencils/production/`, `lab/verify_stencil.py`, `apps/web/next.config.ts`, `identity-anchor.ts:232-242`, staging `identity-anchors` bucket | Regenerate the production stencils and run the Python gate on them: numbers must match P1-1. Then trigger one mock run on staging per script and pull the stencil the deployed Node buildpack rendered from the bucket: it must pass P1-1 too. Font and WASM assets resolve from `import.meta.url` first, keeping the `process.cwd()` candidate chain as a fallback after it (`identity-anchor.ts:232-242`); add `harfbuzzjs` and `@jewelo/identity` to `serverExternalPackages` in `apps/web/next.config.ts`, and `outputFileTracingIncludes` for `packages/identity/engines/**/*.ttf` and the harfbuzzjs wasm | four staging-rendered stencils open in the browser and pass the gate, and the ruler reports `rule === 'luminance'` on each; read the run's block reason and `terminal_error_code` from the database and paste them - a missing font or wasm must surface as `identity_*` there, not as a silent `operator_review`; `select engine_release, count(*) from identity_artifacts group by 1`; pull `identity_artifacts.validation_report` for the staging run and re-measure the downloaded object locally, the two must agree; a deployed dependency dump prints harfbuzz version, wasm path, font path, canvas size and ink rule | S |

## Phase 2 - the real verifier

Corpus truth, measured 8 September 2026 from `docs/goals/overnight-launch/ledger.jsonl`: 91 rows, 73 with a verdict before the merge and 80 after (P2-1 merged the six stage-2 verdicts plus one stray stage-1 v4.3 verdict), 55 passes, 13 fails, 12 tweaks, 11 unscored.
13 rows carry a defect a gate could own: 7 geometry (extra-ring 3, missing-ring 1, disconnected-component 2, floating-mark 1, floating-stone 1) and 6 selection or framing (wrong-stones 4, wrong-display 3, one row carries both).
`chain-not-through-ring` is 5 rows, all stage 1, all fail. There is no `wrong-spelling` row and no stone-in-counter row anywhere in the corpus: those gates are proved on tampered fixtures, never claimed from the ledger.
Gate sets are per presentation view. The full geometry set applies to the studio packshot only; on-skin, close-up and dark are checked against the verified studio master, because their own view briefs ask for a three-quarter macro, a chain leaving the frame, and one ring in shot.
Plan review: `docs/goals/road-to-gold/reviews/phase2-plan-review.md`.

| Id | Task | Files | Change | Proof | Size |
| --- | --- | --- | --- | --- | --- |
| P2-0 | **done (session 2)**: `packages/ai` tests excluded, jobs scripts typechecked, build `--force` exit 0 | `packages/ai/tsconfig.json` | Exclude `src/**/*.test.ts` so P2-7 can retire `OpenAIStudioVerifier` without red-lining the build, exactly as P1-3 did for `apps/jobs` (`index.test.ts:6,84` constructs it). Also widen `apps/jobs/tsconfig.json` include to `scripts/**/*.mts` so the harness scripts are typechecked by the build gate | `corepack pnpm build` passes with a clean `git status` | S |
| P2-1 | **done (session 2)**: `replay-lab.mts`, 91/91 stills and 128/128 references resolved, baseline recorded in `lab/replay-report.json` | new `apps/jobs/scripts/replay-lab.mts` | Merge `lab/stage2/verdicts-{en,ar}.jsonl` with `merge_verdicts.py` first (moved here from P3-1; done, 73 to 80 verdicts, 49 to 55 passes). Read the ledger, resolve every `file` and every `references[].file` from disk (all 91 resolve today), and run whatever verifier is registered over decoded bytes, never a URL. Emit `lab/replay-report.json`: per-row verdict, per-gate `{id, passed, measured, threshold}`, and a confusion matrix keyed by defect tag with the human verdict as truth. Print a census header (rows, verdicts, passes, per-tag counts) so a later reader cannot inherit a stale number | the harness runs against today's `MockStudioVerifier` and reports the baseline it must beat: 55 of 55 passes accepted, 13 of 13 fail rows and 25 of 25 tagged rows falsely passed. Census header matches the numbers in this section | S |
| P2-2 | **done (session 2)**: `photo-mask.ts` + `mask-contact-sheet.mts`, 91 overlays, studio pass p05 0.29 but no separation because the ledger stencils for framed-minimal, diamond-rails and origami-ribbon are bare names while the photograph carries a frame, rail or ribbon; nine on-skin and close-up rows give an empty mask (reported as `registration_failed`, not a low IoU) | new `apps/jobs/src/photo-mask.ts`; new `apps/jobs/scripts/mask-contact-sheet.mts` | Edge based, never colour thresholds. The Sobel-plus-Otsu-plus-largest-component recipe was measured on this corpus and does not find the pendant: the gradient shatters into 84 to 4,263 components, the largest filled component covers 0.4% to 24% of the frame, an opening at 1.5% of width empties two of the 49 passing masks and is wider than a Playfair hairline. So: hysteresis on the gradient rather than one Otsu cut, a close radius derived from measured stroke width rather than 3, and no chain-stripping opening at all (the chain through a ring is what a passing ring looks like). Registration is named explicitly: a similarity transform (uniform scale, in-plane rotation, translation) initialised from image moments and refined by a bounded search, scored by IoU, with the transform stored in the gate record and an out-of-range transform reported as `registration_failed` rather than as a low IoU. Studio packshot only | write per-image overlays plus an `index.html` contact sheet under `docs/goals/road-to-gold/dogfood-<date>/phase2-mask/`; the lead opens it in its own browser at 1440x900 and looks at all 55 passing masks. Print the IoU distribution over the passes (min, p05, median, max). The gate threshold in P2-3 is the measured p05 rounded down, not 0.85, and the row records the number that was measured | M |
| P2-2b | Stencil carries the construction geometry | `packages/identity/src/caleums-arabic-v3.ts`, `shaping.ts`, `packages/config` | Decision by default from the P2-2 contact sheet: the stencil is the truth for the whole physical piece, not only the name. `framed-minimal` gains its rectangular frame rails and `diamond-rails` its bar, drawn deterministically around the name from validated constants (rail thickness, inset, corner radius) with the rings on the frame or bar as the lab prompts describe; `classical` and `origami-ribbon` unchanged (the ribbon is decorative, reported not claimed). The construction id becomes part of the fingerprint input. Prompts are re-proved against these renders in Phase 3 | `render-stencils` matrix renders every construction cell as one piece with two ring holes; the P2-2 contact sheet rerun shows the registered stencil on the frame and letters for `framed-minimal` rows; the studio pass IoU distribution per construction is printed and the per-construction p05 recorded in the P2-3 row | M |
| P2-3 | `DeterministicStudioVerifier`, gates per view | new in `apps/jobs`, geometry from `packages/identity`, thresholds in `packages/config` | Studio packshot: G1 approved text agrees between the revision and `identity_artifacts.approved_text` after the same normalisation both sides (adversarial finding 9: one is NFC, the other raw), recorded as a plumbing consistency gate and not as verification of the photograph; G2 pendant components == 1; G3 an annulus of metal at each expected ring anchor, registered from the stencil, never "empty round holes == rings" (per `VIEWER-RUBRIC.md:41` an empty hole is the defect and a passing ring has a chain link in it); G4 bbox margin from every edge; G5 stones requested implies stone-like blobs inside the registered mask, aimed only at the failure the lab actually measured, "the model returned zero stones"; G6 registered IoU at or above the threshold measured per construction after P2-2b, computed inside the registered stencil's bounding box dilated by one ring diameter so the chain outside the piece does not count; G6 is claimed only for constructions whose stencil describes the whole piece (`classical`, and `framed-minimal` and `diamond-rails` after P2-2b), reported for the rest. IoU cannot see an extra ring (about 2% of the area); that stays G3's job. On-skin, close-up and dark: G1 plus a same-object IoU against the verified studio master (the pipeline already supplies it as `referenceImageUrl`), no G2, G3, G4 or stencil G6. Every threshold is validated configuration in `packages/config` with a schema floor so it cannot be tuned to zero from a deployment console, and every gate returns `{id, passed, measured, threshold}` | P2-4. Plus negative proof in the style of P1-6: take three passing lab stills, paint a ring hole closed, cut a letter free, and move the pendant to the frame edge; each tamper fires exactly the gate that owns it, output saved under `docs/goals/road-to-gold/dogfood-<date>/` | M |
| P2-4 | Replay it, publish the matrix, claim only what it proves | `apps/jobs/scripts/replay-lab.mts` | Run the deterministic verifier over the merged corpus. Claimed: zero false passes on the 7 geometry rows and on the 3 tamper fixtures, at most 10% false fails on the 55 human passes, and the studio-only and dependent-view numbers reported separately. Reported but not claimed: `wrong-stones` (4 rows, and G5 can only see "zero stones when stones were requested" - it cannot tell partial pave from full pave, which is the "competent photograph of the wrong product" risk in `IMAGE-LAB.md`), `wrong-display` (3 rows, a view-brief axis no gate encodes), `wrong-look`, `cgi-look`, `unsupported-geometry`. Note in the report that all four `stage4` stone rows carry an empty `note`, so their tags have no written evidence behind them, and that every ledger row references a pre-Phase-1 lab stencil, so the threshold must be re-measured against `caleums-identity-v4` renders in P3 | `lab/replay-report.json` committed, matrix pasted into the row, and the false-fail images opened in the lead's browser one by one - a false fail on a customer's piece is a pendant that never arrives | M |
| P2-5 | Contract and wiring, with a fixture path that makes it visible | `packages/ai/src/studio.ts:24-35,41-50`, `apps/jobs/src/presentation.ts:390-396,404-412,414-461,1162-1196`, `packages/config/src/index.ts` | `VerificationDecision` gains `gates: [{id, passed, measured, threshold}]`; a failed gate throws `verification_failed:<ids>`. The verifier port takes decoded bytes and the stencil bytes, not a signed URL: a URL fetch inside the verifier turns an expired signed link into a paid regeneration. A deterministic failure gets its own budget - one in-place regeneration, then straight to `operator_review` - because the current name-mismatch path allows three paid attempts and a systematic gate bug would burn the whole P5-1 reservation on every run. `terminal_error_code` is only written when the failure is terminal, so the proof reads the terminal row, not the retrying one. Add `MOCK_STILL_FIXTURE_DIR` (validated config, empty by default): when set, `MockStudioGenerator` returns a real lab PNG while keeping `provider: "mock"`, so the deterministic verifier runs end to end on staging for zero spend and the UI still refuses to present the still as the customer's piece | deploy to staging with `MOCK_STILL_FIXTURE_DIR` pointing at one passing and one `extra-ring` lab still; drive one run per fixture in the lead's own browser; the passing run reaches four ready tiles, the defect run reaches `operator_review` with `verification_failed:G3` in `terminal_error_code`, read back from the database and pasted | M |
| P2-6 | Name reader keeps spelling authority | `packages/ai/src/studio.ts:293-302,325-388`, `apps/jobs/src/presentation.ts:396-461` | Runs only after the deterministic gates pass, so a geometry fail never buys a paid read. Read blind: the approved name comes out of the read prompt entirely (it is in the prompt today, so both `text` and `matches` are anchored on the answer) and the comparison happens in code. `identityTextMatches` accepts any single Damerau-Levenshtein edit on a 5+ character Latin name, which passes Layla for Kayla, Nadia for Nadir and Sarah for Sarai as well as the transposition the old row named; replace the tolerance with exact match, or with an explicit confusable table (l/I, rn/m, cl/d) and nothing else | free half first: a script prints the accept/reject decision for a fixture table of 20 name pairs (exact, transposed, one substitution to a different real name, script mismatch, combining mark) and the reject set is what the row says. Paid half needs DS-11 | S |
| P2-7 | Attachment reader, paid, ceiling enforced in the script | replaces the retired `OpenAIStudioVerifier` (`packages/ai/src/studio.ts:169-264`) | Structured per-ring JSON `{somethingThroughHole, daylightBothSides}` on a 2x crop around each G3 anchor, phrased from `VIEWER-RUBRIC.md:41`. Stencils and stills go to the API as base64, never as a `file://` URL. DS-11's ceiling cannot be enforced by `runtime_policy`: that trigger only fires on `principal_daily_usage`, which a script never writes, so the harness owns its own budget - a call counter, a per-call cent estimate, a hard stop at the ceiling, and the actual spend printed and recorded in the handover | `--with-readers` replay: the 5 `chain-not-through-ring` rows reported twice, standalone recall (all 5 reach the reader) and pipeline-composed recall (2 of the 5 are rejected by G3 first and never reach it); at most 2 false fails on the 55 passes; spend printed and under the ceiling | M |

## Phase 3 - prove the prompts on Runway (needs P0-1, P0-3)

Runway: workspace "Sanchay", `gpt-image-2`, 305,042 credits on 7 September, about 20 per image, 50 tasks in flight.
Current best: look `framed-minimal`, release `caleums-universal-v4.3` (`lab/compile.mjs:14,49-53`).
Seen name: Asma in both scripts. Holdout trio for v4.3 only: Noor, Layla, Muhammad.

| Id | Task | Change | Proof | Size |
| --- | --- | --- | --- | --- |
| P3-1 | **done (session 2)**: viewer scored the 11 (10 pass, 1 tweak `framed-minimal-layla-en` a2 floating letters); merged, ledger 91/91 scored, 65 pass 13 tweak 13 fail; v4.3 studio 3/3 on both scripts, holdout 10/12; final Runway balance 305,042 read from `whoami`. Decision by default: jump rings looped around the frame's top bar with the chain through them pass the ring-and-chain test (`framed-minimal-ar` a6, `framed-minimal-muhammad-ar` a1), lead confirmed by eye | Dispatch one `viewer` on the 11 unscored images: v4.3 studio en a5-a7, ar a6-a7; holdout layla-en a2, layla-ar a2, muhammad en and ar a1-a2. Merge with `merge_verdicts.py` (the stage-2 verdict files are already merged by P2-1), rerun `finalise.py` | both phase-3 gates become decidable: 3/3 on both scripts (v4.3 a5-a7) and 10/12 holdout (5 pass, 1 tweak, 6 unscored today; needs 5 of the 6) | S |
| P3-2 | **done (session 2)**: `holdout.json` (65 hashes, 6 frozen with the trio), `compile.mjs` refuses unfrozen, burnt or seen names and freezing onto a hash with images; six v4.3 prompts byte-identical | new `lab/holdout.json` keyed by `promptSha256`; `compile.mjs` refuses to compile a holdout name for a hash that is not frozen | Any v4.4 written in reaction to layla-en a1 makes the trio "seen"; its holdout must be fresh names frozen per prompt hash | S |
| P3-3 | **done (session 2, `3e731f9`)**: six Kufi `-norings` holdout stencils from the lab renderer, `verify_stencil.py` components 1 on all six, 16 rings-off records manifested, `build_stencils.py` additive and idempotent and refuses production-sourced files; lead opened `muhammad-en-kufi-norings.png`. Was: missing stencils | six Kufi `-norings` holdout stencils; `build_stencils.py` emits and manifests them | `verify_stencil.py` one component each | S |
| P3-4 | **done (session 2)**: `lab/plan_grid.mjs` compiles a grid through the guarded compiler, bare-array `index.json` plus `plan.json`, `--reserve`/`--unreserve`/`--status`, idempotent; `ingest_results.mjs` skips planned rows | new `lab/plan_grid.mjs` emitting prompts and `index.json` from `compile.mjs`; results, credits and download assembly scripted; in-flight accounting; ledger row at submit so a restart is safe | plan for looks 4 x scripts 2 x letterings 2 x names x 3 attempts compiles with no hand edits | M |
| P3-5 | Run the studio grid | `image-lab` agent generates with `count=4`, 12 calls in flight; `viewer` scores; lead merges. One axis per release; a generator never reads verdicts of a release before its batch is complete | one look at 3/3 both scripts, then 10 of 12 holdout, with per-cell evidence in `IMAGE-LAB.md` | L |
| P3-6 | Dependent views on holdout names | on-skin, close-up, dark from each passed holdout master, under the winning release; prove either `DEPENDENT_REFERENCE_RULE` plus blurred anchor (what production does, `presentation.ts:180-185,793-800`) or the lab's stencil-plus-master pattern, and make production match the winner | at or above 8 of 8 per view as in stage 3 | M |
| P3-7 | Lab prompt becomes production prompt | `packages/ai/src/prompt-registry.ts:21-39,89-91,196-211,370-382`; `POST /api/operator/prompts` | Express v4.x per profile with the look brief baked in; add the missing `construction` slot; publish `image.packshot@v2`, `image.worn@v2`, `image.macro_gift@v2`, `image.dark_editorial@v2` through the registry | a mock run pins the v2 releases in `generation_tasks.prompt_release` | M |

## Phase 4 - publish the six anchors (no generation)

The PNGs and `manifest.json` are at `~/hq/projects/devonel/caleums-private/style-anchors-v1/` (laptop) and `~/.codex/state/jewelo/caleums-style-anchors/v1/` (home-mini); source task ids match `docs/CALEUMS-FINAL-E2E-CONTRACT.md`; never commit them.

| Id | Task | Change | Proof | Size |
| --- | --- | --- | --- | --- |
| P4-1 | `scripts/style-anchors/publish.mjs` | Per anchor: sha256 equals manifest; profile map (worn, packshot, macroGift, darkEditorial, studioHero, billboard); `sourceTaskId` equals `STYLE_ANCHOR_SOURCE_TASK_IDS` (`prompt-registry.ts:148-155`); PUT bytes to `style-anchors/<profile>/v1/<sourceTaskId>.png` with the service role; `rpc/create_style_anchor_release` with `p_bucket_id='style-anchors'`; `rpc/publish_style_anchor_release` against the seeded pointer `00000000-0000-0000-0000-00000000060N`; re-download via signed URL and re-hash | script output lists six releases `published` version 2; SQL `select p.profile, r.version, r.status from style_anchor_publications p join style_anchor_releases r on r.id = p.release_id` | M |
| P4-2 | Prove real mode no longer fails closed, zero spend | `new SupabasePresentationRepository(url, key, false).signedStyleAnchorUrl({style_anchor_release_id, presentation_view: 'on_skin'})` returns a low-passed `data:image/png;base64` anchor instead of throwing `style_anchor_missing:<sourceTaskId>` | printed result for all four customer profiles | S |

## Phase 5 - plug in OpenAI (needs P1, P2, P3, DS-6)

| Id | Task | Change | Proof | Size |
| --- | --- | --- | --- | --- |
| P5-1 | Cap before the first call | live `runtime_policy`: `global_max_reserved_spend_cents=800`, `global_daily_generation_limit=2`, `daily_generation_limit=2`; studio-only smoke before P4 publication per DS-6, or cancel the three dependent tasks at run creation. A run reserves 400 cents and the cap bounds reservations, not calls: 4 tasks x 3 attempts can be 12 images | SQL readback | S |
| P5-2 | Flip and smoke | DO env `PROVIDER_MODE=real`, `OPENAI_API_KEY` present, deploy, `smoke.sh`; one run per script driven through `/api/designs/drafts` then `/api/revisions/approve` with `construction=framed-minimal` (the UI refuses that look until P6-2) | the studio still passes P2-3, matches the Runway image for the same stencil and prompt hash, `provider_attempts.actual_cost_cents` and `principal_daily_usage` recorded, both stills opened in the browser | S |
| P5-3 | Rollback rehearsed | `PROVIDER_MODE=mock`, `scripts/digitalocean/rollback.sh`, policy values restored | readiness ready in mock after rollback | S |

## Phase 6 - make the interface honest (needs DS-4, DS-5 for P6-2, P6-3)

Code review of the surface on 8 September 2026: `docs/goals/road-to-gold/reviews/ux-review-1.md`. Its four blocking findings (crash on confirm with an invalid draft, mobile photo cropped to half, false per-view failures with a dead Retry, English errors in the Arabic journey) are part of these rows; B1 and B3 land with defect 6.

| Id | Task | Files | Change | Proof | Size |
| --- | --- | --- | --- | --- | --- |
| P6-1 | **done (session 2, `4c08b35`, staging `d4bb713f`)**: `--action-bar-block 86px` subtracted from the sticky budget and added to `.main` padding; lead measured a 214 px gap between `previewSticky` bottom and `actionBar` top at 1440x900, 1280x720 and 1024x768 with nothing scrolling inside the panel; mobile photo 4:5 contain. Was: preview panel under the action bar | `apps/web/src/features/atelier/atelier.module.css:129-133,1468-1478,1560,1703-1705` | Do not move `.intro` (at 767 px and below `.preview { order: -1 }` puts the photo first and the h1 would fall below it; at 768 to 1099 the h1 would wrap in a half column). Add `--action-bar-block: 86px`; subtract it in `.previewSticky` max-height and add it to `.main` padding-bottom; then lower the 280 px photo floor or compact `.previewDock` at short heights: at 1024x768 the dock alone is 396 px against a 558 px budget, and at 1280x720 the overlap at rest is 295 px | measured in the browser at 1440x900, 1280x720, 1024x768: `previewSticky` bottom at or above `actionBar` top after scroll, and no control hidden at rest; screenshots under `docs/goals/road-to-gold/dogfood-<date>/` | M |
| P6-2 | **done (session 2)**: every construction and English lettering that `preflightRefusal` refuses carries `data-not-photographed` with "Not yet photographed" on the tile and the full sentence under the fieldset when selected (Origami ribbon, Framed minimal, Diamond rails; Minimal, Diwani, Kufi, Signature, Thuluth inspired in English; none in Arabic); refused wins over sample-coming, one note; the sample photograph says "Sample look, not your piece" in the caption and the full sentence under the photo until the shopper's own photograph shows; EN and AR; lead verified on the local app at 1440 and 390 in both locales. Was: silent style refusals | `Atelier.tsx:828-870` (`choices`, one `<small>` and `data-sample-coming`), `personalizedRun.ts:344-360` | Per DS-4: either hide the unrenderable options or add an orthogonal `data-not-photographed` note with EN and AR copy (no AI, generate, magic words); the design stage must import `preflightRefusal`, never re-list its constants; note Kufi is both sample-coming and refused, so define precedence | pick every construction and lettering at 1440 and 390; the shopper knows before the checkbox | M |
| P6-3 | **done (session 2, `823b3cc`, staging `846fb7fc`)**: DS-5 default taken; button renamed "Review my piece" / "مراجعة قطعتي" and opens step 02 with no API call, the checkbox stays the commitment; terminal run states (`cancelled`, `operator_review`) land on honest rows, headline, bar and photo captions; development-only fixture drives them. Was: "Preview my piece" does what it says | `Atelier.tsx:521-609,1322-1327,1653,1997-2008`; AR dictionary keyed by English literal at `:49,:60`; `docs/goals/overnight-launch-2026-09-08.md:62,183` | Per DS-5: either the button starts the run with the spelling confirm inline, or it is renamed to what it does and the checkbox stays the commitment; update the AR key or Arabic silently falls back to English; amend the goal-doc lines that name the old label | click through in EN and AR; the network tab shows the run starting where the label says | M |
| P6-4 | **done (session 2, `7091c90`, staging `846fb7fc`)**: `id="confirm-spelling"` plus `aria-describedby="confirm-blocked"`, disabled while the name is invalid; `error.tsx` boundary for the design segment; the atelier validates with the contracts rules (`NAME_MAX` 30, `NAME_FIT_MAX_LATIN_CHARACTERS` 37); lead verified the former crash path and the 30-character message on the local app. Was: confirm checkbox accessibility | `Atelier.tsx:1322-1327` | Add `id` and `aria-describedby`; today it has neither and does not surface in the accessibility tree by name | keyboard-only pass at 1440x900 reaches it and the name is read | S |
| P6-5 | **done (session 2)**: twelve strings moved into the dictionary (header note, photo caption, footer, `Unavailable`, `mm`, `18K <metal>` via `goldLabel`, carousel role description, storage warning, second-name placeholder); IBM Plex Sans Arabic served through `next/font` for the `ar` locale only, self-hosted, `preload: false`; lead verified `/ar/design/new` at 1440 and 390: computed face is IBM Plex Sans Arabic on the app and the h1, zero Latin runs outside CALEUMS and Asma, footer in Arabic, no overflow; `/en/design/new` unchanged (Instrument Sans, Playfair Display, `18K yellow gold`). Row was stale: "Checkout is coming soon" and the slideshow labels were already translated. Was: Arabic loose ends | `Atelier.tsx` dictionary, fonts | "Checkout is coming soon" and slideshow aria labels untranslated; no Arabic web font served (system font); caption label half untranslated after a run | `/ar/design/new` at 1440 and 390 shows no English strings and a served Arabic face | S |
| P6-6 | **done (session 2)**: `TASK_STATUSES` and `RUN_STATUSES` listed once in `personalizedRun.ts`; an unknown task status maps to `unknown` (terminal, strands dependents), an unknown run status counts as stopped; both land on the P6-3 `unavailable` end state with the contact capture; harness before/after diff shows only the two injected `banana` rows changed. Was: residual polling | `personalizedRun.ts:115-120` | An unrecognised run or task status maps to `queued` and polls the full six minutes; map unknown terminal statuses to stopped | injected unknown status stops the poll | S |
| P6-7 | **done (session 2)**: `deviceState.ts` is the one owner of atelier browser storage (load, save, clear); the saved record is scoped `atelier.v1.<locale>` so a record from another journey never restores a name into the field; "New piece" clears the record, the pipeline submission and the idempotency keys, keeps `jewelo:anonymous-session:v1` (no anonymous sign-in spent), resets state and reloads; with pieces in the bag it asks once (EN and AR); lead drove it at 1024x768: typed name survives a reload, "New piece" from the bag empties the name, keeps the session key, shows the sample panel, no auth call. Was: tablet shared state | `Atelier.tsx` bag and last reference | One tablet is one anonymous principal; bag and reference are shared across shoppers. "New piece" between customers must clear both without burning an anonymous sign-in (30 per hour per IP) | two consecutive shoppers on one tab at 1024x768 see no bleed | S |

## Phase 7 - close the shop loop

| Id | Task | Files | Change | Proof | Size | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| P7-1 | **done (session 2)**: queue shows each request's stills through the `/api/state` signer with non-presentable assets tagged "Sample asset, mock mode - not the customer's piece", `wa.me` and `tel:` links, a persisted note, `mark_contacted` / `mark_fulfilled` / `mark_cancelled` / `note` commands validated by `previewRequestCommandSchema` with the transition table in contracts, a status filter, and "Pieces stopped for a look" listing `operator_review` runs with the reason in plain words plus the raw code and a "Photograph it again" retry per blocked view; lead drove it on the local app at 1440 and 390 and RTL: photographs shown and tagged, note saved and read back after reload, Zara sample request moved to Made with the other buttons disabled, 10 stopped runs listed with `identity_no_ring_seat` in words. Was: operator queue Omran can run a shop from | `apps/web/src/features/admin/PreviewRequestQueue.tsx`, `api/operator/commands/route.ts:43-115`, `lib/backend/preview-requests.ts:42-46` | Show the linked run's ready stills via signed URLs; `wa.me` and `tel:` links; note field; `fulfilled` and `cancelled` commands (the schema allows them, the route only has `mark_contacted`); status filter; surface the 7 runs in `operator_review` with the existing `review_task` retry that has no UI | screenshots at 1440 and 390, RTL, with the 13 live requests | M | agent |
| P7-2 | Remove the dead commerce surface | `OperatorExperience.tsx:343-427`, `supabase-jewelo-client.ts:487-500`, `Atelier.tsx:1318,2161` | Quotes and orders are always empty; "Customer view" links to a page that does not exist; `issueQuote` sends `total || 2290`. Launch is request-only per the journey lock; checkout stays fail-closed 503 until Omran supplies a Shopify store | operator page has no dead links | S | agent |
| P7-3 | New-request notification | new Inngest function in `apps/web/src/inngest/functions.ts` | Nobody is told today; needs DS-8 | injected request reaches the channel inside two minutes | M | Sanchay picks, agent builds |
| P7-4 | Quotas before a shop day | `runtime_policy`, `principal_daily_usage`, Supabase auth config | Clear test usage (32 of 100 global runs used today), set shop values, note the 05:30 IST UTC rollover and the 30 per hour per IP anonymous cap on shop Wi-Fi | SQL readback | S | agent; values Sanchay |
| P7-5 | Observability | `apps/web/package.json`, `packages/config/src/index.ts:26-28`, `packages/observability/src/index.ts`, DO alert spec | Zero SDKs installed, DSN empty, errors reach only DO runtime logs; add `@sentry/nextjs` and PostHog, wire `jsonError`, add restart and memory alerts; needs DS-9 | a forced error appears in Sentry; DO alert email arrives | M | Sanchay accounts, agent wires |
| P7-6 | Inngest durability | `docs/goals/overnight-launch/w0/INNGEST.md:90-96` | A restart loses in-flight runs; the sweeper routes stale reserved attempts to `operator_review` (no double charge, manual recovery). Per DS-10 | kill the `inngest` component mid mock run on staging; the task reaches `operator_review` inside four minutes, and with Cloud it completes instead | M | Sanchay decides, agent proves |
| P7-7 | Backups | none exist (Free tier, `pitr_enabled: false`) | Pro gives daily backups; otherwise nightly `pg_dump` over the session pooler from `home-mini` plus a restore drill into a scratch database | restored database answers `/api/state` | S | agent; Pro is DS-1 |
| P7-8 | Operator auth hardening | `apps/web/src/lib/backend/operator-session.ts:38-53`, `api/operator/session/route.ts` | Unlimited login attempts on one shared account; add a lockout and a revocation path | 429 after N failures in the browser | S | agent |

## Launch (needs DS-7)

| Id | Task | Change | Proof | Size | Owner |
| --- | --- | --- | --- | --- | --- |
| L-1 | Production app and domain | `pnpm do:bootstrap -- production` behind `JEWELO_ALLOW_PRODUCTION_BOOTSTRAP=yes`; Caleums domain from Omran; env copied with real-mode values and shop quotas | `doctl apps list` shows production ACTIVE; `smoke.sh` on the domain | M | Sanchay approves and supplies DNS, agent executes |
| L-2 | Final dogfood on the live URL | Two unseen names, one per script, on a phone and a laptop, through to the request landing in the operator queue and the notification arriving | screenshots per step under `docs/goals/road-to-gold/dogfood-<date>/` | S | agent |
| L-3 | Omran's runbook | new `docs/SHOP-RUNBOOK.md` | Login (`/en/operator`, credentials handed over out of band), one tablet equals one principal, "New piece" between customers, what to do when a request arrives, what to say when a preview is being prepared and the delivery window the shop can keep, the 05:30 IST budget rollover | Omran reads it and runs one request end to end | S | agent |
| L-4 | Handoff packet | `docs/goals/road-to-gold/PROGRESS.md` final entry, PR to `main` | Objective, exact commands and results, evidence paths, open findings with owners, spend actually used, rollback | PR open, not merged | S | agent |
