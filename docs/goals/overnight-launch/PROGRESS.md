# Overnight launch progress - 7/8 September 2026

Durable truth for a restart. Goal: `docs/goals/overnight-launch-2026-09-08.md`.
Branch: `codex/overnight-launch-2026-09-08` (base `main` at `e8d3dc0`).

## Clock

- Run started 02:49 IST, 7 September 2026. 14 hour wall-clock limit ends 16:49 IST.
- The goal file's "05:30" lab stop and "06:30" real-mode decision are read as relative deadlines for this run: lab hard stop 12:00 IST, real-mode go/degrade decision 13:30 IST, deploy and live QA after that.

## Environment facts found at start

- Inngest Cloud: NOT signed in in the Work Chrome profile (redirected to sign-in). The self-hosted fallback from the goal file applies: `inngest start` as a DO service component with `--postgres-uri` on the new Supabase database, keys generated locally, `INNGEST_BASE_URL` set. Cloud remains a one-env-var switch.
- Supabase dashboard: signed in, org Devonel (Free plan, 1 project).
- Runway MCP: workspace Sanchay, `gpt-image-2` available, 306,862 credits at start.
- doctl works through `scripts/digitalocean/doctl.sh` (token from `.env`); app `jewelo-staging` `ec09c9fd-84e4-45c5-b60a-fd62277af322`.
- gh authenticated as kaldrex. Disk 28 GiB free.

## Done (with evidence)

- 03:10 Supabase: new project `jewelo-caleums` created in Devonel org, ref `jggalwuvpcqoenhirmnl`, ap-south-1, Free, Postgres 17, status healthy. PAT `jewelo-agent-2026-09-08` (30 days) created. `.env` rewritten with the new SUPABASE_* values (legacy JWT anon/service_role keys; project ref, URLs, DB password, DB URL, access token); every TRIGGER_* line removed; old `.env` backed up in the session scratchpad only. REST on the new project answered 200 with the service key; Management API answered 200 with the PAT. Finding: the old project `jewelo-v2-mumbai` is the same ref as the old `.env` and IS reachable from this Chrome and from the stored CLI login (the goal's premise was wrong); it is kept until the new project is verified, then deleted. Finding: the Devonel org is in a Free-plan grace period (egress 52.5 of 5 GB, 1,049%) until 29 Sep 2026; after that all its projects return 402. This needs Sanchay (upgrade to Pro or move to another org).
- 03:15 Plan review done: `docs/goals/overnight-launch/reviews/plan-review.md`. Corrections sent to W0a, W0b, W2.

- 02:52 Env schema: empty string for optional URL/key fields now means unset (`packages/config/src/index.ts`, `optionalOf` preprocess). Unit test added. `pnpm --filter @jewelo/config test` -> 7 passed; typecheck clean.
- Agent definitions created in `.claude/agents/`: implementer, platform, image-lab, viewer, browser-qa, reviewer (model claude-opus-5, effort xhigh). This session's Agent tool did not pick up new definitions created after startup ("Agent type 'implementer' not found"), so each role is dispatched as a general-purpose agent with model opus and the role text inline; frontmatter effort therefore could not be applied. Each agent reports its model id in its first report line.

## Done (continued)

- 06:05 W4a request-capture backend done (agent report, model claude-opus-5): migration `20260907010000_preview_requests.sql`, POST/GET `/api/preview-requests`, operator queue route + command + `PreviewRequestQueue`, contracts, 24 route tests, RLS proof 13/13 (`docs/goals/overnight-launch/w4/rls-proof.sql`), e2e steps 47-53. Lead pushed the migration (`supabase db push` applied 20260907010000) and regenerated types (+82 lines); REST probe on `preview_requests` = 200.
- 06:08 Lead fixed three pre-existing `pnpm test` failures on main: `ui-presentation.test.ts` expectation stale since d2bfe4e (all Arabic styles provider-supported), `shopify-adapter.test.ts` expects the deliberate `Checkout is not available yet` 503, `supabase-jewelo-client.ts` guards `document` in node. 10/10 pass.

- 06:12 W1 done (model claude-opus-5): renderer removed, 4 Diwani files rejected (أسمك), 4:5 preview frame, autoplay off in design, Arabic strings. Gates: typecheck 0, lint 0, unit 34/34, e2e 45/45. `docs/goals/overnight-launch/w1/REPORT.md`.
- 06:14 W0b done (model claude-opus-5): Trigger.dev removed entirely; Inngest functions in `apps/web/src/inngest`, served at `/api/inngest`; event id = outbox dispatch key, no function idempotency; crons behind `INNGEST_CRON_ENABLED=1`; readiness/smoke/env-contract/deploy.sh rewritten; self-hosted `inngest/inngest:v1.44.0` deployed as DO component (deployment ad1be728 ACTIVE, 14 tables in schema `inngest`, IPv4 session pooler); local proof: 5 functions synced, one run with 5 tasks ready (`w0/inngest-dev-run.json`, `w0/inngest-dev.png`). Gates: `pnpm typecheck` 0, `pnpm lint` 0, trigger grep empty. Caveat: in-flight Inngest run state is in-process (restart loses it; outbox + sweeper are the durable truth). Docs updated (CLAUDE.md, FINAL-STACK, ARCHITECTURE, MEDIA-CONCURRENCY, DECISION-REGISTER D-017, DIGITALOCEAN-DEPLOYMENT, `w0/INNGEST.md`).

- 06:17 W0b follow-up done: dependent views of a terminal studio still now block once (migration `20260907020000_dependent_view_terminal_gate.sql` applied; `dependencyTerminalStatus` in presentation.ts; 2 unit tests). The two stale `@jewelo/jobs` tests from commit d2bfe4e fixed truthfully. jobs 16/16, data 30/30.

- 06:40 W0a done (model claude-opus-5): project linked, 17 historical migrations applied clean on PG17 + global daily spend cap migration (`20260907000000`, global 6000 cents, 100 runs/day, per-principal 1200 kept, proved in a rolled-back transaction) = 20/20 applied remotely; seeds verified (12 prompt_releases, 6 style_anchor_releases all `missing`, 4 private buckets); anonymous sign-in had to be enabled on the new project (Management API); DO env rotated to the new Supabase values and TRIGGER_SECRET_KEY removed (deployment c2e45a18 auto-started by the spec update). e2e-backend: 4 failures left (06/08 stale want=4 assertions; 38/39 dropped dispatch not reconciled and failed still never retried) handed to W0b. `docs/goals/overnight-launch/w0/DATABASE.md`.

- 10:54 W3 done (model claude-opus-5): two-tier resolver `resolveIllustration` (Tier 1 script/names/layout/construction/lettering; Tier 2 metal/stones/gem/size/chain) with bases exact -> same-script sibling -> other-script -> labelled placeholder; construction never changes; Preview my piece never disabled; only photographed camera tiles render; quiet metal/stones note (EN/AR); `sample-assets-v10.json` ships empty for W2 output. Coverage: 288 Tier 1 designs, 28 exact, 260 labelled siblings, 0 without a photo. Tests: 10 new (never-changes-design across 288 x 19 clicks and 28 x 576 full configs, zero-family base case, Diwani never shown). Gates: typecheck 0, lint 0, unit 75/75, e2e 55/55. Evidence `docs/goals/overnight-launch/w3/` (journeys at 390 and 1440, RTL). Stale non-gated `tests/atelier.spec.ts` left untouched (12/14 failing before W3).

- 10:56 W4 UI done (model claude-opus-5): `previewHandoff.ts` maps the draft to a complete CreateDraftInput with documented defaults; `personalizedRun.ts`, `previewPipeline.ts`, `usePersonalizedPreview.ts` drive anonymous session (persisted, refreshed), draft -> approve (spellingConfirmed from the checkbox, requestKey reused) -> `/api/state` polling + Realtime; review stage shows per-view queued/generating/verifying/ready/retrying/failed/blocked with customer vocabulary, Studio hero on first ready, mock assets never shown as the customer's piece; honest degrade (dispatch failure, 429 spend guard, daily limit with try-again-tomorrow, settled run with nothing presentable, 6-minute ceiling) captures contact via POST /api/preview-requests; bag stores run id, asset ids, preview-request id. 24 unit tests, Playwright photo-journey with stubbed pipeline. Gates: typecheck 0, lint 0, unit 75/75, e2e 55/55. Journeys at 390, 1440 and 390 Arabic under `docs/goals/overnight-launch/w4/shots/`. Deviations (verified): browser signs in anonymously via the Supabase client instead of POST /api/auth/anonymous; approval already creates the run so /run is only a recovery call. Risk: real mode has no geometry verifier (MockStudioVerifier on both branches; only the OpenAI name reader is real).
- 10:58 Root gates on the tree: `pnpm typecheck` ok (13/13), `pnpm lint` ok (13/13), `pnpm test` ok (16/16 tasks: config 7, media 1, data 30, contracts 16, ai 13, testing 1, jobs 16, web 145). `bash scripts/scan-secrets.sh` passed. Checkpoint commit `7b76e5a` pushed; draft PR https://github.com/Sanchay-T/jewelo/pull/12 opened to main. `pnpm do:build` exit 0.
- 11:02 `bash scripts/digitalocean/deploy.sh staging codex/overnight-launch-2026-09-08` started deployment b28ca9c7 (source commit 7b76e5a).

- 11:10 W0b e2e gate: `BASE=http://localhost:3001 E2E_MOCK=1 bash scripts/e2e-backend.sh` -> `PASS · 0 failed assertions · 412396ms`, EXIT=0 (skips by design: 04 transliterate needs a real OpenAI call; 43-44 needs a second web instance). Fixes: steps 06/08/16 assertions match the studio-first contract (08b added, asserts all four outbox rows published after release); steps 38/39 were the crons being off locally (3001 now runs with INNGEST_CRON_ENABLED=1, readiness cronsRegistered true; retry path proved retrying a1 -> a2 -> blocked a3 -> operator_review -> 409 budget exhausted); real defect fixed in `packages/data/src/outbox-dispatch.ts` (outbox read used the caller clock for available_at, so a lagging web clock read zero rows; 30 s skew tolerance, Postgres still enforces available_at). PROVIDER_MODE=mock added to the DO web env. Evidence in `w0/INNGEST.md`.

- 11:15 Deploy: DO deployment `b28ca9c7-f5f3-460d-af57-15aa0321422f` ACTIVE, source commit `7b76e5a5` (branch head moved to a4679fa afterwards for the progress/e2e commit; final redeploy at handoff). `bash scripts/digitalocean/smoke.sh https://jewelo-staging-gqumd.ondigitalocean.app` -> health passed, readiness passed, EXIT=0. `/api/readiness` on the live URL: `{"status":"ready","dependencies":{"supabase":"configured","inngest":{"configured":true,"keyEnvironment":"prod","transport":"self_hosted","cronsRegistered":true},"openai":"configured"}}`. Signed `GET /api/inngest` introspection: HTTP 200 `{"has_event_key":true,"has_signing_key":true,"function_count":5,"mode":"cloud","authentication_succeeded":true,"app_id":"jewelo-caleums","sdk_version":"4.20.0"}` (unsigned GET is 401 by design in cloud mode). Self-hosted Inngest DB (`inngest.apps`, `inngest.functions`): app `jewelo-caleums` sdk v4.20.0 synced 05:47:06 UTC with functions presentation-task, video-submit, video-poll, outbox-recovery, stale-media-recovery.
- 11:19 Dispatched: browser-qa A (live journeys at 390, 1440, /ar), browser-qa B (320, 768, 1024, 390x600, concurrent shoppers), reviewer (adversarial review of the slice).

- 11:35 Live QA A done (model claude-opus-5) against https://jewelo-staging-gqumd.ondigitalocean.app: Journey 1 (390x844, Arabic أسماء, Framed minimal, Kufi, rose, Accent) and Journey 2 (1440x900, English Asma & Fatima, Stacked, yellow) both ended in the honest "Your personalized preview is being prepared. We will send it to you." state with contact captured (references 6d650b10-218a-47f5-8795-b6dd32dee1c8 and 7905a9c1-a1f3-421a-9817-ac3463caa8ae), bag, reload reconstruction, Tier 2 edits never changed the pendant; /ar at 390 and 1440 RTL with Arabic strings. Zero console errors, zero 4xx/5xx (54 requests all 200). Screenshots: `docs/goals/overnight-launch/live-qa/390-*.png`, `1440-*.png`, `390-ar-*.png`, `1440-ar-*.png`; log `live-qa/LOG-A.md`. Rough edges forwarded to the fix slice: untranslated "Checkout is coming soon", slideshow aria label, reference lost after a Tier 2 edit, state poll continues after terminal.
- 11:40 Adversarial review 1 done: `docs/goals/overnight-launch/reviews/adversarial-review-1.md`; fix slice dispatched (implementer for B1, B2, M2, M3, M4, M7, m1, m2, m3, m10, m12; platform for M5, M6 policy values, M1 guard).

- 11:45 Live QA B done (model claude-opus-5): responsive sweep at 320x700, 768x1024, 1024x768 and 390x600 (Arabic أسماء, Framed minimal, Kufi, rose, Accent) all reached the honest "being prepared, we will send it to you" state with a captured reference (60473f3e, b77c96d6, 34f55123, 9e7fc5cf); never a missing-photo dead end, never a borrowed photo; Tier 2 clicks never changed the sample. Concurrent shoppers at 390x844 (English Noor and Arabic نور, confirms in the same second) got separate principals, designs, runs and references (91f70852, dc7f70d1) and cross-reads of the other design's `/api/state` returned zero rows; a third submission from one session created a new run (no cap message in mock). Zero console output, zero 4xx/5xx across ~500 requests. Defects forwarded to the fix slice: D1 confirm checkbox does not survive reload (medium, "Add to bag" disabled next to a captured reference), D2 "Design example" caption persists in the sticky bar after the shopper's own run, D3 review CTA label flips to "Preview my piece" after a re-edit, caption label half untranslated in Arabic, no Arabic web font served (system font). Note: the run is created inside `POST /api/revisions/approve`, not a separate run route. Screenshots `docs/goals/overnight-launch/live-qa/b-*.png` (74), log `live-qa/LOG-B.md`.
- 11:46 Cancelled the stray MOCKFAIL run `c9cae613-297f-4a1e-be64-d526d622af3d` (created 01:00 UTC with the pre-W3 spec shape, no metalKarat) whose four queued tasks the stale sweeper kept re-dispatching: every one of the 13 failed live `presentation-task` runs was this run ("Missing required prompt value: metal_karat"), 324 outbox rows. Tasks and run set to `cancelled` via psql over the session pooler. Root fix (prompt-compile failure is terminal, no re-dispatch) is in the implementer's fix slice. Local `inngest-cli dev` killed at 11:40 so local crons stop claiming live outbox work.

- 11:50 Platform fix slice done (model claude-opus-5): migration `20260907030000_spend_cap_fail_closed.sql` applied to `jggalwuvpcqoenhirmnl` (`supabase db push --linked` exit 0; `supabase migration list --linked` 21/21, last `20260907030000`; `pnpm db:types` no diff). It makes the spend trigger fail closed when the `runtime_policy` row is missing (was: any reservation accepted with no policy row, proved live inside a rolled-back transaction), stops the trigger refusing a reconcile that only records money already spent (was: a paid attempt whose actual cost exceeded its reservation at the ceiling was rolled back, "global daily spend guard exceeded"), and adds an open-attempt guard in `reserve_provider_attempt` ("provider attempt already open") so a duplicate concurrent execution of one dispatch cannot open a second paid attempt (M1 partial; the full execution-token design is written in the agent report and carried into the handoff). Regression check: new over-budget reservations still refuse (per-principal, global spend, global run count). `runtime_policy` now `daily_generation_limit=30`, `max_reserved_spend_cents=6000`, `global_max_reserved_spend_cents=6000`, `global_daily_generation_limit=100`; peak reservation per run is 400 cents (100 studio + 300 dependents) so the worst case is 15 stalled runs per UTC day before the global guard bites; recommendation for the morning: `studio_reservation_cents` 100 -> 20 once the first paid runs confirm the per-image cost. data tests 30/30, jobs 16/16, scan-secrets passed.

- 11:55 Real-mode decision, taken now rather than at 14:00 because none of its inputs can change before the wall clock ends: the customer-facing app stays on `PROVIDER_MODE=mock` with the honest degrade path (illustrated sample plus "Your personalized preview is being prepared. We will send it to you.", request captured for the operator). Reasons, each with evidence in this file or `IMAGE-LAB.md`: (a) the W4 gate ("two consecutive real dry runs on each script by 06:30") was missed on the clock alone, four hours were lost to two session rate limits; (b) no look has met the W2 launch gate (3/3 on both scripts, then 10/12 on holdout names), best is framed-minimal at 3/3 English, 2/3 Arabic, and no holdout name has been generated, so nothing proves an unseen customer name; (c) the production identity anchor has three defects the lab measured (Latin path renders in a fallback sans with unfused letters and hard-coded rings, Kufi silently renders Naskh, rings are welded onto every stencil unconditionally), and the code verifier in real mode is still the mock verifier with no geometry check, so a real run could show a shopper a wrong pendant with nothing in the pipeline to stop it. Style anchors were therefore not published and the OpenAI key was never called: OpenAI spend USD 0. The lab's prompt release and its measured pass rates go into the handoff as the starting point for the next slice, not as a shipped feature.
- 11:57 Gate (5) hygiene: `grep -rn "trigger.dev"` across the tree (excluding node_modules, .git, build caches and the untracked machine-local `.cursor/`) now matches only `docs/goals/overnight-launch-2026-09-08.md` (the goal text itself) and `w0/INNGEST.md` (which quotes the gate command); `.env` has 0 `TRIGGER` lines; `.env.example`, `.mcp.json`, `README.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `.claude/skills/goal/SKILL.md`, `FOUNDATION-MANIFEST.json`, `docs/REFERENCES.md`, `docs/FINAL-STACK.md`, `docs/MEDIA-CONCURRENCY.md` and the `inngest/client.ts` comment were updated to Inngest. `pnpm exec supabase migration list --linked` -> 21 migrations, every local matched by an equal remote, last `20260907030000`.

## In progress

- Fix slice from adversarial review 1 plus QA A/B rough edges (implementer a9a252fb, platform af9ca732).
- W2 image lab stages 4 and 5 (image-lab + two viewers), hard stop 13:00 IST.
- Then: gates on head, scan-secrets, commit, push, redeploy, smoke, final live QA at 390/1440, handoff, PR packet.

## Blocked

- 06:45 to 10:50 IST: second Claude session rate limit (HTTP 429, reset 10:50) killed W0b, W2, W3, W4 again. About four hours lost. All resumed at 10:52 with new deadlines: lab hard stop 13:00 IST, real-mode go/degrade decision 14:00 IST, deploy and live QA after, handoff by 16:30. Wall clock ends 16:49 IST.

- Morning items for Sanchay from W0a: `daily_generation_limit=6` per principal per UTC day (one shop tablet = one principal; raise before the visit), budget day rolls at 05:30 IST and test reservations (400 cents each) are not released, so check `principal_daily_usage` before the visit; anonymous auth rate limit 30/hour.

- 04:xx to 05:50 IST: all five subagents were killed by the Claude session rate limit (HTTP 429, reset 05:50 IST). Roughly 90 minutes of wall clock lost; every agent was resumed at 05:52 with its transcript intact.

- none yet

## Spend

- Runway credits: 640 of 40,000 used at stage 1 close (balance 306,222; start 306,862; floor 266,862). W2 stage 1: 32 images across two prompt releases, all 32 scored by an independent viewer. Stage 3 dependent views in flight. Ledger: `docs/goals/overnight-launch/ledger.jsonl`, results: `docs/goals/overnight-launch/IMAGE-LAB.md`.
- OpenAI USD: 0 (night cap USD 60)

## Next

- 06:10 W3 (design stage) and W4 UI (review stage, real pipeline, honest degrade) dispatched as implementer agents. W5 after W3 and W4.
