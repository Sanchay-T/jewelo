# Road to gold - progress

One entry per session, newest at the bottom.
Each entry: what was done with evidence paths, what is open, the next task id from `docs/TASKS.md`.
Update while working, not at the end; a session can die at any minute and this file is what survives.

## 2026-09-07 - session 1 (laptop, Opus 5)

Done:

- Synced the overnight run off `home-mini`: `1a0219e` pushed with 188 MB of lab and QA evidence; salvaged five branches that existed only on that machine (`backup/pre-reset-2026-09-03`, `codex/shopify-integration-ready`, `work/caleums-e2e`, `codex/gcp-production-deployment` plus the main branch).
- Found the six style anchors outside git at `~/.codex/state/jewelo/caleums-style-anchors/v1/` on `home-mini`; mirrored to `~/hq/projects/devonel/caleums-private/style-anchors-v1/` on the laptop; checksums match the manifest and the source task ids match `docs/CALEUMS-FINAL-E2E-CONTRACT.md`. Not committed: the repository is public.
- `pnpm lint` was red on `@jewelo/jobs` at `1a0219e` (unused binding in `presentation.test.ts`); fixed in `594d378`. Typecheck 13/13, test 16/16 tasks.
- Staging redeployed to `594d378`: DigitalOcean deployment `77c680bb-b6ee-46db-bcf8-dc8d774711bf`, "App updated", `/api/readiness` ready.
- Live audit of the shopper flow on staging at 1440x900: draft 201, approve 201, run `complete`, 5 tasks `ready` with mock assets, UI correctly degraded to contact capture. Layout defect measured: preview panel 339 to 1115 px in a 900 px viewport, bottom 86 px behind the fixed action bar at rest; at 1024x768 the panel needs 676 px against a 558 px budget.
- Wrote `docs/ROAD-TO-GOLD.md`, `docs/MINDSET.md`, `docs/GOAL-PROMPT.md` (3,390 characters), D-018 in `docs/DECISION-REGISTER.md`.
- Runway MCP confirmed from this session: workspace "Sanchay", `gpt-image-2` available, 305,042 credits.
- Local dev server runs from `.claude/launch.json` on port 3011 (3001 is held by an unrelated two-day-old `server.mjs`).

- Four read-only gap analyses (identity engine and verifier, prompt lab and anchors, infra and operator, plus my own localhost measurements) became `docs/TASKS.md`: 11 decisions for Sanchay, phases 0 to 7 plus launch, every task with files, change, proof, size, owner.
- Lab state corrected: the holdout trio (Noor, Layla, Muhammad) was already generated for v4.3, 5 of 6 scored passed, 11 images unscored; 91 ledger rows, not 73.
- Removed Playwright (6 configs, 5 specs, 2 scripts, the dependency) and the vendored `agent-browser` skill, `skills-lock.json`, the `browser-qa` agent. Tests and CI/CD suspended on Sanchay's instruction; `pnpm build` and the lead agent's own in-app browser are the gate. Recorded in `FINAL-STACK.md`, `VERIFICATION.md`, `ROAD-TO-GOLD.md`, `MINDSET.md`, `GOAL-PROMPT.md`.
- Rewrote `CLAUDE.md` (62 lines, no launch-time imports) per the Claude Code memory and best-practice docs, the HumanLayer CLAUDE.md guidance, and the Fable 5.1 prompting guide (autonomy block, scope is the deliverable, finish the turn, targeted edits, lead keeps working while subagents run). `AGENTS.md` is now a symlink to it so Codex reads the same file.
- Local git push is pinned to the `Sanchay-T` account in `.git/config`; the active `gh` account had switched to `sanchay-devstrum` and was getting 403.

Open:
- Product decisions still unanswered: which looks the shop sells if only framed minimal plus Classic prove out; wait-on-page versus send-it-to-you.
- Supabase Devonel org egress grace ends 29 September 2026: Sanchay's action.
- Repository `Sanchay-T/jewelo` is public: Sanchay's action.
- A Claude session is parked mid-task in a jewelo tmux pane on `home-mini`, out of usage credits.

Next: first open task in `docs/TASKS.md`.

## 2026-09-08 - session 2 (laptop, Fable 5.1 lead, Opus 5 subagents)

Scope: next open tasks in order, Phase 0 then Phase 1.

Done:

- Phase 0 closed. P0-2, P0-3, P0-7 in `ff5e1a8`; P0-4, P0-5 in `77c9dfa`; P0-6 and P1-1 in `e6b8f0f`. Each was implemented by an Opus 5 subagent and its proof rerun by the lead before commit (`do:check-env` clean, `report.py` and `finalise.py` exit 0 over 91 rows, P0-5 config and completion-path scripts, P1-1 `MATCH 16/16`).
- Ledger truth measured by the lead: 91 rows, 73 with a verdict; stage 2 has 12 holdout PNGs, 6 verdicts sit unmerged in `lab/stage2/verdicts-{en,ar}.jsonl` (5 pass, 1 tweak), 11 images unscored. Documents now say so.
- P1-1 ruler: `measureMask` (pure, `packages/identity/src/geometry.ts`) and `decodeMask` (sharp, `apps/jobs/src/decode-mask.ts`) reproduce `verify_stencil.py` exactly on 16 lab and 16 production stencils. The ruler already shows the production renderer lying: English names measure 4 to 7 components while `render-report.json` says 1 and `passed: true`.
- DS-3 default B taken for Phase 1; D-019 and the `caleums-identity-v4` release bump are part of P1-2.

- P1-2 in `a54b1d3`: `packages/identity/src/shaping.ts` shapes with harfbuzzjs 1.6.1 over the pinned font bytes resolved from `import.meta.url`; `fontSha256Measured` and `exactCharactersPreserved` are measured; a name with an uncovered codepoint now throws `identity_shaping_gate_failed`; engine release `caleums-identity-v4`, D-019. Lead reran the proofs: Kufi and Naskh gids differ for four names, Latin advances equal `hb-shape` at 0% drift. The rendered PNGs are still byte-identical across styles because the raster still goes through Pango `<text>`; P1-3 replaces that.
- P1-1 proof is durable: `corepack pnpm --filter @jewelo/jobs measure-stencils [dir] [manifest]` (tsx 4.23.13 dev dependency). Production stencils measure 4 to 7 components for every English name with no claim in `render-report.json`.
- plan-reviewer challenged P1-3 to P1-7 and found four blockers (test import breaks the build gate on export removal; solver has no PNG decoder; ring-free contradicts the frozen prompts; canvas and ink rule unspecified) and six majors (style-to-font map, pipeline release pin, silent identity failures, Next asset tracing, WASM deployability found last). Rows amended in `9f6f160`: P1-2b deploy probe and P1-5a prompt dependency added, rings default on with `IDENTITY_RINGLESS_CONSTRUCTIONS` behind config.

- P1-2b in `8838de5`, deployed to staging as `6a536690-41aa-4182-a051-486f864b11cc` (smoke passed). The lead called the route on the live URL: 401 without a session, 200 with one, harfbuzz 14.4.0, `wasmLoaded: true`, Playfair, Naskh and Kufi resolved from `.next/server/assets` with the same shas as the laptop. The probe found a live defect on its first run: `identityFontUrl` built the URL by string interpolation, which Turbopack cannot follow, so the deployed bundle returned Amiri for every requested face; fixed with a static per-file `import.meta.url` map and an unknown file now throws. No Next config change was needed; staging Node is 24.15.0 against the pinned 24.18.1.
- P1-3 in `2f51a44`: path-only SVG from the HarfBuzz outlines on the 1024 lab canvas, black on white, no alpha; one `IdentityRasterizer` and `solveIdentity` for both scripts; Kufi English on `cairo.ttf`; `<text>`, fontconfig and the bucketed Latin advance table deleted; tests excluded from the jobs tsconfig; `render-stencils` script. Lead opened the renders: real Playfair, Cairo, Naskh, Kufi; letters still overlap because `fuse()` moves islands and rings still land on glyphs (P1-4, P1-5).

- P1-4 in `af13e2b`: `fuse()` deleted; `bridgeAll` and `drawBar` ported with an exact distance transform and capsule bars; ink preservation asserted before recentre (`identity_bridge_moved_ink`); recentre matches Pillow LANCZOS and reports offset and scale; 216 cells (17 ZIP names plus Asma, both scripts, six live styles) are one piece with zero moved pixels, and the Python ruler agrees on all. Lead opened `muhammad-en-classic` and `asma-ar-classic`: letters where the font put them, bars on the baseline, rings still on the old rule. Behaviour change noted: every style now thickens two passes like the lab, not one.

- P1-5 and P1-5a in `c68fdc7`: `addRings` ported from `make_stencil.py` (11x11 erosion, corner anchors, weld fillet) plus one deliberate improvement, the ring lifts until its hole holds no name ink (the lab punches 7 to 55 pixels out of a hairline on 6 of its own 16 stencils; this engine punches 0 on 464 renders). 232/232 rings-on cells have exactly two ring holes, sizes within 1.7% of the lab's; rings-off gives 0 ring holes and one piece. `IDENTITY_RINGLESS_CONSTRUCTIONS` is validated config (normalises case, spaces, trailing comma; rejects non-hyphen ids) and is absent from the staging spec. Lead compared `asma-en-classic` against the committed lab stencil: same rings, same holes.
- Review 1 of the engine (P1-2 to P1-4) recorded in `docs/goals/road-to-gold/reviews/identity-engine-review-1.md`: the port's maths verified against the sources; four majors (ink assertion checked before rings, endpoint-only cluster coverage, ring literal 560, customer name in error-class columns) and ten minors, each with an owner.

- Review 1 fixes in `12087fa`, reproved by the lead: `identity_ring_punched_ink` fires on an injected block (642 pixels); character coverage is per codepoint with `setClusterLevel(2)` plus a reshape probe so ligature-swallowed indices (آلاء, عبدالله on rakkas) pass while a private-use or unsupported combining mark mid-name fails with the index; no customer text in any solver error message (four persistence paths in `presentation.ts` listed); `identity_fit_overflow` at 45 Latin characters while `Abdulrahman Nooralhuda` still fits; `ENVELOPE_BOUNDARY`; diagnostics `pathExists` from `existsSync`. New follow-up: the minimum font size now sets a hard name-length ceiling (about 40 Latin characters) and the customer form does not say so.

- P1-6 in `5fbbb07`: `IdentityValidationReport` widened to measured numbers and booleans plus `measuredBy`, `rule`, `bbox`, `holes`, `ringHoles` and the construction block; the solver encodes, then decodes its own PNG through the rasterizer port's `decodePng` and measures it with `measureMask`; `passed` is a conjunction of those measurements and disagreement throws by name. Lead reran: `MATCH 16/16` with exact ring counts (Asma has four holes, two of them letter counters, which the old `>=` check could never tell apart), and the tampered decodes throw `identity_ring_gate_failed:holes=1,expected=2` and `identity_component_gate_failed:components=2`. `PIPELINE_RELEASE_ID` config, default `caleums-final-media-v2`; migration `20260908120000_pipeline_release_v2.sql` written, functions `expand_final_media_run` and `request_video_task` read the active release instead of a literal; not yet pushed.

- P1-7 in `e5e598b`, staging deployment `ebdca539-24c8-422d-a659-369e2e21032f`: migration applied (`pipeline_releases`: v2 active with `caleums-identity-v4`, v1 legacy); 16 production stencils regenerated and identical under both rulers; two staging mock runs (`5917f2df` Rania en, `46030711` ريم ar) complete on v2 with `engine_release caleums-identity-v4`, the bucket stencils deep-equal their stored `validation_report`; an injected Devanagari name blocked as `identity_shaping_gate_failed:notdef=4,uncovered=4` with dependents unspent. Lead opened both staging stencils: Playfair and Naskh, one piece, two rings. `identity_artifacts` by engine: 19 latin-existing-v1, 15 arabic-v3, 2 identity-v4.
- Adversarial review 1 recorded in `docs/goals/road-to-gold/reviews/identity-engine-adversarial-1.md`: two highs (a ring can be welded onto a floating dot with every gate passing; pipeline release lineage not cross-checked), four mediums, six lows, each with an owner.

- Adversarial fixes in `6d7382b`, deployed to staging as `08d63e17` (health 200, readiness ready): ring placement lifts and shifts outward until the hole is clear and no name ink sits under the metal (`identity_ring_welded_to_glyph`, 0/232 cells after 130/232 before); per-task `identity_pipeline_release_mismatch` when the stamped release differs from the active row; migration `20260908130000_one_active_pipeline_release.sql` applied (partial unique index, one active release); `blockPreSpend` failure on a retry falls back to terminal `fail` instead of rejecting; negative-proof outputs in `dogfood-2026-09-08/identity-gates.md`.
- Lead dogfood of `08d63e17` in the in-app browser, recorded in `dogfood-2026-09-08/staging-journey.md`: Rania journey approves (`drafts 201`, `approve 201`), polling stops at a terminal state, the four tiles read "Being prepared" with the contact fallback, and every image on the page is an Asma example, so no mock asset is shown as the customer's piece. 390x844 has no horizontal overflow. P6-1 sticky numbers unchanged.

- Platform re-proof on `08d63e17` passed: Salma (en) and هدى (ar) mock runs complete on v2, both stencils downloaded from the bucket, sha equal to `png_sha256`, re-measured locally deep-equal to `validation_report.measured`, `glyphPixelsUnderRingMetal 0`; a second active pipeline release is refused with 409 `pipeline_releases_one_active`; diagnostics route 401/200 with harfbuzz 14.4.0 and the pinned font shas. `identity_artifacts` by engine: 19 latin-existing-v1, 15 arabic-v3, 5 identity-v4.
- Adversarial pass 2 recorded in `reviews/identity-engine-adversarial-2.md`: one high (the under-metal count exempts a 46 px disc around the anchor, and a dot survives the 11x11 erosion, so `noor-ar-kufi` and `noor-ar-classic` weld the ring onto the ن dot with every gate green; lead confirmed by eye), three mediums (canvas guard breaks the lift search for five left rings; P1-5 proof wording; `prompt_compile_failed` path lacks the retry fallback), four lows. F-3 decision taken by default: `aboveAnchor` is the standard, row reworded.

- Fix pass 3 in `b2ebea4^`: rings anchor only on 4-connected islands of the pre-bridge raster that are at least `IDENTITY_RING_ANCHOR_MIN_ISLAND_FRACTION` (0.1, measured: moved anchors sat on 2.4-9.9% islands, kept ones on 11.3-100%) of the largest island, so a dot, hamza or bridge bar can never carry a ring; the under-metal count exempts only the weld capsule and drawn fillet; the x-legality guard clamps instead of breaking so the five left-edge rings are searched; `blockPreSpendTerminally` covers `task_prompt_release_mismatch`, `prompt_compile_failed`, `prompt_snapshot_lineage_mismatch`; release compared before the render; `measure-stencils` prints a `CLAIM` cross-check of `measured` against `claimed`. 179/232 ring centres moved. Lead reran: `MATCH 16/16`, `CLAIM 16/16`, build exit 0, and opened `noor-ar-kufi` and `noor-ar-classic`: right ring on the ن stem and bowl, dot free.
- Phase 2 plan review recorded in `reviews/phase2-plan-review.md` and the rows rewritten in `b2ebea4` (P2-0 to P2-7, corpus recount 73/49/13 with 7 geometry defect rows, gates per view, mask threshold measured not assumed, `MOCK_STILL_FIXTURE_DIR` so the verifier runs on staging for zero spend, DS-11 ceiling enforced by the replay script). `ROAD-TO-GOLD.md` phase 2 gate now names the 79 verdict-bearing images.

- P2-0 committed: `packages/ai` tests excluded from the typecheck, `apps/jobs/scripts/**/*.mts` typechecked by the build gate (zero errors surfaced), `pnpm build --force` exit 0.
- P2-1 committed: `merge_verdicts.py` picked up seven unmerged verdicts (six stage-2 plus one stray stage-1 v4.3), ledger now 80 scored, 55 pass, 12 tweak, 13 fail, 11 unscored; `IMAGE-LAB.md` appendix regenerated (all-lab pass rate 54% to 60%); `apps/jobs/scripts/replay-lab.mts` resolves 91/91 stills and 128/128 references with matching sha256 and writes `lab/replay-report.json`; mock baseline: 55/55 passes accepted, 25/25 tagged rows and 13/13 fail rows falsely accepted. The `StudioVerifier` port still takes a stencil URL, so the script passes a `file://` URL and marks gates `projected` until P2-5 adds real gates. Lead reran the script: same census.

- `b2ebea4` deployed to staging as `2334f8a0-b300-4783-8e17-c6a9eeb7b831` (ACTIVE, smoke passed, readiness ready). Re-proof: Noor en run `c4ac9607` and نور ar run `18714ee5` complete on v2, eight tasks ready, zero spend; both stencils downloaded, sha equal to `png_sha256`, deep-equal to `validation_report.measured`, under-metal and punched 0. Lead opened the 3x crop of the Arabic right ring: on the ن stroke, dot separate. Note: this deployment predates fix pass 4, so the "Ali" defect is live on staging until the next deploy.

- Fix pass 4 committed in `0d5c5fb` (lead opened `Ali` and `أمير`: rings on letter strokes, dots and hamza free; build `--force` exit 0; `MATCH 16/16`, `CLAIM 16/16`). It keeps a raster carrier rule (compactness) and is superseded by the D-020 redesign now in flight.
- Security fixes deployed to staging as `2e58159e-1bcc-41fb-91b9-126f20bc8a06` (platform proved every probe: board 404, headers, readiness token, Inngest 401 unsigned, 422 on bad specifications, operator backoff). `READINESS_PROBE_TOKEN` generated and installed on both machines and the DO app; `smoke.sh` sends it. Lead verified the CSP on `/en/design/new` in the browser: no violations, fonts and images load, state API 200. Finding: `deploy.sh` does not reconcile envs from `env-contract.mjs` (only bootstrap does), so a new variable must be set on the app by hand; follow-up.
- Lead finding: `/` redirects to `/en`, an empty landing page since `e8d3dc0`; the atelier is at `/en/design/new`. Recorded in `reviews/ux-review-1.md` for P6-4.
- Fix pass 4 (implementer) on adversarial review 3 (`reviews/identity-engine-adversarial-3.md`): three highs. The 10% island rule still anchors a ring on the tittle of i ("Ali" 13.5%, "Niki" 10.5%, lead confirmed by eye); the under-metal count scans only the annulus and exempts 30.8% of it; five combinations (Maji kufi, أمير kufi, قق in three styles) now throw where `6d7382b` rendered. Plus deterministic RPC raises left to the sweeper, and a harness that reads the engine's own claims. P1-5 reopened; Phase 1 is not closed.
- Five rookie-mistake reviews dispatched on `dde9058` at Sanchay's request (identity package, jobs pipeline, security, tooling, customer journey). Three are back and recorded under `reviews/`: `tooling-review-1.md` (turbo build gate can pass without compiling, concurrency config dead, bootstrap destructive, harness exit codes), `pipeline-review-1.md` (two-name English can never pass the name gate, sweeper window 2 min against a 180 s provider timeout, model boolean bypasses the text comparison, midnight spend not recorded, unvalidated storage path), `security-review-1.md` (unauthenticated upload served on the app origin and a public internal board on staging, both confirmed by the lead; no specification schema; operator login without throttle; no security headers).
- All five reviews recorded (`reviews/identity-package-review-1.md`, `pipeline-review-1.md`, `security-review-1.md`, `tooling-review-1.md`, `ux-review-1.md`). Security fix 1 committed in `61df414` (board deleted, specification schema with NFC and script rules at three routes, operator backoff, anonymous and transliteration limits, headers, readiness token, magic-byte sniff, same-origin on operator commands; lead verified the web build and the 404s). Tooling fix 1 committed (turbo cache now hashes `.env` and restores `.next`; Inngest refuses unsigned; concurrency 2; bootstrap refuses; deploy.sh fixed; replay-lab exit codes; lab tools atomic). Note: the six sample-images deletions were staged by the security agent and swept into the earlier "UX review 1 recorded" commit `8c2b2bb`; intended, wrong message.
- P2-2 committed in `09de943`; lead opened the contact sheet at 1440x900 and the overlays: `classical` registration exact, `framed-minimal` meaningless without the frame. Decision by default: P2-2b (stencil carries the construction geometry) added; G6 per construction inside the dilated stencil bbox.

- Pipeline fix 1 committed (lead: build `--force` exit 0, `pnpm audit --prod` clean, four migrations applied to staging): two-name English and `O’Neill` pass the script test; `reading.matches` no longer sufficient; provider timeout, stale window (derived: timeout plus margin, 300 s over 180 s), poll and sweeper limits, signed URL expiry in `packages/config`; attempt budget in `runtime_policy.provider_attempt_budget`; usage rows keyed on the attempt date with upsert (midnight test rolled back on staging); `referenceAsset.id` validated and storage paths encoded; explicit-null patches; sweeper indexes; sharp 0.35.4 with overrides in `pnpm-workspace.yaml`. Bar-fallback routing skipped until D-020 lands `ringPlacement`.

- P3-1 committed: viewer scored the 11 unscored images (10 pass, 1 tweak: `framed-minimal-layla-en` a2 floats its letters inside the frame, second time for that cell); lead opened the three flagged images and accepted the ring-looped-around-the-bar attachment as a pass (chain through the ring, manufacturable). Ledger 91/91 scored: 65 pass, 13 tweak, 13 fail. v4.3 studio 3/3 on both scripts, holdout 10/12. `finalise.py` run with the real Runway balance from `whoami` (305,042), budget paragraph restored in `IMAGE-LAB.md`.

- P3-2 and P3-4 committed (lead verified: frozen hash compiles, unfrozen refuses with the hash named; planner plans, replans as a no-op, refuses a holdout leak, reserves and unreserves; ingest now skips planned rows). Review of the security and tooling commits recorded in `reviews/security-tooling-fix-review-1.md`: no blocker, four majors (transliteration limiter widened by the composite key, letterless names accepted, some real Latin names rejected, anonymous cap on a route the site never calls); security fix 2 in flight for all but the last, which is a Supabase dashboard setting under Needs Sanchay.

- Pipeline fix 1 deployed to staging as `2a1a8a7a-a4d9-40c6-8eb4-debb96298830` (ACTIVE, commit `30a9edc`, smoke passed with the readiness token). Re-proof: two-name English run `fbcf44fa` (`Sara & Omar`) and Arabic run `d85dbdc6` (`نور`) complete on v2, four tasks each ready, zero spend; the two-name stencil downloaded, sha equal, re-measured deep-equal (`decodeMask@sharp0.35.4`, one component, two ring holes); lead opened it: one piece bridged through the ampersand, rings on the S and the final r. `runtime_policy.provider_attempt_budget` 3, both sweeper indexes present, readiness `keyEnvironment prod`. Follow-up: a two-name string fits into the same pendant width, so letters are small against fixed-size rings; the P6-4 copy must say so.

- Security fix 2 committed in `3ee31c2` (lead: config, contracts and web builds exit 0; `packages/config` staged as the appended block only because pipeline fix 2 is editing the same file): source limiter before auth plus principal limiter after (request 21 from one address returns 429), name schemas demand a letter of the script and reject Arabic digits while accepting combining marks and the ʻokina (20-case table in the agent report), `TRUSTED_CLIENT_IP_HEADER` validated with default `do-connecting-ip`, `deploy.sh` merges the env contract into the live spec with `DEPLOY_DRY_RUN=1` printing key names only, readiness degrades to the public answer on a throw, one `operatorMockMode`. Not yet deployed; ships with pipeline fix 2.

- P1-5 (D-020) committed in `f221b44` (lead: opened `ali-en-classic`, `zoe-en-classic`, `noor-ar-kufi`, `amir-ar-kufi` from the agent's scratchpad, rings on letter strokes with tittle, diaeresis, nuqta and hamza whole; reran `measure-stencils` myself `MATCH 16/16 CLAIM 16/16`; `pnpm build --force` exit 0; no `ringAnchorCarrier` or raster anchor constant left in `packages/identity`, `apps/jobs`). Agent proof: 48 names x 2 scripts x 6 styles, 576/576 single piece, welded 0, punched 0, bar fallbacks 0, hole floor 576/576, Python oracle 16/16, adv3 corridor-mark probe no longer under metal. The 16 production PNGs changed bytes (six-place SVG precision, fillet start, new anchors). One thing for pass 4 to probe: in `أمير` Kufi the fillet from the lifted ring runs down beside the hamza. Follow-ups: `presentation.ts` does not read `construction.ringPlacement` yet (bar fallback routing); `identity-anchor.test.ts` imports a removed symbol (tests untouched by instruction); `IDENTITY_RING_CARRIER_MIN_RUN_HEIGHT_FRACTION` 0.34 is a preference, re-measure on any new face.

- Pipeline fix 2 committed in `f3f8775` (lead: `pnpm build --force` exit 0, diff read for the state route, studio and presentation gates). Letterless approved text now fails closed as `approved_text_has_no_letters` and is terminal at once; the reader's answer is NFKC-folded so Arabic presentation forms compare as letters, the approved side never; both vision calls abort at 60 s and the stale window is 420 s (180 + 2x60 + 120); `generation_tasks.reservation_usage_date` (migration 150000, backfilled 207 rows, midnight case rolled back on staging) so reconcile and the pre-spend and dependency releases hit the booking-date row; signed paths reject empty and dot segments; `terminal_error_code` is codes and counts only; `/api/state` projects `verification_result` through a five-field allowlist; `outbox_pending` dropped (150100). Follow-ups from the agent: the generic catch near `presentation.ts:705` and `blockPreSpend` can still store a PostgREST body in `terminal_error_code`; `reserve_provider_attempt` top-ups book on `current_date`; video tasks have no `reservation_usage_date`; `state/route.test.ts` asserts the old shape (tests untouched); the one-edit tolerance is P2-6.

- Bar-fallback routing committed in `acb2706` (lead: jobs and config builds exit 0, diff read): `IDENTITY_BAR_FALLBACK_REVIEW` (default on) read once in `productionPresentationDependencies`; a `ringPlacement: "bar"` construction is blocked pre-spend with `identity_bar_fallback` before the budget, the reservation and any provider call; harness proved bar-on blocked with zero provider calls, bar-off and welded proceed. `.claude/worktrees/` gitignored in `6b31fe7` (the harness creates it for isolated reviewers). Follow-up: at attempt > 0 the pre-spend RPC refuses and the code lands as `identity_bar_fallback|pre_spend_block_failed:<class>`, shared by every pre-spend gate.

- P3-3 committed (lead: opened `muhammad-en-kufi-norings.png`, one piece, eight islands bridged, no ring holes; manifest diff is 538 insertions and zero deletions): six Kufi rings-off holdout stencils from the lab renderer, `verify_stencil.py` components 1 on all six; 16 rings-off records manifested; `build_stencils.py` now additive, idempotent, refuses production-sourced files. Zero Runway credits (balance 305,042). Follow-up: no Python in the repo or on PATH runs the lab scripts (scipy missing); the agent built a scratch uv venv (3.12, numpy 2.5.3, scipy 1.18.1, pillow 12.3.0) that reproduces the published bytes; pin it in the lab.

- Fix-2 review recorded in `reviews/fix-2-review-1.md` (lead wrote the file from the reviewer's report; the reviewer definition cannot write): no blocker, six majors. M1 `terminal_error_code` still gets raw PostgREST bodies through `blockPreSpend` and `fail`; M2 the retry top-up books on `current_date` so a midnight-crossing retry permanently eats the daily reserved cap; M3 `deploy.sh` would push the laptop `.env` into production; M4 an empty `TRUSTED_CLIENT_IP_HEADER` is dropped by the contract so "trust no header" cannot ship; M5 `notes` is model prose in the shopper payload; M6 the 420 s window exceeds the 300 s `maxDuration`. Minors 8, 9, 12, 13 recorded as follow-ups: harakat and modifier-only Latin names accept then shape to notdef (fail closed, worse message); the atelier client accepts any `\p{L}` for English; the source limiter before auth is per NAT address (no caller today); policy change between reservation and expansion leaves a swallowed difference.

- Deploy `f617a568-73d2-4637-ac9b-75744796e48b` ACTIVE (branch tip `503e252`, code `f3f8775`; smoke passed; dry run listed 14 contract keys, `TRUSTED_CLIENT_IP_HEADER`, `INNGEST_BASE_URL`, `INNGEST_CRON_ENABLED` absent from home-mini `.env` so left on defaults). Platform re-proof: `Ali` run `5d873850` and `أمير` run `0975a48c` complete, four tasks each ready, zero spend; `verification_result` reaches the browser with `passed, exactText, identityScore, notes` only while the row holds ten keys; both stencils downloaded, sha equal to `png_sha256`, `measure-stencils` on the two files `MATCH 2/2 CLAIM 2/2`, `ringPlacement welded`; readiness 200 with forged cookies; transliterate 429 on the 21st call with 21 fresh principals; `'''` and `محمد٠١` 422, `O’Neill` 201; `reservation_usage_date` and `provider_attempt_budget 3` confirmed by REST. Lead browser pass recorded in `dogfood-2026-09-08/staging-journey.md`: fresh `Ali` design through the atelier, 201/201, tiles to "Being prepared" then the honest-degrade contact card, no console errors, no overflow at 390. The agent's "ring on the dot of the i" note is the tittle bridged to the stem (P1 bridging), byte-identical to the render the lead already inspected.

- Adversarial pass 4 recorded in `reviews/identity-engine-adversarial-4.md` (lead confirmed by eye: `علي` classic has both rings side by side on the lam; `آية` classic and `آلاء` kufi have the madda fused into the ring fillet). Two blockers: the fillet exemption covers the whole capsule so marks under it are swallowed and both scanners are blind by identical construction (five corpus names in three styles); the run-height floor lets a mark disqualify every base letter so both rings land on one glyph (18 of 576 cells, no separation gate). Majors: bar fallback unreachable and unproved (routing half closed by `acb2706`); GDEF rule inert on cairo, Scheherazade, Rakkas; bare `measure-stencils` greens the stale `stencils/lab` directory. P1-5 stays at 0.5. Fix pass 5 dispatched.

- Fix 3 committed in `d6ca41b` (lead: `pnpm build --force` exit 0, read the inngest route and deploy.sh diffs): `errorClass()` at every `terminal_error_code`, `p_reason`, `p_error_class` write with the full message in an audit event; migration 160000 books the retry top-up on `reservation_usage_date` and stamps video tasks (applied; midnight retry reproduced against the old body, fixed, rolled back); `deploy.sh` refuses an env file whose `JEWELO_DEPLOY_TARGET` is missing or differs from the environment (exit 2), `DEPLOY_ENV_SYNC=0` ships the branch only; an explicitly empty `TRUSTED_CLIENT_IP_HEADER` ships as GENERAL; `notes` out of the state allowlist; `executorRequestCapSeconds` 360 derived in config, route `maxDuration 360` asserted at import, stale window 480 s; comparison folds both sides, customer signer rejects dot segments, `signedUrlRefreshAfterMs` published from config, trusted-header parse throws a named error. `JEWELO_DEPLOY_TARGET=staging` appended to the laptop and home-mini `.env` (lead did home-mini over ssh). Follow-ups: `video.ts:160,168` writes fal's raw error into the customer-visible column (video is off); `webGuardEnvSchema` exported and parsed nowhere.

- Fix-3 review recorded in `reviews/fix-3-review-1.md` (lead wrote the file from the report): no blocker, four majors. M1 not closed on the video path (`video.ts:160,168` still write fal's raw error); the new audit write sits before the block RPC and its failure strands the run-start reservation because `fail` at attempt 0 finds no attempt row to reconcile; booking the retry top-up on the historical `reservation_usage_date` lets a bulk retry of yesterday's tasks spend past today's cap (the old bug leaked the safe way, this one the paying way); `maxDuration` is not enforced by App Platform's standalone `next start`, so M6 is a consistency fix, not a proof, and the real edge timeout is unmeasured (DigitalOcean's limits page names none for services; traffic goes through Cloudflare). Safety note: the reviewer's `psql` probe echoed the pooler URL into its own transcript; nothing reached the repository, and that transcript is never to be pasted into the handover or the PR.

- Fix 4 committed in `734162c` (lead: `pnpm build --force` exit 0, jobs lint 0, read `error-class.ts` and the readiness diff; lead's own edit: a raw message keeps only its first four words in `errorClass`, because the 60-character squashed head still carried a short name, probed with nine messages). `errorClass` shared and applied on the video path with fal's sentence in an audit event; block RPC before the audit detail; `reconcile_provider_attempt` releases the run-start reservation for a terminal fail at attempt 0 (migration 170000); `provider_attempts.usage_date` books and guards on the day of spend and carries an earlier reservation forward (bulk retry, midnight retry, attempt-0 top-up proved and rolled back; cap guard refused at 5950 of 6000 plus a 100 carry); refresh window published with a floor; readiness reports `trustedClientIpHeader valid|invalid`; bootstrap checks the deploy target; one unsignable asset dropped, not the response. Follow-ups: `cancel_generation_task` releases from `current_date` (safe direction); operator commands route writes operator free text into `terminal_error_code` sliced to 120; `recover_stale_generation_tasks` ambiguous-charge branch still derives the date from `created_at`; `packages/config/src/index.test.ts` may need the new field (tests untouched).

- P1-5 fix pass 5 committed in `2ad683c` (lead: opened `aya-ar-classic` (bar rail, madda whole and gripped by the rail), `ali-ar-classic` (rings on the ya and the ain at opposite ends), `alaa-ar-kufi`, `amir-ar-minimal`, `ali-en-classic`, `zoe-en-classic`; bare `measure-stencils` `MATCH 16/16 CLAIM 16/16`; `pnpm build --force` exit 0; identity lint 0). Engine: the fillet is exempt only off the plane of other contours' ink, in both the seat search and the gate; carriers chosen jointly and never the same glyph while another exists; `identity_ring_span_too_narrow` (min 0.307 over 576, was 0.091); bar fallback reachable on 20 of 576 cells (aya in the three Naskh styles, qq, Bartholomewsonlongest, six thuluth cells), routed to operator review by `acb2706`; pinholes filled and gated; seat search capped and reported; GDEF claim made honest per face; `measure-stencils` defaults to production. Agent matrix: FILLET-FOREIGN 0/576, PINHOLES 0/576, WELDED 0/576, BAR 20/576, Python oracle 16/16. Also regenerated the four production-sourced stencils at `lab/stencils/*-ar-classic.png` with their manifest rows (they had described a Pango render). Follow-ups: `taim-ar-thuluth-inspired` passes at 0.307 but both ring tops sit near the middle so the piece hangs tilted (a "hangs level" criterion is a product call); worst seat search about 20 s CPU on `أمير` kufi; `identity-anchor.test.ts` still imports a removed symbol.

- Edge timeout measured (platform, probe deployment `8b8aa87a`, restore `d87b715c` at `3916d2b`, smoke passed, probe branch and route gone from every machine): Cloudflare fronts the app and cuts a silent request at 600 s with HTTP 524 (400 s answered, 800 and 900 s cut at 600); a response writing a byte every 10 s survived 900 s, so it is an idle timer; the app spec has no timeout field. The 360 s executor cap is inside the bound, fix-3 review major 4 closed by measurement; recorded by the lead in `docs/DIGITALOCEAN-DEPLOYMENT.md` "Request timeout at the edge".

- P6-1 committed in `4c08b35` (CSS only; agent proof: web build and lint exit 0, compiled CSS read back; the lead has not yet measured it in the browser). `--action-bar-block 86px` subtracted from the sticky panel budget and added to `.main` padding; panel scrolls inside its own box; a max-height 820 block compacts the dock; mobile photo is 4:5 contain (B2); dead `data-fit` rules removed; RTL arrows mirrored; reduced-motion spinner fades. Follow-ups from the agent: three contradictory unconditional `.photo > img` object-position rules; a 9:16 dark still now letterboxes on cream and a `[data-carousel-view="Dark"]` background would need a visual decision; `.previousPhoto` keeps cover on mobile; `.app` padding-bottom 100 duplicates the token.

- P6-1 measured by the lead on the local app (`preview_start web`, port 3011): `previewSticky` bottom against `actionBar` top after scrolling to the bottom is a 214 px gap at 1440x900 (sticky 690 budget, dock 396), 1280x720 (budget 510, dock 313, tile bottoms 302 against bar 634) and 1024x768 (budget 558, dock 339, tiles 337 against 682); `scrollHeight == clientHeight` so nothing scrolls inside the panel; no console errors. Not yet looked at: the dark still at 390 (needs a run with a 9:16 asset), RTL arrows, reduced motion; those ride with the next staging pass. Observation: 300 px of blank tail below the last content at the page bottom (main 126 plus app 100 plus the token), pre-existing and larger by the 86 px token now.

- P6-4 committed (lead verified on the local app at 1440x900: the former crash path, two names with one blank then the 02 Review chip, leaves the checkbox disabled and described by the alert "Enter a name." with an "Edit the name" button, page alive, no console error; 30 characters shows "This is the longest name we can make: 30 characters." with `maxLength` 30; `ليلى` in the English journey is refused with the Latin-letters message; `pnpm build --force` exit 0). `error.tsx` boundary for the design segment; B3 unavailable cameras honest with no Retry; `NAME_FIT_MAX_LATIN_CHARACTERS` 37 measured against Playfair; the atelier validates with the contracts rules (zod now in the client bundle). Follow-ups from the agent: empty `/en` landing (P6-4 in the review, not in this brief); `specification()` does not collapse whitespace and changing it would invalidate saved bag signatures; field notices appear on submit while the checkbox guard is live; AR dictionary keys overlap with P6-3's B4.

- Adversarial pass 5 recorded in `reviews/identity-engine-adversarial-5.md` (lead wrote the file from the report): two blockers, three majors. The rings are not level on 20% of the matrix (`لي` minimal 64.7 degrees, every gate green) because fix 5 measured separation and the harm is level; `عائشة` classic puts both rings in the right third (span 0.291, 66% cantilevered) because the foreign-ink rule refuses every seat over the ta-marbuta's dots and the search walks inward. The rail grips a diacritic and floats over the rest (`قق` hangs from two nuqta), no gate scans under it; the bar path is a terminal pre-spend dead end for about 7.5% of common Arabic names and every single-letter pendant (product decision for Sanchay); the harness re-derives the engine's rule from the engine's constants and reported glyph box. Confirmed closed: pass 4 blocker 1 on the welded path, the measure-stencils default, the lift cap, dots as carriers structurally impossible, determinism and fingerprint. P1-5 stays at 0.5. Fix pass 6 dispatched.

- P6-3 committed (lead verified on the local app in EN and AR: "Review my piece" / "مراجعة قطعتي" opens step 02 with no API call; the development fixture run lands on `data-own-outcome unavailable` with the honest rows, headline, bar caption and, after the lead's own edit, the photo caption; "12" as a phone shows the international-format sentence inline before any request; web build and lint exit 0). Lead's two edits: the fixture gate is the development build, not the data mode, because the laptop `.env` is the remote mode; the photo caption said "Being prepared" in the terminal state. `personalizedRun.test.ts` reverted to HEAD (Vitest files stay untouched; the agent had changed four call sites for typecheck, which is not the gate). Stray: the lead's first attempt ran through the real pipeline against the staging database and left a queued design (`3ca3b157`) with no worker; harmless mock-mode noise. Follow-ups: `personalizedRun.test.ts` asserts the old `preparing` status and the old degraded resume shape (will fail when Vitest returns); the dev-only controls panel is English-only; empty `/en` landing still open.

- Deploy `d4bb713f-2b82-4d06-930c-6fd1226c2727` ACTIVE (branch tip `4c08b35`: fix 3, fix 4, fix pass 5, P6-1; smoke passed; 14 contract keys synced, `IDENTITY_BAR_FALLBACK_REVIEW` unset so default on). Re-proof: `آية` classic run `e402564f` lands in `operator_review`, studio task `blocked` with `terminal_error_code identity_bar_fallback`, zero `provider_attempts` rows, artifact `ringPlacement bar`; `علي` run `623614a3` and `Ali` run `eaa03eb8` complete, four assets each, rings on glyphs 1 and 3 / 0 and 2; `/api/state` verification keys `exactText, identityScore, passed` only, `signedUrlRefreshAfterMs 240000`, `signedUrlRefreshFloorMs 30000`; readiness with the token reports `trustedClientIpHeader valid`, without it and with a forged cookie 200 `{"status":"ready"}`; `علي` stencil downloaded, sha equal, `measure-stencils` `MATCH 1/1 CLAIM 1/1`; `provider_attempts.usage_date` exposed. Recipe note for the next drive: an Arabic name needs `arabicStyle: "contemporary"` (the classic alias), `"none"` makes the approve RPC read the English field and 422. Follow-up: `designs.status` stays `generating` after the run completes or goes to review (pre-existing).

- Deploy `846fb7fc-3ad4-4c0b-be87-135df54ba5c4` ACTIVE (tip `f2f0ea2`: P6-3, P6-4 on top of `4c08b35`; smoke passed; 14 keys synced). Curls: `/en/design/new` carries "Review my piece" and no "Preview my piece"; `/ar/design/new` carries "مراجعة قطعتي"; `/api/state` without a session answers 401, not 500.

- P1-5 fix pass 6 committed (lead: opened `li-ar-minimal` (was 64.7 degrees, now level), `aisha-ar-classic` (a ring at each end), `aya-ar-classic` and `qq-ar-classic` (former rail pieces, now welded with the marks whole), `ali-en-classic`, `zoe-en-classic`, `thirty-e-kufi` (every counter open); bare `measure-stencils` `MATCH 16/16 CLAIM 16/16`; `pnpm build --force` exit 0; identity lint 0). Joint pair choice with tilt and overhang gates; the rail deleted, `ringPlacement` is `welded | none`, no seat throws `identity_no_ring_seat`; 8 of 576 refuse, none a real name in a live style; harness independent with a zero delta. Two lead observations for pass 6: the weld stems are now long posts (the A in `Ali` carries one about a third of the canvas tall; `لي` has two) and most pieces downscaled to 0.894 to make room for lifted rings, so the name is smaller on the same pendant; both are look questions, not gate failures. Follow-ups: `presentation.ts:488` dead `bar` branch and the flag (cleanup dispatched); the four production-sourced stencils at `lab/stencils` are stale copies of pass 5; seat search mean 49k steps, max 490k, about 45 minutes for a full matrix; the harness pre-ring plane is still resampled where the ringless render downscales.
- Bar routing cleanup committed in `5b3f065` (lead read the diff and opened `asma-ar-classic.png`: one piece, two welded rings, no rail; agent proof: `pnpm build --force` exit 0, jobs, web and config lint exit 0; scratch harness `bar-cleanup/no-seat-gate.mts` drives the real `executePresentationTask` with an identity render that throws `identity_no_ring_seat`: status `operator_review`, blocked pre-spend, zero provider calls, no reservation, `terminal_error_code identity_no_ring_seat` through the real `errorClass`). The dead `ringPlacement === "bar"` gate, `barFallbackReviewEnabled`, the `ringPlacement` pass-through, `identityBarFallbackReview` in `@jewelo/config`, `IDENTITY_BAR_FALLBACK_REVIEW` in the env contract (now 13 keys) and the runbook paragraph are gone; D-020 records the removal. The four production-sourced lab stencils (`asma`, `layla`, `muhammad`, `noor` ar-classic) regenerated from the pass 6 engine, all `welded`, one component, two rings; only their manifest rows moved. Lead observation on `asma-ar-classic`: the left ring hangs from a post taller than the hamza it stands on, the long-post look again, for adversarial pass 6. Follow-ups: `manifest.json` `productionGaps.kufi` and `productionGaps.latin` prose is stale (kufi renders from Noto Kufi with its own sha; Latin rings are welded, not fixed at x=255/945); next env sync drops `IDENTITY_BAR_FALLBACK_REVIEW` from the app spec if it was ever set (it was left unset). TASKS.md rows P3-3, P6-1, P6-3, P6-4 marked done in `dfebd7a`.
- Deploy `b1ea4b6d-300b-4924-b427-0937b54dc8de` ACTIVE (tip `65a93ad`, code `5b3f065`: fix pass 6 plus the bar cleanup; smoke exit 0; 14 keys synced, `IDENTITY_BAR_FALLBACK_REVIEW` never in the env file so the count did not move). Re-proof by the platform agent, lead opened the downloaded `آية` stencil: `آية` classic run `989ca554` now completes welded (on `d4bb713f` it went to review with `identity_bar_fallback`), tilt 0, overhang 0.050, rings on glyphs 1 and 5, one piece; `علي` `0f4e18a4` and `Ali` `9a3be903` complete, tilt 0; `Bartholomewsonlongest` `33424925` lands in `operator_review` with `identity_no_ring_seat:left=6,right=6,steps=420888` on both the run reason and the studio task, zero `provider_attempts` rows; bucket stencil sha equal to the artifact row, `measure-stencils` `SINGLE-PIECE 1/1 MATCH 1/1 CLAIM 1/1`; `/api/state` without a session 401; readiness with the token `trustedClientIpHeader valid`. Stale notes corrected: `deploy.sh` did print `deployment_id` over ssh this time; mock runs write four `provider_attempts` rows at `actual_cost_cents 0`. Look note for pass 6: the `آية` left ring hangs from a post about 300 px tall off the ta marbuta, the long-post look again. Section "Deploy `b1ea4b6d`" appended to `staging-journey.md`.
- P6-2 committed (lead verified on the local app: EN at 1440 shows eight tiles marked `data-not-photographed` with the aria-label "- Not yet photographed", zero elements carry both `data-not-photographed` and `data-sample-coming`; selecting Origami ribbon leaves only the three constructions marked plus one `<p data-not-photographed="selected">` with the full sentence, the lettering tiles go quiet; AR marks the three constructions in Arabic and no lettering; the dock note under the sample photo reads the ownership sentence in both locales and the caption adds "Sample look, not your piece" / "قطعة نموذجية، ليست قطعتك"; 390 has no horizontal overflow; no console errors; agent proof web build and lint exit 0). The design stage imports `preflightRefusal` and compares to the field's own reason with a single name, so a two-name refusal or another field's refusal never marks this field. Follow-ups: the photo caption is 7 px at 390 (the dock line carries the message); `Atelier.tsx:1051` disabled option still hardcodes "Unavailable" / "جار التحضير" outside the dictionary (P6-5).
- P6-6 committed (lead read the diff and re-ran the agent's harness `p6-6/harness.ts` against old and new code: the only differences in 18 rows are `task=banana` and `run=banana`, both now `outcome=unavailable settled=true polling=false`; every known status unchanged; agent proof web build and lint exit 0). Unknown task status maps to `unknown` and strands the three dependent views; unknown run status counts as stopped; no new copy, the P6-3 `unavailable` state and contact card carry it. Not reachable from staging without an injected status, so the harness is the proof. Follow-ups: the harness row `run=complete` with tasks still `queued` polls the full window (a run should not be `complete` with queued tasks; the existing `designs.status` follow-up is the same family); on mock staging every `ready` view reads `unavailable` by design, worth a distinct operator signal.
- P6-5 committed (lead verified on the local app: `/ar/design/new` at 1440 computes `IBM Plex Sans Arabic` on the app and the h1, a DOM scan of leaf text finds no Latin run of three letters outside CALEUMS and Asma, the footer reads ◇ ذهب عيار ١٨ / ✧ قطعة شخصية بتصميمك / CALEUMS · دبي, 390 has no overflow, no console errors; `/en/design/new` keeps Instrument Sans and Playfair Display and the `18K yellow gold` strings; agent proof web build and lint exit 0, three Arabic-range woff2 under `.next/static/media`, no `fonts.gstatic.com` reference in the build). IBM Plex Sans Arabic chosen over Noto Naskh for its flat contrast beside Instrument Sans; reasoning in the layout comment. Follow-ups: in the dev server the English page's CSS also references the three Arabic woff2 files (cache hits, `initiatorType css`), check on the production build on staging that `/en` fetches none; `usePhotographicPiece.ts:71` builds an English alt for the fading previous photo; `Atelier.tsx` "About this example" Arabic branch ignores the `noSample` variant; the dev-only controls panel is English-only.
- Adversarial pass 6 recorded in `reviews/identity-engine-adversarial-6.md` (`91e24dc`): one blocker (the ring sits on a post welded at the foot of the letter; stems p50 203 px, 219 of 1136 at the 346 px cap; `Sara` en classic reads `iSarai`, lead confirmed by eye), three majors (11 of 52 single Latin letters refuse in both live styles; `كوثر` minimal refuses at tilt 15.5; names drawn about 7% smaller, 518/568 downscaled to 0.894 by rings pushed to the canvas edge), seven minors. Pass 5 blockers closed by independent recomputation. P1-5 stays open; fix pass 7 brief: post length scored above overhang and gated, anchor at the top of the stroke per D-020, single Latin letters and the 90 extra names join the matrix, thresholds move to config. Dispatch waits for P2-2b to release the engine files.
- P7-1 committed (lead drove `/en/operator` on the local app through a locally minted session cookie: 13 requests listed, "Show photographs" on the Zara sample request renders four signed stills each tagged as a mock sample, the note "lead dogfood note 9 sep" saved through the command route and read back after reload, "Made and handed over" moved it to Made with the three action buttons disabled and the count from 11 to 10 awaiting, "Pieces stopped for a look" lists 10 `operator_review` runs with `identity_no_ring_seat` in a jeweller's words and the raw code under it; 390 no overflow; `/ar/operator` RTL renders). Lead edits: `{" "}` before `mode` (rendered `mockmode`), `dir="ltr"` on the date and contact lines (the `+` landed on the wrong side in RTL). Agent proof: web build and lint exit 0. Follow-ups: the queue's own strings are English on `/ar/operator` (operator-only); the operator tab is still labelled "Prompt Library"; runs ending `partial` with a reason are not surfaced; `OperatorExperience.tsx` dead quotes/orders surface is P7-2.
- P6-7 committed (lead drove the local app at 1024x768: `Rania` typed, reload keeps it under scope `atelier.v1.en`; bag opened, "New piece" pressed: name empty, `localStorage` holds only `caleums.atelier.v1` (fresh) and `jewelo:anonymous-session:v1` (kept), sample panel with its ownership note shown, no auth request in the network log; `/ar/design/new` shows an empty name and the button `قطعة جديدة`; no console errors; agent proof full build and web lint exit 0). Not driven: the with-bag confirmation (needs a run that lands a piece in the bag); the with-bag path is code-reviewed only. Follow-ups: `snapshotStore.ts` IndexedDB blobs are orphaned, not deleted, on reset (sample photographs today, add `clearSnapshots()`); a mid-design language switch now resets the working draft (bag survives) by design of the locale scope; the first load after this ships drops any unscoped draft once.
- P7-2 committed (lead checked `/en/operator` on the local app: nav is "Work queue" and "Photograph recipes", the only internal links are `/en`, `/en/operator` and `/en/operator?tab=prompts`, no stats strip, no quotes/orders tabs, no "Customer view"; no console errors; agent proof full build and web lint exit 0, greps for `Customer view`, `/commerce/`, `updateFulfillment`, `Checkout unavailable` return nothing in `*.tsx`). Follow-ups: `OperatorExperience.module.css` (555 lines) is imported by nothing; `globals.css` keeps the unused `clm-queue*` and `clm-operator-*` rules; the commerce chain (`requestQuote`, `issueQuote`, `acceptQuote`, `createOrder`, `updateFulfillment`) still exists on the port in contracts, `legacy-direction-compat.ts` and `mock-client.ts`, a one-pass removal with `supabase-jewelo-client.test.ts` when Vitest returns.
- P7-3 committed (lead read the emit site and the function head; agent proof: `db:push` and `db:types` exit 0, migration `20260909000000_preview_request_notified_at.sql` read back on staging (`notified_at timestamptz`, index `preview_requests_unnotified`), full build exit 0, web, jobs, ai, config, data lint exit 0; scratch harness against the staging database: an injected Arabic request produced one `notification_logged` with name, language, construction, lettering, gold, stones, width, chain, contact and the queue link, the second run of the same event `already_notified`, the injected row deleted afterwards, the 13 existing requests left `notified_at null`). DS-8 default: Supabase SMTP is not configured (`smtp_host` null via the Management API), so the shipped transport is `log`; the SMTP sender has never spoken to a real server. Not deployed yet; the nine keys are optional and absent. Needs Sanchay: `NOTIFICATION_TO` (the shop's inbox) and a sending account (Resend or Brevo free relay, or the Zoho account with an app password); then five keys in `.env`, `do:check-env`, deploy. Follow-up: a crash between the claim and the send leaves one request claimed and unannounced (still visible in the queue).
- P4-1 committed (lead checked `scripts/style-anchors/` holds only `publish.mjs` and `README.md`, `.gitignore` guards added, no PNG or manifest in the tree; agent proof: `node --check` exit 0, run 1 six `published` v2 with re-download sha equal, runs 2 and 3 six `unchanged`, SQL readback six `published` v2 rows under `operator:style-anchors-publish`, 12 releases and 12 publication events total, 6 `style_anchor.release_created` audit rows). Release ids for P4-2: worn `8dab23b7`, packshot `56e8b727`, macro_gift `a341eda1`, dark_editorial `89d37442`, studio_hero `6b508f42`, billboard `f1180969`. Notes: the pooler role has an empty `search_path`, schema-qualify `public.` in readbacks; each full run pulls about 22 MB of egress; if the Supabase project moves (DS-1) the anchors are republished with one run.
- P4-2 proved with no code change (platform agent harness `p4-2/anchor-proof.mts`, exit 0): `SupabasePresentationRepository(url, key, false).signedStyleAnchorUrl` returns `data:image/png;base64,...` for `on_skin`, `studio`, `close_up`, `dark` (decoded 547 KB, 225 KB, 296 KB, 584 KB; portrait anchors bound to 1024x1807 by the repository's own `fit: inside`), the `missing` v1 pointer throws `style_anchor_missing:ee78f9a4-...`, a null pointer throws before any request; SQL readback six `published` v2 rows; `parseJobsEnv` with `PROVIDER_MODE=real` parses the laptop `.env` with nothing missing, so P5-2 needs only the DigitalOcean app env. Egress measured 16.18 MB for one four-view proof: no anchor cache in `signedStyleAnchorUrl`, about 4 MB per task attempt; follow-up (P7-4 or a cache) given DS-1. Tripwire from the concurrent P7-5 work: `@sentry/cli` and `core-js` need real booleans in `pnpm-workspace.yaml` `allowBuilds`, or `corepack pnpm build` fails at the pre-run install; P7-5 must land that.
- P7-7 committed (lead checked `scripts/backups/` contents and that no script echoes a URL or password; agent proof on home-mini: manual dump exit 0 in 2 s, launchd kickstart exit 0 in 15 s, two dumps of 469,676 bytes with sha256 sidecars in `~/backups/jewelo/` (dir 700, dumps 600), restore drill exit 0 on local Postgres 17.10 and on a `postgres:17` container, row counts equal live, `readback.sql` mirror of `/api/state` returns the five views for design `0f08ba63`, `launchctl list` shows `com.jewelo.backup` loaded, no scratch DB or container left, shellcheck clean). Decisions: `public` only (auth holds hashes and 94 anonymous principals; the drill stubs `auth.users` so 17 FKs validate), storage objects not covered (stencils regenerate free, stills cost a re-run). Not run: the HTTP form of the proof. Needs Sanchay: DS-1 Pro would replace this with managed backups; off-machine copy destination if wanted. Build tripwire from P7-5 in flight: `packages/observability/src/client.ts` pulled `@jewelo/config` (`process.loadEnvFile`) into the browser bundle; P7-5 must fix before it lands.
- P5-1 done (platform agent: caps set to 800 / 2 / 2 on staging `runtime_policy`, previous values 6000 / 100 / 30 recorded; proof at zero spend: `Nadia` `e2c8f600` and `Omar` `45892bcc` complete, `Sara` refused 409 `daily generation quota exceeded` with no run row, fresh principals refused 429 `spend_guard`; rolled-back synthetic-date test raised the global limit on the third run and the spend guard past 800). Lead call: caps restored to 30 / 6000 / 100 right after (read back), because staging is mock for days and 2 runs a day blocks dogfooding, Umayr and P7-6; the cap UPDATE becomes P5-2's first step. DS-6 finding: studio-only no longer happens by itself since P4-1 published the anchors; `expand_final_media_run` (`20260908150000_reservation_usage_date.sql:170-224`) fans out four tasks; the smallest honest change is a `runtime_policy.studio_only` flag inserting the dependents `cancelled`, about 60 SQL lines; dispatched as part of P5-2 prep. Finding: `supabase-rest.ts:108` maps the per-principal refusal to 409 `state_conflict` while the global one is 429 `spend_guard` (shopper copy unaffected). Note: the 2026-09-08 UTC day carries 24 mock run starts.
- P7-6 done (platform agent; docs only: `staging-journey.md` "P7-6 durability drill" and `w0/INNGEST.md` corrected). Two `inngest` restarts on staging (deployments `489c7b72` calibration, `7fbb8f4c` drill, both ACTIVE, smoke exit 0 after). P5-1's run `45892bcc` was in flight across the drill restart and completed normally; the new container logged `run not found in state store` five times, so the in-process loss is observed and harmless to the customer. The `operator_review` path (stale reserved paid attempt) is 8 to 10 minutes by arithmetic (`executorRequestCapSeconds` 360 s plus `staleRecoveryMarginMs` 120 s, sweeper every 2 minutes) and was proved by inserting a synthetic 15-minute-old reserved attempt and calling the deployed function under `ROLLBACK`: task `blocked` `operator_review_ambiguous_paid_request`, run `operator_review` `stale_worker_ambiguous_paid_request`, attempt `ambiguous` at `actual_cost_cents 100`, audit `task.stale_paid_request_blocked`, zero outbox rows. The agent's own live run was refused 429 by the P5-1 cap (since restored). Needs Sanchay: DS-10, stay self-hosted (8 to 10 minutes plus one operator click) or an Inngest Cloud account (interrupted runs resume).
- P7-5 committed (lead re-read `client.ts`: the Sentry initialiser imports `@sentry/nextjs`, the automated security note that caught an intermediate state importing `posthog-js` there is resolved; local `/en/design/new` loads with no console error and no third-party host; agent proof: full build exit 0, web, observability and config lint exit 0, `pnpm install` exit 0, `do:check-env` exit 0, deploy dry run exit 0 listing the five merged alerts, production `next start` with empty keys shows no outbound socket under `lsof` and the served chunks carry no Sentry or PostHog SDK code, merged-spec proof script keeps the existing alert id and rewrites a console-edited CPU rule). Extra files justified: `packages/config/src/observability.ts` as a browser-safe subpath (importing `@jewelo/config` from a client module pulled `load-env`'s `node:fs`), `pnpm-workspace.yaml` `allowBuilds` entries `@sentry/cli: false`, `core-js: false` (pnpm 11 refuses unlisted build scripts). Not deployed. Follow-ups: three `captureJourneyEvent` call sites in `Atelier.tsx` (step chips `:1194`, `:1203`, `confirmSpelling` `:643`, capture `~:910`); `bootstrap-app.mjs:112` should read the alert lists from the contract. Needs Sanchay: Sentry project (`SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN`), PostHog project (`NEXT_PUBLIC_POSTHOG_KEY`), the DO alert e-mail destination in the console.
- DS-6 studio-only switch committed (platform agent; lead read the migration: `runtime_policy.studio_only` default false, `expand_final_media_run` replaced with `security definer` and empty `search_path` kept; agent proof: `db:push` and `db:types` exit 0, column and `pg_get_functiondef` read back, full build exit 0; staging mock runs: `9f5991c0` with the flag off is four tasks ready as before, `d6f03864` with the flag on is studio `ready` plus three dependents `cancelled` `studio_only_policy` at 0 cents, one `provider_attempts` row, one outbox row, `principal_daily_usage.reserved_spend_cents 100` not 400, audit `taskCount 1`, run `complete`; `/api/state` shows the same rows and `personalizedRun.ts` maps `cancelled` to `unavailable` with no code change; flag set back to false and read back). The P5-2 order is in the runbook and in `staging-journey.md`: caps UPDATE, `studio_only = true`, flip to real, smoke, `studio_only = false`.
- Landing follow-up committed (lead opened `/` and `/ar` on the local app: they land on `/en/design/new` with "Your name, made precious." and on `/ar/design/new` RTL with "اسمك، قطعة ثمينة."; agent proof: full build and web lint exit 0, curl `/en` and `/ar` 307 to the atelier, `/zz` 404). `[locale]/page.tsx` redirects to the atelier with a `notFound()` guard for an unknown locale; lead one-line edit: the root `page.tsx` now redirects straight to `/en/design/new` (one hop instead of two). Follow-up: the bare domain ignores `Accept-Language` (an Arabic walk-in gets the English atelier and switches), a product call.
- P3-7 committed (agent proof: `db:push` applied `20260909020000_prompt_construction_variable.sql` only, four `POST /api/operator/prompts` create and publish 200 through the local dev server against staging with a locally minted operator cookie, full build exit 0, ai and web lint exit 0; mock run `7f8edc88` (`Asma`, framed-minimal) pins `image.packshot@v2` `b0e4d0fb`, `image.worn@v2` `3a09c39b`, `image.macro_gift@v2` `2eada36b`, `image.dark_editorial@v2` `ff47db1b`; the lab v4.3 studio prompt reproduces its documented hash `0a5a009a…`; unified diff lab vs production at scratch `p3-7/lab-vs-production.diff`, differences are slots plus three recorded production edits). `letteringStyle()` fix: `arabicStyle: none` on an English piece compiled to "Lettering: none"; now falls back to `lettering`, then `classic`. IMPORTANT for the next deploy: the published `@v2` templates need `{{construction}}`, which only this branch's `buildPromptVariableSnapshot` produces; until staging runs it, runs block pre-spend with `prompt_compile_failed` (fail closed, no spend). `docs/CALEUMS-FINAL-E2E-CONTRACT.md` hunk rides with the P2-2b commit. Follow-ups: `DEPENDENT_REFERENCE_RULE` says "first supplied image is the finished pendant" while the template says Image 1 is `@stencil` (P3-6); enum tokens (`full-pave`, `on_skin`) compile raw; `piece_spec` near the 512 ceiling for two names; `specification.lettering` reaches the prompt only as a fallback.
- P2-2b committed (lead opened `layla-en-framed-minimal.png`: one piece, rounded frame, rings on the top corners level, welds at the L foot and the final a into the bottom bar; `muhammad-ar-diamond-rails.png`: two rails, rings on the top rail's ends, posts from the letters to both rails, marks whole; build on the mixed tree exit 0; agent proof: two full 576-cell sweeps with 0 refused, 576/576 single piece and two clear holes, `WELDED-DELTA`, `PUNCHED-DELTA` and the new independent `CARRIER-DELTA` 0/576, tilt 0, overhang max 0.164, span min 0.670, name scale min 0.89; classical rerun unchanged, 592 PNGs byte-identical, 8/576 refusals as pass 6; `measure-stencils` 16/16; `verify_stencil.py` agrees on four cells; identity and jobs lint 0). Decisions recorded in D-021: two rails not one bar; welds at the baseline near the name's ends; `ringPlacement: "frame"` plus `carrier.kind`; geometry constants in `packages/identity` because they sit inside the fingerprint. IoU: registration lands on the frame (overlay `framed-minimal-ar-a5`) but p05 did not move (0.278 framed-minimal, 0.285 diamond-rails); the photo mask caps it; P2-3 carries 0.31 / 0.28 / 0.27. Follow-ups: weld point on `لي` minimal lands on a nuqta pair; `diamond-rails-en-a2` registers 10% oversized; `carrier -1:-1@-1` cosmetic in the per-cell line; engine release label still `caleums-identity-v4` though two constructions draw differently (construction id is in the fingerprint so no collision). Fix pass 7 (posts at the letter foot, single Latin letters, `كوثر`) is dispatched next on top of this.
- Security fix 3 committed (all 13 findings of `reviews/security-review-2.md`; lead checked the queue and atelier still render on the local app; agent probes: `/api/state` with the operator cookie and no `designId` 422 and 18 KB with one, `preview_requests` column privileges for `authenticated` equal the 11 customer columns and an anonymous JWT gets 42501 on `operator_note`, `contact`, `notified_at` (migration `20260909021000` pushed), cross-site GETs on three operator routes 403, `mark_contacted` on a fulfilled request writes zero audit rows, `fulfillment_transition` validated and `issue_quote` 404, the log sender logs `{transport, requestId}` only, subject folded at 72 characters, 70 KB body 413; full build exit 0, five lints 0, shellcheck 0). Deviations recorded: migration timestamp `021000` to avoid colliding with P3-7's `020000`; the RETURN trap self-clears (the literal form crashed every backup script); `issueQuote` client stub stays because the port declares it. Follow-ups: `supabase-jewelo-client.ts:592` calls `/api/state` without `designId` (customer branch today); two Vitest files assert removed behaviour; still no retention policy for `preview_requests.contact`.
- Storyline fix 1 committed (B3, M1, M3, M4 and three minors of `reviews/storyline-review-1.md`; lead verified on the local app: `/ar/design/new` title `صمّم قطعتك · CALEUMS`, no `32 mm`, 8 tiles marked, switching the script to Arabic puts `اسم واحد بالعربية` on the two-names tile; CSP `connect-src` names no posthog host with the key empty; full build exit 0, four lints 0). Replay paths announce; cron `preview-request-notification-sweep` every two minutes over `notified_at is null` older than `notificationSweepMinAgeMs` (60 s), batch 50, `not_configured` without `NOTIFICATION_TO`; dry run against staging lists exactly the 13 unannounced test rows and sends nothing (default: never announce them; the runbook has the SQL to clear them before the address is set). D-022: the sellable set is validated in `@jewelo/config/sellable`; the 10 Sep user direction now exposes all contract looks and removes the deployment allowlist (D-022 rode into `f0bb7cd` under the P2-2b message). `PRESENTABLE_PROVIDERS = ["openai"]` in contracts. Follow-ups: the notification function has no `onFailure` reporter; Inngest dedupes an event id for 24 h so a `not_configured` announcement cannot be re-sent by the sweep for a day.

Doing:

- Adversarial pass 6 on the identity engine (adversarial-reviewer) against the fix pass 6 commit; the lead records the report. P1-5 closes only after two consecutive passes find nothing above minor.
- Bar routing cleanup (implementer): the dead `ringPlacement === "bar"` branch, `IDENTITY_BAR_FALLBACK_REVIEW` and its contract and runbook entries go; `identity_no_ring_seat` proved as the pre-spend block; the four production-sourced stencils at `lab/stencils` regenerated.


- Field finding, 8 September 22:40 (Umayr testing staging `d4bb713f` on his phone): "What triggers generation" and "Put my name it gave me Asma". He typed his name and the photo panel kept showing the Asma example, because the sample stays until the shopper's own run produces photographs, and in mock mode it never does; the old "Preview my piece" label sent him to the review step without telling him the checkbox is the trigger. Second person in one day (the lead's own UX finding earlier) confused by the same panel. P6-3 (button renamed, honest end states) and P6-4 were committed after that deploy and are being deployed now. Open for P6-2/P6-3 follow-up: the "Asma example" caption is not read as an example; the panel needs to say plainly that it is a sample look until the shopper confirms, and in mock mode the honest end state should arrive faster than the current 30 s of "Being prepared".

Follow-ups found by subagents, not fixed (outside the task rows):

- `.env.production.local` at the repo root holds stale Convex keys from February; inert and gitignored; delete in a hygiene slice.
- `scripts/digitalocean/check-env.mjs` still accepts several env files although the runbook forbids it; a one-line guard would enforce it.
- `scripts/digitalocean/bootstrap-app.mjs:87-92` rebuilds `services` from the contract alone, so running it against staging would delete the `inngest` component. Warned in the runbook; script unchanged.
- `scripts/digitalocean/configure-github.sh` and the `do:github` script entry are dead (write a token into GitHub environments nothing reads).
- `apps/jobs/src/video.ts:69,140` uses `config.FAL_KEY!`; an operator video command in real mode with video off would fail at runtime instead of at the gate.
- `VIDEO_ENABLED` is not yet listed in `docs/DIGITALOCEAN-DEPLOYMENT.md` or the app spec.
- The stage 2 and v4.3 verdict files were never merged into the ledger (`merge_verdicts.py`); that is P3-1's first step.
- `lab/final/reference-studio-prompt.txt` is still the v4.2 compile while `compile.mjs` emits v4.3.
- `review/sample-images-board` on `home-mini` was refused by `git branch -d` (remote tip diverges); left in place.
- DigitalOcean token expires 25 November 2026 (Sanchay).
- `scripts/digitalocean/deploy.sh:63-70` prints no deployment id when run over a non-interactive ssh channel (the `read -r ... < <(...)` yields nothing); the id has to be read from `doctl` afterwards. One-line fix owed.

Open:

- The parked Claude session on `home-mini` (`hq-claude2:2.1`) is stuck on an out-of-credits prompt with two background agents pending; only Sanchay can answer it.
- Everything under Open in session 1 still stands.

## Session 3 (continued), 2026-09-09 early morning

- Fix review 3 (`reviews/fix-review-3.md`): BL-1 fixed and applied (`d358b81`, migration `20260909022000`); MJ-1 (note audit), MJ-2 (sweeper env values, 24 h dedupe limit) and seven minors open.
- Storyline fix 2 committed (`200bcfe`): real-mode spend ceiling gate `spend_ceiling_not_set` (`apps/jobs/src/spend-ceiling.ts`, config `REAL_MODE_MAX_RESERVED_SPEND_CENTS` 800 / `REAL_MODE_MAX_ATTEMPT_BUDGET` 3, `policyCacheMs` 60 s), queue hides retry on `DETERMINISTIC_REFUSAL_CODES`, same-origin without `x-forwarded-host`, three journey events, scoped client state read.
- Staging deployment `e04b835e` ACTIVE at `f63b3e3` (security fix 3 + storyline fix 1); `d358b81` and `200bcfe` are not yet deployed.
- P1-5 fix pass 7 is half written and unproven; pushed as `codex/fix-pass-7-wip` (`93c22af`) and left uncommitted in this tree. The matrix has not been run and no stencil has been opened. Next: run the letters and names matrices, open the eight named renders, then adversarial pass 7.
- The session process exited mid-run; the fix-review-3 implementer (BL-1 staging proof, MJ-1 migration, MN-1..MN-7) and the deploy agent were stopped. Only the migration above landed from that brief.
- Housekeeping: the laptop disk hit 100%; the 13 GB `.turbo` cache was deleted (9.8 GiB free now). `.tmp/rnd-stills` (2 GB, 4 Sep, untracked) was left alone.
- Rule recorded: publish prompt releases only from a deployed build (staging broke 22:25-22:56 UTC on 8 Sep when `@v2` prompts were published before `c9265aa` was live).
- Umayr's feedback of 8 to 9 Sep read from WhatsApp and stored with his screenshots and a ranked nine-item list at `docs/goals/road-to-gold/feedback/umayr-2026-09-09/README.md`; items 1 to 4 are the next customer-page slice (P6-8, DS-4 env flip), 5 is a new task P6-9, 6 to 8 need Sanchay.

## Session 4, 2026-09-10

- Local hydration investigation closed. At `http://127.0.0.1:3011/en/design/new`, the page was server-rendered but had no React root/fiber; mobile accordion clicks left `aria-expanded`, `data-expanded`, and body display unchanged. The same build hydrated and responded at `http://localhost:3011` and on staging, so this was a Mac mini dev-origin/runtime issue, not an Atelier state bug. Next dev logged blocked HMR requests for `127.0.0.1` (and the Mac mini Tailscale address), and the in-app browser recorded the failed WebSocket handshakes.
- `apps/web/next.config.ts` now allows `localhost`, `127.0.0.1`, and `100.102.144.100` in development. The port-3011 LaunchAgent now sets `JEWELO_DEV_DIST_DIR=.next-3011`; the unrelated port-3001 process was not killed or changed, and the two dev servers no longer share the Turbopack lock.
- Proof after restart: `/api/health` 200 on loopback and Tailscale; the 127.0.0.1 page has a React fiber and all desktop sections report `aria-expanded=true`; at 390x844, clicking `02 Style` changes it from collapsed/`display:none` to expanded/`display:block`; Tailscale page also has a React fiber and no browser console errors. `corepack pnpm build` with `JEWELO_DEV_DIST_DIR=.next-build-verify` passed 13/13.
- Existing uncommitted `apps/web/next-env.d.ts` and tool/playwright artifacts remain untouched. The dev-origin fix is not yet committed or deployed; it is local-machine development plumbing, not a staging product change.

## Session 5, 2026-09-10

- Cloud provider activation is now derived from `NODE_ENV=production`: production always selects the real OpenAI adapter, while non-production development/test may still use the local `PROVIDER_MODE` convenience. `parseJobsEnv` rejects an explicit production `PROVIDER_MODE=mock`, and `apps/web/src/instrumentation.ts` validates the full jobs environment before a production server starts, so missing real credentials fail closed.
- `PROVIDER_MODE` was removed from the DigitalOcean env contract. `deploy.sh` validates the environment before syncing, removes any stale provider-mode key from the live app spec, and the readiness response now reports `provider: real`; `smoke.sh` asserts that field. The ignored local `.env` no longer carries the unnecessary mock toggle, so local development still defaults to mock without exporting a cloud activation flag.
- `JEWELO_DEV_DIST_DIR=.next-build-verify corepack pnpm build` passed 13/13. Shell syntax, env-contract syntax and a no-value deployment dry run passed; the dry run listed 18 merged keys and no `PROVIDER_MODE`, and made no DigitalOcean call or provider call.
- No paid image generation was performed. Staging deployment and live readiness/runtime proof remain the next actions in this session.

### Session 5 deployment proof (2026-09-10)

- Provider selection is now runtime-derived: `NODE_ENV=production` selects the
  real OpenAI adapter; local development/test may still use `PROVIDER_MODE`.
  Production startup parses the jobs environment and fails closed when the
  OpenAI credential is absent. `PROVIDER_MODE`, `JEWELO_CLOUD_BUILD`, and
  `JEWELO_CLOUD_TARGET` are not cloud activation controls; deploy/bootstrap
  remove stale copies from existing specs and no longer create them.
- Commit `32404c7` deployed as DigitalOcean deployment
  `9439c0a6-0810-4d9b-96c7-58d55dfb7791` (source
  `32404c70a04fabbd981fdb74f2720e60b747838e`); that deployment was superseded
  by the env-spec cleanup below. The active deployment is
  `b50be520-f728-4234-870e-7141dbea3cfa`, source
  `cc9fb01c8153d1c562a50da281f9137605eda5b0`, with App Platform rollout 9/9.
  Live protected readiness returned `status=ready`, Inngest
  `keyEnvironment=prod`, OpenAI `configured`, `provider=real`, and a valid
  trusted IP header. The live smoke script passed health and readiness. No
  provider/image call was made.
- The app spec still carried a legacy `JEWELO_CLOUD_BUILD` key after that
  deploy, so deploy/bootstrap cleanup was committed and pushed as `cc9fb01`.
  Follow-up deployment `b50be520-f728-4234-870e-7141dbea3cfa` is ACTIVE at
  source hash `cc9fb01c8153d1c562a50da281f9137605eda5b0`; its web env key list
  contains none of `PROVIDER_MODE`, `JEWELO_CLOUD_BUILD`, or
  `JEWELO_CLOUD_TARGET`.
- Verification: `corepack pnpm build` passed 13/13; `corepack pnpm lint`
  passed 13/13; `node --check` for deploy/bootstrap/env-contract, `bash -n`
  for deploy/smoke, and `git diff --check` passed. The build still emits the
  existing Next Edge warnings for the Node-only config loader but compiles and
  starts successfully.
- Deployment timing was recorded for the runbook: the source deploy took about
  15 minutes (roughly 9m50s build plus 5m rollout) on the shared staging
  instance; a restart-only deployment took about 63 seconds. Config-only spec
  changes should use `doctl apps update --spec ... --wait` without
  `--update-sources`, while source changes continue to use the full deploy and
  its immutable deployment record.

### Session 6 local-first path (2026-09-10)

- Scope is local development plus one staging instance; production is
  intentionally skipped. The local web app is `apps/web` (Next.js 16, `next
  dev`, port 3011) and the local job runner is Inngest Dev on port 8288,
  posting to the local `/api/inngest` route. Both health endpoints return 200.
- The local API uses the hosted Supabase project from `.env` for durable data
  and storage: `SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_URL` both point at the
  `jggalwuvpcqoenhirmnl.supabase.co` project. The local `.env` contains an
  OpenAI key and a Fal key, but the dev runtime defaults to mock provider mode;
  no provider call is made unless a real-provider run is explicitly authorized.
- Provider boundaries are deliberate: OpenAI is the production still-image
  adapter; Fal is only the optional Seedance video adapter and remains off when
  `VIDEO_ENABLED` is unset/default `0`; Runway is the prompt-lab bench, not an
  application runtime dependency.
- The local page is open and marked as the deliverable in the Codex in-app
  Browser at `http://localhost:3011/en/design/new`; the page exposes the name
  control and design steps with no browser error/warning logs.
- Not implemented / still open: P1-5 identity pass 7 plus two clean
  adversarial passes; BL-1 anonymous request/replay proof; fix-review-3 MJ-1
  and minors 1–7; the viewport/RTL/reduced-motion ladder; P2-3 through P2-7;
  the paid P5-2/P5-3 real run; P3-5/P3-6; P7-4/P7-8; and L-1 through L-4.
  These remain documented in the task list rather than being silently treated
  as shipped.

### Session 7 custom restriction cleanup (2026-09-10)

- Removed the deployment-time look override and the extra customer-facing
  restriction: all four constructions and six lettering styles are now
  selectable in the normal flow. `sellableLooks()` mirrors the request contract
  deterministically; identity spelling, geometry, verification, auth, and spend
  gates remain unchanged.
- Removed the override from the local environment and deployment contract;
  deploy sync also deletes stale copies from an existing app spec. Decision and
  feedback docs now record the user-directed all-look behavior. Local browser
  proof at
  `dogfood-2026-09-10/declutter-styles-expanded-390x844.png` (and the full-page
  `declutter-styles-390x844.png`): 1440×900, 390×844, and
  Arabic RTL all render without reference-only labels, browser errors, or
  horizontal overflow; the review confirmation remains enabled for
  `Framed minimal` + `Kufi`. Staging deployment `84f54bd8-927f-48cd-b871-071e7fc78923`
  is ACTIVE at branch head `eba96fd`; smoke and protected readiness passed with
  `provider=real`. No paid provider call.

## Session 4, 2026-09-22 - Omran call: calmer styles on gpt-image-2.5-sunburst, site feedback

Source: Wispr Flow meeting "Jewelry Design Image Generation", 22 Sep 06:10-06:45 UTC (styles too stylized; origami needs sharp folded-paper edges; 2-3 variants on the boxy look; reuse his WhatsApp references; one default chain; multiple stones with quantity; size first; bigger results; newest saved first; email on save; no italics). Sanchay approved capitals and the model `gpt-image-2.5-sunburst`.

Evidence so far (Runway, 1:1, name ASMA, origami):
- Today's serif stencil + current prompt on sunburst: still crumpled facets and "lAsmal" - the model change alone does not fix it.
- Cairo at wght 800 with tracking -60 units in the stencil gives the boxy capitals (12/12 lab stencils pass every engine gate).
- Calmer wording alone gives a flat plain plate; adding a text-free crop of Omran's image 36 as a texture-only `look` reference gives the folded planes with nothing copied. Same result on gpt-image-2.
- OpenAI docs: Sunburst `high` is about gpt-image-2 `medium`; production hardcodes `quality: "high"`.

Undone: unrequested edits by two viewer agents (carrier constants, construction prose, D-021 amendment, a PROGRESS entry). Their reference reports stay: `reference-visual-spec-shortlist.md`, `reference-visual-spec-specsheets.md`.

In flight: site feedback (implementer), stencil per-construction lettering + fix pass 7 proof (implementer), look reference role / quality config / sunburst lineage (implementer), four-style lab on sunburst (`docs/goals/road-to-gold/lab-2026-09-22/`).

- Review of de584ed (site feedback): blocker multi-stone missing from piece_spec/stones prose, major dead Add-to-bag on design stage, major double submit / unstable requestKey, minor Arabic stone-cap key drift - all sent to an implementer. Follow-up only: bags saved before 22 Sep with a non-Cable chain re-run once because the chain is now forced to Cable 45 (self-healing, no data loss).

### Session 4 close-out, 23 Sep 2026

- Reviews: seven rounds on the branch; rounds 6 and 7 found nothing above minor, and their minors are fixed through d29e0fc.
- Follow-up (pre-existing, from 7b76e5a, outside this branch): a bag row falls back to `item.sampleId`, which for a design with no exact photograph is a sibling or cross-script substitute, and shows it under "NAME PENDANT · YOUR DESIGN" with no substitution label (`Atelier.tsx` bag row). Closest standing risk to never-borrow-a-design; needs the sample note or the guarded example path.
- Follow-up: origami shop sample photos still show the old stylized mixed-case look, not the boxy capitals Omran chose.
- Follow-up: stones prose is safe only up to GEMSTONE_MAX=3; classical "a middle letter" for two-letter names; `visualFields` stale (chain, gems); `look_rule` not in the variable snapshot.
- Known: the still reservation floor only bites at quality max/xhigh/auto; at the default high it checks nothing, and the runtime_policy spend cap is what stops staging spending.
- Known: `DIGITALOCEAN_ACCESS_TOKEN` in `.env` returns 401; deploys used doctl's own credential on home-mini.
- 23 Sep: first deploy from the laptop, deployment `6a67f2a7-587c-4185-895b-ddd2137d2cf3` on `a1fb414` (app code identical to `dff6102`), smoke passed. Browser pass in `dogfood-2026-09-23/live-a1fb414/`: sections Size/Name/Style/Gold, 0 italics, no chain picker, no AI/generate/prompt/magic words, Arabic RTL at 390x844 with no overflow. Finding: the first cold load right after the deploy timed out every sample photo at 15 s ("This sample photo could not load"); warm, style switches settle in about 0.5 s. Sample PNGs were about 1.8 MB each; fixed in 76103b9 (WebP, hero first, year cache on images only). Follow-ups: ~400 MB of unreferenced PNGs still ship in public/atelier; no PNG fallback for browsers without WebP (Safari below 14); Vitest literals still name .png.
- 23 Sep: staging now builds from the Dockerfile (standalone). Deployment `5d810bda-b6eb-4933-8442-a0f86c5f5b68` on `32ce585` went live in 376 s wall clock on its first, uncached build (buildpack deploys were about 870 s). Smoke passed; `/en` and `/ar` 200; `/api/inngest` 401 (route loads); `.webp` and `.jpg` samples 200, old `.png` 404. Inside the live container (`doctl apps console` through `script`), sharp renders with libvips 8.18.6 and the identity fonts are in `.next/server/assets`. First visit on a clean browser: 1.3 MB of images, hero ready in about 1.5 s (was 15.6 MB, 15 s timeouts).
- Still Sanchay's: authorize the DigitalOcean GitHub app for push-to-deploy; decide on removing about 1.5 GB of proof files from git history (about 2 minutes per deploy).

## 23 Sep 2026 - first paid runs on the live staging link

Spend approved by Sanchay; staging on `gpt-image-2.5-sunburst-2026-09-08` quality max, `runtime_policy` cap 800 cents/day, 3 provider attempts, daily limits 4.
Evidence in `dogfood-2026-09-23/real-run/`.

- Run 1, English origami "Asma" (studio_only on): studio ready on attempt 1; boxy folded ASMA, chain through both rings (`01-studio-ready.webp`).
- Run 2, Arabic framed-minimal Kufi "أسماء" (studio_only off): studio, on_skin, close_up and dark all ready on attempt 1; spelling correct and identical in all four, same frame, cable chain (`02-ar-page.webp`, `03-ar-four-photos.webp`).
- Live form pass: size first, 0 italic elements, no chain picker, third stone disables the rest with "Choose up to 3 stones.", no horizontal scroll at 1440 and 390, review readable on phone (`04`, `05`).
- Email capture on the live link: "We will send it to you", reference `be5ee13d-...`, row present in `preview_requests`. The shop is not emailed yet (log-only until Resend).
- Spend: about 5 stills at about 21 cents each, roughly USD 1.05.

## 23 Sep 2026 evening - Omran tested the wrong app; staging database had gone read-only

- Omran's WhatsApp ("Jewelry AI" group, 19:22 UAE): "the design first part is not there", a script pavé "love" in rose gold, "supposed to be origami rose gold", "the link above". The link above is `caleums-xp8xk.ondigitalocean.app`: a different DigitalOcean app (`caleums`, region lon) built from the separate private repo `Sanchay-T/caleums` main `e5d267a` (15 Sep). None of this repository's work has ever been on it.
- On this repository's staging, a fresh visitor failed at anonymous sign-in: Supabase `POST /auth/v1/signup` 500 "Database error creating anonymous user". Cause: project read-only mode (Management API `/readonly` enabled) because the database was 1571 MB against the 500 MB free limit; `inngest.spans` 939 MB, `traces` 276 MB, `history` 178 MB, `trace_runs` 79 MB. Writes stopped at 13:34 UTC.
- Fix, with Sanchay's approval: the last 24 hours of those four tables exported, tables truncated, rows reloaded (database 186 MB); read-only lifted via `POST /v1/projects/<ref>/readonly/temporary-disable` after it re-armed once; hourly pg_cron `inngest-log-retention` keeps 24 hours (migration `20260923000000`, commit `26c7795`). Fresh-visitor sign-in then 200 and a run started.
- The origami, framed, rails and default classical shop samples are still the old stylized mixed-case photographs; being replaced with v10 photographs made by the live pipeline.
- Spend: `REAL_MODE_MAX_RESERVED_SPEND_CENTS` and `runtime_policy.global_max_reserved_spend_cents` raised to 2000 (each run reserves 400 up front against about 84 actual) and `global_daily_generation_limit` to 12, for the sample runs; to be restored to 800/4 after.
- Follow-ups: 27,000 Inngest spans a day come from the every-minute outbox and every-two-minute sweeps; the session pooler hit its 15-client cap from Inngest's idle Supavisor connections; the old `caleums` app still serves the old studio to anyone holding its link.
- Samples replaced (commits `5a9a528`, `d457eb3`, deployed `fd6cb4c9`): v10 classic, origami, framed, rails (English Asma, yellow, no stones) and Arabic framed Kufi, each four views from real pipeline runs `d59288cb`, `65427f44`, `93a3564d`, `ebc530f6`, `50a2d290`; dark stills centre-cropped to 4:5 (`dark45`). Lineage in `apps/web/public/atelier/v10/manifest.json`. Checked live at 1440 and 390, Arabic RTL framed Kufi exact.
- Omran's case on the live link: origami rose gold "Love", run `203bd0a5`, all four views ready and correct (boxy folded LOVE, rose gold, cable). Evidence `dogfood-2026-09-23/omran-case/`.
- Reviewer on the sample diff: no blockers; "ASMA in capitals" rejected (the capitals are the D-023 construction lettering the customer's own piece gets); comment and lineage minors fixed; dark crop fixed; open: a bag saved before this deploy still shows its old sample thumbnail.
- WhatsApp "Jewelry AI" group, 22:18 IST: tagged Omran with the staging link (old one "cached"), the Love four-view photo, the feedback list.
- Spend today: 9 runs, 546 cents actual at the check (about 5 more stills since). `runtime_policy` left at cap 2000 and 16 runs a day so Omran can try names tonight; restore to 800/4 when he is done.

## 24 Sep 2026 early - Omran's folded origami look is live

- Omran replied "Perfect / will start testing" and shared a ChatGPT origami LOVE (triangular folded panels, polygon O, centre bail). Lab `lab-2026-09-23-origami/` (Runway, sunburst): V3 prompt (folds allowed inside the stencil footprint, triangle-panel style, 2.5 mm rendered depth, his crop as look) reads as folded origami on LOVE, ASMA, أسماء; the w600 Latin stencil makes it slim with open A counters; the centre bail works once the prompt forbids a jump ring.
- Ported V3 to production (`4bf9373`, compiler v4, `lab-diff` 17/17, look reference republished 4/4), deployed `411aac47`. Live run `c3df0628` origami rose gold "Love": all four views ready and correct (`dogfood-2026-09-23/omran-case/09-love-v4-four-views.webp`).
- In progress: origami Latin stencil weight 800 -> 600 (engine change, needs sweep and two adversarial passes); "free mode" lab (short prompt, no stencil) across all four styles to measure spelling pass rate.

## 24 Sep 2026 - split design lab: free piece, our presentation

- Free-mode lab (`lab-2026-09-24-free/`): a short Omran-style prompt with no stencil spells most names and looks like cast gold; the stencil is what makes our pieces look laser-cut.
- Split lab (`lab-2026-09-24-split/`, commit `4a391b7`): free piece sentence per style plus fixed real-jewelry-physics and real-photograph blocks; studio first, other three views from it as `@master`. 8 pieces (4 styles x Muhammad, فاطمة), all 32 views pass: spelling exact, Arabic dots fused, real 35 mm scale on skin, same piece across views. Sheets in `sheets/`, prompts and verdicts in `ledger.md`. 1437 Runway credits.
- Risk: one on-skin call failed provider moderation (`SAFETY.OUTPUT.THIRD_PARTY`); modest wardrobe wording passed.
- Waiting on Sanchay's review of the sheets before porting the split design into the compiler.
- Origami Latin weight 600 (implementer, measured, reverted to 800 in `5db635c`): build exit 0, 576-cell sweep refuses the same 18 Arabic names as before, LOVE/ASMA byte-identical to the lab stencils, other constructions byte-identical. Held uncommitted: if the split design is approved the stencil no longer drives the piece, so review and deploy of this change wait on that decision.
- Human review (Sanchay): classical فاطمة is two pieces in all four views (ا never joins the next letter). Weld-line test fixed فاطمة, broke عمران; stencil-guided keeps one piece but shows bars (`59f3611`, ledger "Found in human review").
- Sanchay said "do it" to: one-piece check, free prompt first, stencil where free cannot hold. Phase 8 in `docs/TASKS.md`: SP-1 (enforced `singleConnectedPiece`, implementer running), SP-2 (deterministic free/stencil route per task, plan-reviewer challenging it before code).
- Origami weight 600 committed as its own slice `f310301`; still needs the review loop before deploy.
- Plan review of SP-2 (no-go as one task): SP-1 is inert on real runs because production wires `MockStudioVerifier`; free must still render and gate the stencil; route must be wider (hamza, Latin print, tittles, layouts). SP-2 split into SP-2a..f in `docs/TASKS.md`. SP-2a (real piece reader) and SP-2b (route function) dispatched in parallel on disjoint files.
- Open question for Sanchay (default taken): a free photograph shows a piece the stencil did not measure. Default: the shop makes the piece shown in the photograph, as Omran already does from his own ChatGPT images; the stencil stays the spelling and ring gate.
- Landed since the plan review (all pushed): `06de81d` SP-1 gate; `1d73117` database refuses a completion without `singleConnectedPiece = true` (migration applied to `jggalwuvpcqoenhirmnl`); `0726fe2` + `45ac449` SP-2b `stillRoute` (JEH is R, reason codes carry no name letters); `5b5ef65` SP-2a blind `OpenAIPieceReader` on every paid still, reuse guard and replay tool carry the field; `ad69475` SP-2c compiler can build a studio prompt with no stencil (unused until SP-2e); `8b72988` a piece refusal fails the attempt in place and reserves the next one (fixes the retry-after-checkpoint loop); `4574935` SP-2d free piece wording and ten pinned free prompts, `lab-diff` 17 stencil + 10 free MATCH.
- Adversarial pass on w600 + SP-1 (fresh context): 2 blockers, 4 majors, 3 minors. Already fixed by later commits: SP-1 inert on real runs (`5b5ef65`), reuse guard and DB restatement (`5b5ef65`, `1d73117`), retry loop (`8b72988`), replay tool blind to the field, duplicate operator wording, refused photo not kept (`8b72988`). Open, implementer running: w600 ribbon hits tracking 120 and 25 of 47 Latin names need bridge bars (muhammad 3 bars); `identity_reuse_lettering_mismatch` strands in-flight origami runs (not deterministic, no operator words); framed-minimal CARRIER-DELTA silently dropped ~282 Latin cells.
- SP-2d lab running: three production look crops uploaded to Runway, ten free prompts x 2 takes byte-exact (image-lab), then lead review and blind piece-reader replay.
- Live check on staging (deploy `c0034242`, commit `8b72988`), run `4cfd9a5b` origami yellow "Asma": studio ready with `pieceCheck` recorded (one piece true, name read "ASMA"), a slim folded w600 ASMA, both rings attached (`dogfood-2026-09-24/live-asma/01`). The three dependent views were blocked pre-spend with `prompt_compile_failed:still_unresolved_reference_tag:@stencil` - a regression from `ad69475`, whose new check read the IMAGE ROLES header (the `master` rule has always said "@stencil"). The shopper saw "The shop will photograph it and send it to you" on those three (`02`).
- Fix `0843792` + `8b3240a`: the check reads the release body only; run `c3df0628`'s four stored snapshots recompile byte-identical; `lab-diff` now compiles all 12 dependent profile x construction pairs and fails on the old code. Reviewer: no blocker or major. Deployed `9c19668e`. Run `4cfd9a5b` stays blocked (deterministic refusal); a fresh rose-gold Asma run `440f6323` checks all four views.
- SP-2d generations done (image-lab): 20 stills, 10 cells x 2, all prompt sha256 matched, 340 Runway credits. Blind piece reader + name reader replay (USD 0.35): name 20/20 match; one piece fails on ليلى a/b (both dots float - confirmed by eye), Love a/b, Omar a, rails b, framed سلمى a. By eye origami محمد b reads closer to أحمد and a to لحمد, which the name reader passed. Blind viewer scoring running.
- RC-1 (platform): checkpoint resume now needs an open attempt, in the sweeper (migration `20260924010000_resume_only_open_checkpoints.sql`, pushed and read back live) and in `loadStoredOutput`; a dispatch past the attempt budget goes to operator review. Fixes the `440f6323` studio loop (audit 1305/1306); commit `c1aa8c7`, deployment `726fe3e1`.
- Open finding RC-2 (major, pre-existing, widened by RC-1): two dispatch events for the same task can both reserve attempt N+1 when the Inngest still queue backs up past the stale window (602 s after RC-1c) - `reserve_provider_attempt` hands an existing non-succeeded row back as a go-ahead - so both call the image provider. Fix direction: a dispatch lease on the task (compare-and-set token in the reserve RPC). Not started; owner next session.
- RC-1 follow-up (platform, commit `07c1839`): a transient vision read failure is repeated once inline (`readVision`, `pipelineLimits.visionReadRetries` = 1) before the attempt closes; executor cap 360 -> 480 s, stale window 480 -> 600 s. Budget exhausted, missing runtime policy, spend guard, checkpoint download failure and missing checkpoint attempt end at operator review instead of looping.
- RC-1b (platform, `e458b6e`, deployment `4a71cdfe`): only timeouts, dropped connections, 429 (after `visionRetryDelayMs` = 2 s) and 5xx are repeated, not empty or malformed answers; cap 480 -> 484 s, stale window 600 -> 604 s (route `maxDuration` 484). The spend guard stops only once the task's `reservation_usage_date` is before today UTC, otherwise it waits for the next window. A checkpoint download stops only on 404/410, any other status keeps the attempt open. The in-loop re-reservation uses the same refusal rules. `provider_attempt_budget_exhausted` and `provider_attempt_checkpoint_missing` are deterministic refusals now; operator words added for the read-failure codes. `docs/DIGITALOCEAN-DEPLOYMENT.md:521` still says 360 s.
- RC-1c (platform, `acd33fa`): third review pass. Missing stored photograph (sign 400 `NoSuchKey`) and more than 3 resumes of one stored photograph end at review charged at the estimate (`stored_output_resume_exhausted` is new); checkpoint read before preparation, and a preparation failure on a resume keeps the attempt open; piece and name reads concurrent, vision timeout 60 -> 120 s, cap 484 -> 482 s, stale window 604 -> 602 s; storage transfers 30 s timeout; retry hidden at the attempt budget. Live check: `440f6323` studio task `c2909f28` finished `ready` at attempt 3 after one recovery of attempt 2.
- Retry path review loop: pass 1 (c1aa8c7) 3 majors, pass 2 (07c1839) 1 major, pass 3 (e458b6e) 1 blocker + 2 majors, pass 4 (acd33fa) nothing above minor. Live proof: run `440f6323` recovered through RC-1 (attempt 3 booked by the new retry branch, all four views ready, `dogfood-2026-09-24/live-asma/03-06`). Recorded minors, not fixed: a duplicate dispatch can close the other dispatch's open paid attempt at $0 (RC-2 family); customer cancel marks an open attempt ambiguous with null cost and releases from current_date (pre-existing); a generate timeout books $0 for a request OpenAI may have billed (pre-existing); the 482 s cap is not a hard bound because PostgREST calls and in-generate reference downloads have no timeout; a storage stall over 30 s on a first dispatch now blocks it for operator retry instead of hanging; a terminal verdict whose blocked transition fails can be re-dispatched once by the widened retry branch.
- Origami English back to wght=800 (`5db635c`): 600 needed bridge bars on 25 of 48 names; 700 missed by one name each way; only 800 met the rule. Omran's slim look is traded for no bars; a per-name weight ladder is the lever if he asks for slimmer letters again.
- Plan review of SP-2e (option A): the free route stays off until free dependent views hold the same piece; SP-2e/SP-2f replaced by SP-2e1 (plumbing, inert), SP-2f1 (compiler, done `8c509b0`), SP-2f2 (48-still lab, running), SP-2e2 (switch on with D-024 + contract + CLAUDE.md line), SP-2g (cleanup).
- RC-1d (platform, `17e6270`, on top of `acd33fa`/`80ed393a`): operator words for every stop code the still and film jobs and the sweeper can write (derived mechanically, 60 missing -> 0 of 106); 31 more deterministic refusals (configuration and row properties); a failed runtime_policy read no longer fails the review queue; config comment on which read failures retry corrected.
- SP-2e1 (platform, `374eb19`, adversarial pass nothing above minor): route recorded in `generation_prompt_snapshots.still_route` (migration `20260924020000_prompt_snapshot_still_route.sql`, pushed; 357 existing rows read `stencil`; a six-argument call still resolves), pinned to stencil by `STUDIO_STILL_ROUTE`; stencil-route prompts and requests unchanged (`lab-diff` exit 0). Also: deterministic refusal list corrected per the fifth review pass (dependency_blocked/failed, inspiration_reference_missing and the configuration codes are retryable again).
- SP-2e1b (implementer, committed by the lead): four findings from the second adversarial pass on SP-2e1 (`8c509b0..9a7833c`) folded. A snapshot with no `still_route` (a database without migration `20260924020000`, e.g. a restored backup) now reads as `stencil` once in `apps/jobs/src/presentation.ts:659` and that one value feeds the lineage check and the request build, so only a present-but-invalid route is a `prompt_snapshot_lineage_mismatch`; a dependent view whose parent went terminal before writing a snapshot is refused pre-spend with `dependency_<status>` (`presentation.ts:574`) instead of waiting for the 602 s stale sweeper; `still_free_route_carries_stencil` and `still_stencil_required` have operator words and are deterministic refusals (`still_route_variables_mismatch` was listed too, then removed in SP-2e1c because it is never stored); D-023's addendum records that `task_prompt_release_mismatch`, `prompt_snapshot_lineage_mismatch` and `identity_pipeline_release_mismatch` are final only for the current deployment - after a prompt-release republish or a rollback to the pinned pipeline release the operator has to re-open those rows in the database. Build exit 0, `lab-diff` exit 0 (92 MATCH, 0 DIFFER). Follow-up: `still_route_variables_mismatch` is raised inside `compileStillPrompt`, so today it is always stored as `prompt_compile_failed:still_route_variables_mismatch:...` and the queue reads the `prompt_compile_failed` words; the new entry goes live when SP-2e2 moves that assertion pre-spend into `presentation.ts`.
- Live stencil-route proof on staging (deploy `3841cafe`, commit `9a7833c`): run `7541f131` classical yellow `فاطمة`, the name whose two-piece pendant started SP-2; all four views ready at attempt 1, every task snapshot reads `still_route = stencil`, the piece reader recorded one piece on all four, and in my own browser all four show the same فاطمة as one connected piece with both rings attached (`dogfood-2026-09-24/live-fatima/01-04`).
- Second adversarial pass on `8c509b0..9a7833c`: 1 major (a missing `still_route` column read as a corrupt snapshot) and 4 minors, all folded in SP-2e1b; the clean-pass count restarts.
- SP-2f2 (image-lab generated, viewer scored blind, lead replayed the readers): 48 free dependent stills from the production-compiled bytes, 912 Runway credits. Viewer: 8 of 8 cells, 48 of 48 pass. Reader replay (USD 0.70): name 47 of 48, the miss a false "Asmaa" on a plain Asma; piece 40 of 48, all 8 refusals framed-minimal, and my own look at three of them says one piece (word on the inner rail, or tied by the lam strut). The piece reader cannot yet read a framed pendant in dark, close-up or on-skin light, which matters on the stencil route too: new task SP-2a3, and framed-minimal stays off the free route until it closes.
- Deployed `2989ce0` to staging (`bb763e04`, health and readiness passed). Third adversarial pass on `8c509b0..2989ce0`: 1 major (the dependent-view route reads name the column, so an unmigrated database loops the three views and the reuse guard calls an absent route a mismatch) and 6 minors; folding as SP-2e1c, clean-pass count restarts after it.
- SP-2a3 (implementer, uncommitted in the tree): `ONE_PIECE_RULE` (`packages/ai/src/studio.ts:66`) now says joined is transitive - a word linked to the frame by one strut, inner rail or shared edge is one piece with it, and the open background inside a frame is expected, not a break - while a point or corner contact is still no link, a floating dot is judged on its own contact, and a non-joining Arabic letter with nothing bridging it is still two pieces. Three wordings measured on the SP-2a2 calibration set: wording 1 and 2 let point-contact dots through (4 and 3 misses of 30 split reads), wording 3 refuses all 10 split images on all 3 reads (0 misses) and is what stands. Dependent replay of the 48 SP-2f2 stills: piece 46 of 48 against 40 of 48 before, framed-minimal 10 of 12 against 4 of 12, classical and diamond-rails still 36 of 36; the two that still refuse are `framed-minimal-muhammad-ar-dark-b` and `framed-minimal-salma-ar-close_up-b`, both single reads saying no metal bridge reaches the word. Name reader untouched, 46 of 48 (the same false "Asmaa" cell on both takes). Spend USD 2.869 of the USD 3 cap; build exit 0. Numbers and wordings in `lab-2026-09-24-free-route/reader-calibration.md`. Not done for cost: wording 3's false-refusal rate was not re-measured on the 35-image calibration set, only on the 48 dependent stills.
- SP-2e1c (implementer, `e8899be`, deployed to staging, health and readiness passed): every stored route is read through one reader that names no column, so a pre-migration database reads stencil on the dependent path too; the reuse guard compares the parent with the child's already-resolved route; a free snapshot whose text names `@stencil` is a lineage refusal; `still_route_variables_mismatch` was removed from the operator words and the deterministic list (it can never be stored, `prompt_compile_failed` wraps it), so the SP-2e1b line saying it has words is superseded; `still_model_required` and `unsupported_still_aspect_ratio` gained words; lab-diff 95 MATCH, 0 DIFFER, with both directions of the route guard and the stencil studio reference order pinned, and the guard's removal proven to fail them.
- Fourth adversarial pass on `8c509b0..e8899be`: 1 major (the snapshot write on a pre-migration database answers PGRST202, which the classifier does not know, so new requests loop silently) and 7 minors; folding as SP-2e1d.
- SP-2e1d (implementer): a PGRST202 answer to the snapshot write (a database behind this code) is now a pre-spend block with its own retryable code `prompt_snapshot_schema_behind` and operator words, not a silent sweeper loop; the film job's four recipe heads have words; lab-diff pins the stencil dependent order [master, stencil, look, style] (96 MATCH, 0 DIFFER); `dependencyStillRoute` and `dependencyTerminalStatus` are required; the route read takes `limit=1`. Build exit 0.
- SP-2a3 is NOT closed: my first count of the full calibration run used the stale labels in `cal.mts`. Recounted on the corrected labels (review 5 caught it): split 29 of 30 refused, one miss on the framed corner-dot فاطمة (`v2/f-fat-studio`, 1 of 6 reads over both runs); one-piece false refusals 4 of 71 (5.6%). A miss is a wrong pendant reaching a shopper, so the SP-2a2 rule text is restored byte-identical and deployed; wording 3 stays in `reader-calibration.md` as evidence.
- Live framed proof on staging with wording 3 deployed (`0affdd26`, `01aa6bb`): run `2ac83312` framed-minimal yellow محمد, all four views ready at attempt 1, every snapshot `stencil`, piece check one piece on all four. Superseded for the gate by the revert above, kept as evidence that the dependent-view route read works live.
- SP-2e1e (`fb90889`): the schema-behind check reads the code field (`PGRST202` or `PGRST203`, the latter answered as 300) (PGRST203 arm reverted in SP-2e1f `b0786e4`) instead of the text anywhere in the body; lab-diff pins the inspiration-photo reference orders. Follow-ups recorded in the row. Rule revert `4d618f4` deployed (`c40190d9`).
- SP-2a3 second attempt: 12 framed controls generated on Runway (192 credits, `negative-controls.md`), labelled blind by a viewer; the lead labelled one close call (`neg-N3-2`, dots meeting tip to tip) split. 6 split, 6 one piece. Implementer is calibrating wording 4 against 47 images, 5 reads per split image, pass bar 0 misses of 80, cap USD 6.
- Sixth adversarial pass on `8c509b0..03c6d85`: the still route plumbing held on every claim (no path to free, stencil prompts byte-identical, classifier as advertised, build and lab-diff clean). Two majors are in the film job, which is off (`VIDEO_ENABLED=0`): the PGRST203 arm guarded a caller that cannot get it while the film caller that can has no terminal path, and about 18 film operator words are for codes the film job can never store. Folding as SP-2e1f by deletion; the missing film terminal path is its own row FILM-1.
- SP-2e1f (`b0786e4`, deployed `9cfec9d9`): the dead PGRST203 arm and 18 film operator words no film failure can store are deleted; FILM-1 records the film job's missing stop-code path; D-023 names all six deployment-scoped final codes. Seventh adversarial pass on `8c509b0..b0786e4`: nothing above minor, the first clean pass; its minors folded as SP-2e1g.
- SP-2e1g (`9031b6f`, deployed `7a3a50e8`) and eighth adversarial pass on `8c509b0..9031b6f`: nothing above minor, the second clean pass in a row, so SP-2e1 and its folds are closed; minors folded as SP-2e1h. Recorded, not fixed: `#snapshotStillRoute` reads the whole snapshot row (up to 16 KB) about six times per four-view run so a pre-migration database cannot 400 it, on an org over its egress allowance.
- SP-2a3 done with wording 4 (implementer, votes re-read by the lead): the joint that holds a word must be a visible band of metal, and a dot or diamond on a point, corner or tip floats. 0 of 80 split reads accepted (every split image FFFFF, framed controls included), 6 of 89 one-piece reads refused (all lone dissents), framed dependent stills 12 of 12. `v2/r-fat-studio` still needs a viewer re-score (FFF under both rules). Vision reads at high detail timed out 16 of 48 at six workers; retries at two all succeeded (follow-up: concurrency or timeout).
- SP-2e2 platform lane: migration `20260924030000_dependent_prompt_authority_variables.sql` (applied live, `create_prompt_release` allows but never requires `glyph_rule`, `bridge_rule`, `rings_rule`, `spelling_rule`; one line of the function changed, read back from the live database). `image.worn`, `image.macro_gift`, `image.dark_editorial` published as `@v4` (the live database already had `@v3`) through the operator console's two RPCs, now scripted as `corepack pnpm --filter @jewelo/jobs publish-prompt`; lab-diff before publishing: 24 stencil dependent prompts byte-identical; new tasks pin `@v4`, existing tasks keep their pins. Staging already runs SP-2f1 (`8c509b0` is in the deployed `6e767b9`), so the deployed compiler fills the new templates. `STILL_FREE_ROUTE=0` added to `.env` and `.env.staging` (flipped to 1 for staging by the lead after 0f11534).
- SP-2e2 code lane (implementer): `STILL_FREE_ROUTE` is validated config (default 0, in the deploy env contract); `studioStillRoute(revision)` on the repository replaces the pinned constant and returns stencil while the switch is off, `stillRoute` otherwise; the route/reference pairing is asserted before the attempt budget and the reservation (`still_stencil_required`, `still_free_route_carries_stencil`, `still_master_required`, the last now deterministic with words); D-024 written, D-010 points to it, the contract's still graph and verification describe both routes, and the CLAUDE.md stencil sentence covers both. Build 0; lab-diff 102 MATCH, 0 DIFFER, including the switch routing Omar free and فاطمة stencil when on and both stencil when off.
- Live free-route proof on staging (`2981c567`, `0f11534`, STILL_FREE_ROUTE=1): run `b50c0bd2` classical yellow Omar and run `19565f93` diamond-rails yellow محمد, each four views ready at attempt 1 on the free route, piece reader one piece and name reader exact on all eight, same piece across the four views in my browser (`dogfood-2026-09-24/live-free-omar/`). Found live: the studio photograph broke about five minutes after the run settled (signed link expired, page had stopped reading) - fixed as SIGN-1.
- SP-2e2b (implementer): Arabic free letters narrowed to the six the labs drew (ح د س ل م ى), stones and a named lettering face route stencil, a studio release whose template mentions the stencil routes stencil (live `image.packshot@v4` does not, checked; only old `@v2`/`@v3` tasks do), the pre-spend pairing assertion now compares the stored compiled text with the request, the route is written into the verification record, D-024 and the contract describe the real allowlist, the runbook says how to turn the switch off and that compiled free snapshots stay free, and CLAUDE.md carries the real daily caps. Build 0; lab-diff 126 MATCH, 0 DIFFER.
- `b7a3c23` (SP-2e2b and SIGN-1) deployed to staging (`44e3eb5a`, health and readiness passed, STILL_FREE_ROUTE=1).
- SP-2a3 wording 4 fails its held-out check (`reader-cal` scope `held`, 15 split images x 5 reads, 7 one-piece x 3, USD 0.84): 16 of 75 split reads accepted. Framed `held-H2` (محمد hung from the top bar on a hair-thin wire) accepted 10 of 10; framed `held-H1a` (سلمى alif tip touching the top bar at one point) accepted 6 of 10, the exact case the rule names. Diamond-rails, inner-rail and classical held-out images all refused 5 of 5; one-piece false refusals 1 of 21. Labels were fixed before the run and are not changed after it. Consequence: the reader cannot be trusted on point or hair-thin joints in framed pieces, and framed-minimal Arabic was on the free route, where the reader is the only guard. SP-2e2c (implementer) moves framed-minimal Arabic back to the stencil; SP-2a4 is the reader wording with a fresh held-out set. The previous wording (SP-2a2 rule, `4d618f4`) is being read on the same held-out set to decide which wording stays live.
- Live stencil-route order on staging with the switch on: `ليلى` classical, request `5e02649a`, studio task `a2ca2eba` recorded `still_route = stencil` and was refused pre-spend with `identity_ring_tilt_too_steep` (19.9 degrees, max 15; reproduced locally). The shopper saw "The shop will photograph it and send it to you" on all four views. Not a regression: the ring gate is D-register behaviour and this is its first live hit. Follow-up (product, owner next session): ليلى, ليلي and لبنى are refused in classic, minimal, diwani and signature lettering and pass only in kufi (a tall lam at one end and a low final ya at the other put the two ring holes too far apart vertically); Layla is a common name, so the ring seat for names like this needs a fix. The three dependent views stay `queued` until the stale sweeper closes them as `dependency_blocked`, which is how all 57 earlier blocked-parent views resolved.
- Live stencil-route proof, switch on (`b7a3c23`): run `217cb812` classical yellow `زينب` (dotted, routes stencil). Every task snapshot `still_route = stencil`; the studio passed the new pre-spend pairing assertion and was ready at attempt 1, spelled right, one piece, every dot held by a stem. Three live findings from this one run:
  - RETRY-SIGN (major, pre-existing since `8b72988`, now frequent): provider input URLs are signed once per dispatch (300 s) and reused by every in-place regeneration; close_up attempt 3 was sent about 305 s after signing and failed `openai_input_download_failed:400`, ending the view blocked (on_skin's attempt 3 went at about 264 s and succeeded). Implementer lane RETRY-SIGN re-signs per attempt in `apps/jobs/src/presentation.ts`.
  - RUN-REVIEW (major, web): `refresh_run_status` sets a run to `operator_review` as soon as any still task is blocked, with siblings still generating; the page treats `operator_review` as stopped, so dark showed "The shop will photograph it and send it to you" while it was generating attempt 2. Folded into the SIGN-1b lane (`personalizedRun.ts`).
  - READER-DOTS (major, product): wording 4 refused 5 of the first 6 dependent-view attempts of زينب (USD 1.05). The stencil draws a real bar between ي's two diamonds and a short bar from ز's diamond, but the on-skin, close-up and dark photographs shrink those bars to corner contacts, which wording 4 calls floating. By eye the refusals are borderline, not wrong. Earlier dotted stencil runs under the SP-2a2 rule (فاطمة) passed at attempt 1. Evidence for SP-2a4, which must hold both lines: refuse framed point and wire joints, and not make dotted names cost three attempts per view.
- Second adversarial pass on `52096887..b7a3c238`: no blocker, 2 majors (a customer inspiration photo reaches a free-route model unlabelled - latent, 0 of 102 revisions carry one; SIGN-1 moves the broken-photo cliff from 5 to about 35 minutes), 6 minors. Clean-pass count for SP-2e2 restarts. Folding as SP-2e2d (routing and docs) and SIGN-1b (web), in parallel with RETRY-SIGN.
- The SP-2a2 rule on the held-out set: 20 of 75 split reads accepted, 3 of 21 false refusals - worse than wording 4 on both, so wording 4 stays live.
- زينب run `217cb812` settled: studio, on-skin (attempt 3) and dark (attempt 2) ready, the same زينب one piece in all three in my browser; close-up blocked by RETRY-SIGN. SIGN-1 held live inside its window (studio still loading 9.5 minutes after it first showed). Evidence `dogfood-2026-09-24/live-stencil-zaynab/01-04`. Dark's thumbnail strip shows only the cloth, not the pendant (minor, crop).
- Lanes landed (implementers, reviewed by the lead): RETRY-SIGN, SIGN-1b (with the `operator_review` reading), SP-2e2d. Build 0, lab-diff 164 MATCH, 0 DIFFER.
- Deployed `7a90316` to staging (`2836defa`, ACTIVE 13:30Z; health and readiness passed): SP-2e2c, RETRY-SIGN, SIGN-1b, SP-2e2d live.
- Sanchay (24 Sep, evening): every name should work as simply as Omran typing a name into ChatGPT - the stencil and font only guarantee unbroken letters, the model's creativity does the rest. Started UNIV-1 (image-lab): the production free studio prompt on 16 common UAE Arabic names in classical and 6 in diamond-rails, 2 takes each, blind-scored next, to widen the free route as far as the evidence allows tonight; names that fail keep the stencil look (they still get four photographs).
- Third adversarial pass on `b7a3c238..7a90316f`: no blocker, 2 majors - M1 SIGN-1b's watcher dies for good if a settled run moves again after 30 minutes (operator retry); M2 diamond-rails Arabic stays free although the point-contact and hair-thin-wire joints the reader failed on framed pieces were never drawn on rails - and 5 minors. Clean-pass count restarts. Folding: SIGN-1c (web: M1, m3, m5), SP-2e2e (jobs: m1 Arabic rose to stencil, m2 route reasons recorded, m4 fail closed); M2 measured by four diamond-rails controls added to UNIV-1.
- Live run `d078a258` (يوسف classical, stencil route, on `7a90316`): the page labels views correctly mid-run (studio and close-up ready at attempt 1, dark on attempt 2 after a one-piece refusal and on-skin shown "being made", run `partial`). New finding RING-POST (major, identity engine, pre-existing): the stencil seats the left ring on a tall vertical post rising from the final ف, which reads as an alif - the studio photograph reads يوسفا to an Arabic reader, the model copied the stencil faithfully and the name reader passed it. Reproduced locally with `renderIdentityAnchor` (classical, contemporary). Same family as LAYLA-1 (ring seat for names whose ends sit low); folded into that row.
- Run `d078a258` (يوسف) settled: studio and close-up ready at attempt 1, dark at attempt 2, on-skin blocked `identity_not_one_piece` after 3 attempts (USD 1.47 in all). RETRY-SIGN proven live: on-skin attempt 3 was sent at 13:52:58, about 300 s after the first signing, and ended with a reader verdict instead of `openai_input_download_failed`. READER-DOTS again: 4 of 7 attempts refused as not one piece on a dotted stencil name.
- UNIV-1 generated (image-lab, 48 stills, 816 Runway credits, prompts checksum-identical to production's free compile). Production readers replayed on them (USD 0.83, 3 timeouts): the name reader said "matches" on all 45 it read, including stills that look misspelled at contact-sheet scale (an extra lam in عبدالله b, a leading hook reading as ل on حمدان, حصة, خالد, شيخة, شمة) - it is told the approved name before it reads (`packages/ai/src/studio.ts` OpenAINameReader prompt "The approved name for this pendant is ..."), so it is not a blind check; the piece reader refused 36 of 45 (classical 4 of 32 pass) because the free prompt draws dots as loose diamonds, and it accepted one of the four diamond-rails point-contact controls. Blind viewer scoring running. UNIV-2 (image-lab) tests two stronger dot-join wordings on 8 dotted names on Runway.
- Sanchay (24 Sep): all image generation for testing goes through the Runway MCP; production takes the same prompt. Staging orders spend API money (USD 7.98 on 24 Sep, 8 runs, mostly agent proof orders); from now on at most one confirming order per deploy.
- UNIV-1 blind verdict: spelling 48 of 48 (my own contact-sheet suspicions of misspellings were wrong); one piece 28 of 48 - diamond-rails 12 of 12, classical 15 of 32 (floating dots of ش ث ن ز, two split words). The production piece reader let no broken still through (0 false accepts, three rail split controls refused) but refused 16 good ones. My viewer brief wrongly called diamond-rails "set with diamonds"; stones come only when ordered.
- UNIV-2 blind verdict (same session with the UNIV-1 baseline): wording A 14 of 16 spelled and one piece against 8 of 16 for the production wording; B 13 of 16. Wording A taken. UNIV-3 (12 names covering ق ك ج غ ض ظ ذ ت ؤ أ إ آ): rails generated and being scored; classical with wording A generating. UNIV-4 (implementer): wording A into the compiler for Arabic free prompts and the free route opened to every standard Arabic letter in classical and diamond-rails; framed and origami stay stencil. Reader replay on the wording-A stills running (about USD 0.30).
- UNIV-3 rails blind verdict (12 names covering ق ك ج غ ض ظ ذ ت ؤ أ إ آ, production free prompt): spelling 20 of 24 (both ذكرى takes draw an extra shape, one جميلة drops the dot of ج and reads حميلة, one تسنيم puts three dots on ت and reads ث), one piece 23 of 24, four short letters welded to one rail only (still one piece; accepted as a build choice).
- NAME-BLIND (major, found by measurement): the production name reader is told the approved name before it reads and decides `matches` itself; on the wording-A still `classical-shaikha-ar-A-a` (a blind viewer reads شبخة, medial ي with one dot) it answered "شيخة", matches, and the piece reader passed it - a wrong name would have reached the shopper. It is the only spelling gate on the free route. Implementer lane NAME-BLIND: the model transcribes blind, code compares with a narrow normalisation; measured against the blind viewer labels on the UNIV-2 and UNIV-3 stills before the free route is widened. Piece reader on wording A: one piece on 10 of 15 read (was 9 of 45 on the production wording).
- UNIV-4 committed `cc62ff3` (not deployed): wording A in the compiler for every Arabic free studio prompt, free route open to every standard Arabic letter in classical and diamond-rails, position rules gone; lab-diff 191 MATCH, 0 DIFFER; build 0; no prompt release needed (`name_spelling` is a compiler variable).
- UNIV-3 rails reader replay (USD 0.42): the name reader matched both readable misspellings (جميلة read as correct where the ج has no dot; تسنيم read as correct with three dots on ت) - NAME-BLIND is a deploy prerequisite; the piece reader refused 16 of 21 rails stills the viewer measured whole, all for diamond dots on their tips. UNIV-5 (image-lab) measures rails with wording A compiled by HEAD.
- UNIV-3 classical with wording A, blind verdict: one piece 22 of 24 (wording A holds the structure) but spelling 18 of 24 - dot counts: ق drawn with one dot on both قاسم takes, an extra dot under م on أحمد and كريم, one dot under ي on تسنيم, an extra dot under the final ن on مؤمن. UNIV-6 tests a per-name dot inventory sentence ("Its dots, exactly and only: two above ق ...", "This name has no dots at all") on the 8 names that failed; the six failing stills added to the NAME-BLIND measurement.
- NAME-BLIND measured (OpenAI replays, USD 1.44 in all, scratch only): a reader that transcribes without being told the name refused every misspelling it read (5 of 5) but also refused about two thirds of correct names, often answering a different common name (قاسم read as فاطمه, ظافر as محمد, كريم as علي) and on 35 of 56 first reads returned no text at all. Neither reader can guard every Arabic name: the told-the-name reader passes dot errors, the blind one cannot read full-frame calligraphy. The NAME-BLIND code stays uncommitted in the tree (`packages/ai/src/studio.ts`, `packages/config/src/index.ts`, `apps/jobs/src/presentation.ts`), never enabled.
- UNIV-4 reverted (`106f8bed`): the free route is back to the six proven Arabic letters with their position rules and the production free wording; wording A and the widening come back only with a spelling gate that works (NAME-2) and a dot fix (UNIV-6). Lab records UNIV-5 and UNIV-6 committed `2e6c50ff`; build exit 0 on the committed tree; staging deploy of SIGN-1c and SP-2e2e from `2e6c50ff` running.
- UNIV-5 (rails, wording A, 8 hard dotted names): blind viewer one piece 16 of 16, spelled 10 of 16 - all six failures are marks (extra dot, hamza as a ball, madda as a hamza hook), the same extra dot under م on both كريم takes. Piece reader accepted all 8 it read (the tip-set refusal is gone with wording A); the blind name transcriber read 0 of 8. Dots remain the one failure mode; UNIV-6 (dot inventory) is being scored.
- Deployed `2e6c50ff` to staging (deployment `c195a33b`, ACTIVE 9/9; smoke health and readiness passed): SIGN-1c and SP-2e2e live, the every-name widening not. SIGN-1c proven live in my browser on settled run d078a258: all five image links re-signed at about 240 s, before their 280 s expiry, 0 broken (`dogfood-2026-09-24/live-sign1c/`). No paid order placed for this deploy.
- Fourth adversarial pass on `7a90316..2e6c50ff`: nothing wrong reaches a shopper; the revert of cc62ff3 is byte-exact outside docs. M1 (cost): past the 30 min watch window a settled run that un-settles polls `/api/state` every 3 s for as long as the tab is open (about 14 Supabase requests each). Minors: `identity_resign_unavailable` unreachable with the real repository, `stillRouteReasons` recomputed at write time rather than stored with the route, loose lab-diff reason checks, a stale comment in `api/operator/review-runs/route.ts`. SIGN-1d (implementer) folds M1, the lab-diff checks and the comment; clean-pass count restarts.
