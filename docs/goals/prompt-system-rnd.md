# Reusable jewelry prompt research

User approved the complete implementation plan on 6 September 2026. This task supersedes the UI goal only in this isolated worktree. Branch: codex/prompt-system-rnd. Parent checkout and its ongoing UI work are preserved.

Objective: build a deterministic variable compiler and immutable Runway GPT Image 2 research package, preflight four construction references, execute staged comparisons, and report measured first-attempt correctness truthfully.

Authorized provider: Runway MCP gpt-image-2, workspace 1068827 Sanchay, existing credits only. Maximum campaign 2,832 images (48 screening, 1,024 development, 96 ablation, 64 regression, 1,600 qualification). Max two active submissions; reconcile credits every 20; stop at 50 submissions while suspected limit/reset remains unverified. No credit purchases. Initial balance observed 308802. No direct API or native ImageGen calls.

Stopping conditions: completed qualification package with 400/400 complete four-view unseen requests passing, OR an explicit supported stop condition (invalid reference, exhausted bounded development, provider limit, unresolved quota or failed qualification) with remaining work identified. A supported stop is NOT a claim that qualification succeeded.

Retain 22/32 mm as nominal labels, no dimensional certification. All current form choices are target coverage, not a statement that geometry support exists. English calligraphic names use Latin adaptations. Preserve production adapters, identity implementation, customer UI, verification, and archives. No deployment or merge.

Proof packet: reviews/2026-09-06-prompt-system/report.md, test output, immutable manifests and gallery. Scripts are R&D only and cannot invoke production workflows.

## Current stopping record

Screening infrastructure and 48-image Runway comparison completed. All 48 original outputs retained; 30 overall passes, 14 rejects and 4 uncertainties. Observed workspace debit 960 credits; final reconciled balance 307842. The candidate is not qualified. Complete assembly is the next development candidate (14/16), not a production release.

Paid work is paused at the accepted unresolved quota/reference gate: 48 submissions leave only two under the conservative 50 boundary, insufficient for one complete four-view development request. The missing full-form reference packages and later execution remain listed in the proof report. No automatic continuation, production integration, deployment or merge occurred.
